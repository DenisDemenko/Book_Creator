# Загальний план розробки: 11 сторінок + Fusion Living Characters (Harness + Jev, FLC 2.0)

> ✅ **Затверджено власником 24.09.2026** (сесія Claude #01, журнал #239):
> план, рішення Н1–Н5 за рекомендаціями. **Версія 2 (журнал #240)** — прийнято
> доповнення власника «Fusion Living Characters 2.0»: безперервне життя
> персонажів, суб'єктивна пам'ять, три рівні Jev, три типи секретів, Causality
> Engine, перший продукт «Допит живого персонажа», етап перевірки якості;
> тепер **35 задач**. Бази даних — як вирішено (К1: ядро в PostgreSQL, решта
> в SQLite). Система тегів обговорена (журнал #241, `TAGS_ANALYSIS.md` §7):
> розширення — окремою задачею Т0.10. Задачі — у `task.md`.
> Першоджерела — `docs/tz/`.
>
> Об'єднує два ТЗ власника:
> - **ТЗ-11** — «Fusion Lab Studio — ТЗ на 11 окремих сторінок» v1.0
>   (`docs/tz/TZ_11_pages_v1.0.md`); докладний
>   план — `PLAN_ENTITY_FEATURES.md` (v2, рішення К1–К7);
> - **ТЗ-H** — «Fusion Lab Studio — ТЗ інтеграції DeepSeek Harness +
>   TypeSafe Jev» v1.0 (модуль Fusion Living Characters / Magic Characters;
>   `docs/tz/TZ_Harness_Jev_v1.0.pdf`, текст — `.txt` поруч);
> - **FLC 2.0** — «Fusion Living Characters 2.0 — доповнення до ТЗ Harness +
>   Jev» (`docs/tz/TZ_FLC_2.0_addendum.md`).

## 1. Як два ТЗ стикуються

ТЗ-H будується **поверх** ядра з ТЗ-11 і не може стартувати раніше за нього:
його схема прямо посилається на `entities`, `entity_mentions`,
`entity_relations` і PostgreSQL. Половина компонентів ТЗ-H — це ті самі
частини, що потрібні сторінкам ТЗ-11, тож кожну будуємо **один раз**:

| Компонент ТЗ-H | Той самий компонент у ТЗ-11 | Задача плану |
|---|---|---|
| Story Core (PostgreSQL, UUID, статуси, версії) | Семантичне ядро, 13.1 | Т0.3–Т0.6 |
| Hybrid Retrieval (full-text + vector + graph) | Сторінка 1, пошук | Т1.2 |
| Retrieval Service (усі згадки героя, інкрементально) | Синхронізація 13.4 | Т0.6 |
| Character Profile Builder (confirmed / suggested / contradicted / unknown) | Сторінка 3, профіль | Т1.5 |
| «Стан героя на момент сцени, без спойлерів» | Сторінка 3 + сторінка 6 (час світу, `/revelation`) | Т1.5, Т2.1 |
| Continuity & Knowledge Check | Сторінка 8 | Т2.4 |
| Causality Engine (FLC 2.0 §5) | Залежні сутності гілки (сторінка 5), причинність у сторінці 8 | Т2.9 |
| Суб'єктивна пам'ять і її інвалідація (FLC 2.0 §2) | Синхронізація 13.4 (`needs_review`), знання в часі (сторінка 6) | Т2.6, Т0.6, Т2.1 |
| Isolated Draft / Simulation → Author Review → canon | Сторінка 5 (гілки), diff і затвердження фрагментів | Т3.3 |
| Job queue (job_id, progress, cancel, retry, idempotency, бюджет) | Фонові задачі 13.5 | Т0.7 |
| Права й ізоляція проєктів, RBAC | Сторінка 10, 13.5 | Т0.1, Т0.8, Т3.1 |

## 2. Що з'ясовано про Harness і Jev (перевірка джерел 24.09.2026)

- **DeepSeek Harness** — відкритий (MIT) агентний каркас на Node.js,
  «everything is a plugin», статус **Developer Preview з попередженням про
  несумісні зміни**; запуск `npx @deepseek-ai/dsh web` (веб-інтерфейс на
  127.0.0.1:3080). Задуманий передусім як агент із доступом до файлів і
  оболонки — ТЗ-H це прямо забороняє в продакшені книги. Програмний інтерфейс
  для виклику з іншого бекенда з README не очевидний — це питання прототипу (Т1.6).
- **TypeSafe Jev** — модель рішень, **не пише текст**: питання Choice,
  Score (з розподілом і confidence) і Noul (0–1). Контекст ~32 тис. токенів,
  до 255 варіантів на питання, без потокової відповіді. Доступ: пряме API
  TypeSafe (`TYPESAFE_API_KEY`), TypeScript-SDK `@typesafe-ai/sdk`, а також
  через OpenRouter (Decisions API, альфа) і Cloudflare Workers AI. Ціна —
  частки цента за виклик.
- У Студії вже є провайдер **DeepSeek** (`server/chatProviders.ts`), тож LLM
  для агентів і автора сцени підключається через наявне ядро ШІ
  (`aiCore.generateText`, `resolveModuleModelId`).

Висновок для архітектури: Harness підключаємо **лише через адаптер**
(`AgentRuntime`), з власною реалізацією оркестрації в Node як основною для
MVP і Harness як другою реалізацією за перемикачем — рішення після
прототипу (Т1.6). Так виконується критерій ТЗ-H №10 («оновлення Harness не змінює UI і
Story Core») і не ставимо продакшн у залежність від Developer Preview.

## 3. Рішення для ТЗ-H (ухвалено за рекомендаціями 24.09.2026)

Н1–Н2 остаточно підтверджує прототип Т1.6: якщо він покаже, що Harness не можна
безпечно викликати з бекенда, MVP іде на власній оркестрації, а Harness
лишається необов'язковою реалізацією адаптера.

| # | Питання | Рішення |
|---|---|---|
| Н1 | Де працює Harness | Окремий сервіс (sidecar) на Railway у приватній мережі, без інструментів файлів/оболонки/мережі; основна оркестрація MVP — наша (`InProcessRuntime`), Harness — друга реалізація `AgentRuntime` після прототипу Т1.6 |
| Н2 | Як звертаємось до Jev | Напряму до TypeSafe через TS-SDK, ключ лише на сервері; OpenRouter — запасний шлях |
| Н3 | Моделі для агентів і автора сцени | Наявні рушії Студії через ядро ШІ; окремий модуль і модель для кожної ролі (агент, режисер, автор сцени, профіль, безперервність) |
| Н4 | Ключ шифрування таємниць | Змінна середовища Railway поза базою (менеджера секретів у Студії немає); чесне повідомлення автору, що адміністратор інфраструктури технічно має доступ (ТЗ-H §9) |
| Н5 | Бюджети | Ліміт токенів і запитів на проєкт і на симуляцію; доступ до «Магічної сцени» — за тарифом |

## 3а. Fusion Living Characters 2.0 — що змінилось у плані

Концепція: не генератор окремих сцен, а **безперервне життя персонажів** —
пам'ять про пережите, зміна переконань і намірів, вплив на сюжет; канон
змінюється лише після затвердження автором. Для плану це означає:

| Доповнення FLC 2.0 | Як враховано |
|---|---|
| Суб'єктивна пам'ять: світовий факт, знання, переконання, спогад, наслідок; джерело, час світу, статус, прогін або ревізія канону | Т2.6 переписано на `CharacterMemoryService`; інвалідація за ревізією через `core_sync` (Т0.6) |
| Три рівні Jev: стратегічний, сценічний, тактичний, кожен зі своїм кешем | Т2.5 |
| Три типи секретів + Mystery Director | Т4.1 |
| Causality Engine | Нова Т2.9 — одна служба для сторінок 5, 8 і симуляцій |
| Перший продукт — «Допит живого персонажа» з шістьма критеріями | Т2.7 з критеріями FLC 2.0 §7 |
| Етап перевірки якості (контрольний набір, з Jev / без Jev, час і вартість) | Нова Т2.8 — ворота перед автономною сценою |
| Технічний прототип повного циклу з mock-Jev і fallback | Т1.6 розширено з «спайку» до прототипу циклу |
| Після затвердження — оновлення канону **й пам'яті**; наступна сцена стартує з нового стану | Т2.7, Т3.5 |
| Етапи FLC 0–5 | Вплетено: 0 → етап 1 (Т1.6), 1–2 → етап 2 (Т2.5–Т2.8), 3 → етап 3 (Т3.5), 4 → етап 4 (Т4.1, з Т2.9), 5 → етап 4 (Т4.2) |

## 3б. Теги — рішення власника (журнал #241)

Обговорено за `TAGS_ANALYSIS.md` (§7 — рішення): **П1** — правило найближчого
героя + необов'язкове `@Ім'я`; **П2** — поля значення через « — » (м'яко,
вільний текст лишається правильним); **П3** — підтверджена пропозиція ШІ
одразу стає тегом у тексті; **П4** — загального інваріанту для AI-правок не
буде (ризик прийнято); **П5** — у перекладі ключ той самий, значення за
глосарієм; **П6** — перейменування пропонує заміну; **П7** — теги в
чернетках AI пропозиціями; **П8** — таємниці до розкриття не з'являються в
тексті, після — пропозиція `/revelation`; **П9** — з вад виправляємо лише
В2. Розширення синтаксису — окремою задачею Т0.10 з корпусом тегів для
регресії; В2 — Т0.11.

## 4. Етапи

| Етап | ТЗ-11 | ТЗ-H | Результат для показу |
|---|---|---|---|
| **0 — Основа** | безпека, PostgreSQL, ядро, ID абзаців, синхронізація, черга, маршрути | — | Ядро на сервері наповнюється після кожного збереження книги |
| **1 — Семантичне ядро** | сторінки 1, 2, 3 | FLC 0 — технічний прототип циклу | Пошук, граф, профіль героя; один повний цикл retrieval → Jev → LLM → чернетка |
| **2 — Аналіз історії** | сторінки 6, 4, 7, 8 + причинність | FLC 1 — «Допит живого персонажа» з пам'яттю; FLC 2 — перевірка якості | Хронологія, емоції, ілюстрації, безперервність, причинність; допит героя з пам'яттю й звіт якості |
| **3 — Розвиток і виробництво** | сторінки 10, 11, 5, 9 | FLC 3 — 2–3 автономні персонажі, безперервне життя | Команда, переклад, гілки, вправи; сцени поспіль, що пам'ятають одна одну |
| **4 — Секрети, прискорення, приймання** | приймання ТЗ-11 | FLC 4 — три типи секретів, Mystery Director; FLC 5 — прискорення | Усі три документи пройдено за своїми критеріями |

Розмір задачі: **S** — кілька днів, **M** — до двох тижнів, **L** — більше.
Кожна задача — окремий запис журналу, модульні тести (`test:*`) і, де є
інтерфейс, живий прогін (`live:*`) у ширинах 360 / 768 / 1280 px і двох
темах.

## 5. Задачі

### Етап 0 — Основа

**Т0.1 · Безпека WebSocket (S).** Сесія з cookie під час підключення до `/ws`,
перевірка права на книгу, роль — із сервера, а не з повідомлення; книга
кімнати — лише учасникам із правом.
Готово: негативний тест — чужий клієнт без сесії чи права не отримує книгу.

**Т0.2 · Права на маршрутах ШІ (S).** `requirePermission('canUseAi')` і квоти
на `/api/ai/translate`, `/api/ai/coach-feedback`, `/api/ai/generate-exercise`,
`/api/ai/analyze-emotional-arc`.
Готово: гість отримує 403, звичайні сценарії не змінились.

**Т0.3 · PostgreSQL для ядра (M).** База на Railway з `pgvector`, підключення
(`pg`), міграції SQL-файлами з версією, шар репозиторіїв з реалізацією в
пам'яті для тестів; SQLite Студії не чіпаємо (К1).
Готово: міграції накочуються на порожню базу й повторно без помилок; тести
ядра проходять без PostgreSQL.
Залежить: рішення К1 (ухвалено).

**Т0.4 · Схема ядра (M).** `projects`, `documents`, `paragraphs` +
`paragraph_versions`, `entities` + `entity_aliases`, `entity_mentions`,
`entity_relations` + версії, `analysis_runs` + `analysis_findings`,
`project_members`; `project_id` і індекси всюди; статуси
suggested / confirmed / rejected; поля ТЗ-H для висновків
(`source_paragraph_ids`, `source_revision`, `valid_story_time`, `visibility`,
`created_by`).
Готово: тест — запис без доказу для AI-висновку відхиляється; зміна статусу
пише версію.
Залежить: Т0.3.

**Т0.5 · Стабільні ID абзаців (M).** Атрибут `pid` у редакторі (новий UUID для
нового блока й другої половини після розбиття, id першого при злитті),
`Section.paragraphIds`, присвоєння id наявним книгам під час першого
відкриття.
Готово: тест — правка, розбиття, злиття, переміщення зберігають id
незмінних абзаців; текст рукопису не змінився.

**Т0.6 · Синхронізація після збереження — `core_sync` (M).** Сервер
зіставляє блоки з `paragraphs` (id, запасний шлях — хеш), пише версії
змінених, згадки з тегів (`confirmed`) із суб'єктом (П1: `@Ім'я` або
правило найближчого `/character`) і полями значення (П2) — розбором із
Т0.10; позначає `needs_review` висновки AI **і спогади персонажів**, чиї
абзаци змінились; записує сповіщення про вплив (ТЗ-11 13.4). Це й є
Retrieval Service ТЗ-H (інкрементальне оновлення). Теги лише читаються.
Готово: після збереження книги з тегами — абзаци й згадки в базі за
секунди; повторне збереження без змін нічого не пише; текст рукопису
байт-у-байт той самий.
Залежить: Т0.4, Т0.5, Т0.7, Т0.10.

**Т0.7 · Фонова черга задач (M).** Таблиця задач у PostgreSQL: `job_id`,
`progress`, `cancel`, `retry`, ключ ідемпотентності, ліміт частоти, бюджет
токенів і запитів на проєкт (вимога ТЗ-H §11 і ТЗ-11 13.5); за зразком
`server/etsy/publishQueue.ts` (відновлення після перезапуску).
Готово: задача переживає перезапуск сервера; скасування зупиняє її; бюджет,
що вичерпано, не пускає нову.
Залежить: Т0.3.

**Т0.8 · Маршрути сторінок і API проєкту (M).** Синхронізація вкладки й
`projectId` з адресою `/studio/projects/:id/<сторінка>` (К3); 11 порожніх
сторінок у меню; `requireProjectAccess` на всіх `/api/projects/:id/*`;
перші маршрути `GET entities`, `entities/:entityId`, `relations`.
Готово: адреса кожної сторінки відкривається напряму й кнопкою «назад»;
чужий проєкт — 403.
Залежить: Т0.4.

**Т0.9 · Три ролі AI в ядрі ШІ (S).** Модулі AI-1 / AI-2 / AI-3 у
`CORE_MODULE_KEYS` з окремими моделями (`resolveModuleModelId`); кожен
виклик — `analysis_runs` з витратою в `usage_log`; серверний валідатор схем
відповіді (JSON Schema).
Готово: адмінка показує модель кожної ролі; відповідь, що не пройшла
схему, не зберігається.

**Т0.10 · Розширення тегів без поломок: `@Ім'я` і поля значення (M).**
За рішеннями П1, П2 (`TAGS_ANALYSIS.md` §7): розбір необов'язкового `@Ім'я`
в кінці значення (суб'єкт) і полів через « — » у порядку характеристик
реєстру — **в одному місці** (`coreEntities.ts`), яке читають редактор,
приховування, експорт, чат, тренажери й сервер; слеш-меню підказує порядок
полів і `@Ім'я`; **корпус тегів** — тестова книга з усіма формами
(канонічні, сирі, українські, розрізані форматуванням, діалогові, з `@`, з
полями, невідомі ключі), на якій разом ганяються розбір, чипи,
«Сховати сутності», експорт і нормалізація.
Готово: кожен тег корпусу, що розбирався до зміни, розбирається так само
(регресія); `@Анна` і поля видно в підказці чипа; експорт не лишає `@`.
Залежить: нічого — можна одразу.

**Т0.11 · Пошук і заміна з урахуванням тегів — В2 (S).** «Знайти й замінити»
показує, скільки збігів у тегах, і дає вибір: лише текст / лише теги / усе.
Готово: заміна «Олена» → «Олеся» в режимі «лише текст» не змінює
`[/character:Олена]`; у режимі «усе» — змінює обидва.
Залежить: нічого.

### Етап 1 — Семантичне ядро (сторінки 1–3) + прототип FLC

**Т1.1 · AI-1: пропоновані згадки (M).** `entityClassify` для змінених
абзаців: згадки й первинні зв'язки зі статусом `suggested`, зіставлення з
наявними `entities` (псевдоніми → один UUID). Рішення П3: **підтвердження
пропозиції одразу записує тег у рукопис** — редактор у браузері ставить його
на початку абзацу за `paragraph_id` (з суб'єктом `@Ім'я`, якщо потрібно, і
лімітом 12 на абзац), далі звичайне збереження → `core_sync`; відхилена
пропозиція не повертається; без підтвердження текст не змінюється.
Готово: нерозмічений розділ отримує пропозиції з доказами; «Підтвердити»
додає тег у потрібний абзац і тільки його; відхилене не пропонується вдруге.
Залежить: Т0.6, Т0.9, Т0.10.

**Т1.2 · Гібридний пошук — Retrieval (L).** Повнотекстовий (`tsvector`),
векторний (`pgvector`, ембединги лише змінених абзаців), графовий (фільтри за
згадками й зв'язками); злиття результатів з поясненням джерела; обмеження
правами. Один сервіс для сторінки 1 і для ТЗ-H.
Готово: запит «персонаж + емоція» повертає абзаци з точними `paragraph_id`.
Залежить: Т0.6, Т1.1.

**Т1.3 · Сторінка 1 — Розумний пошук (M).** Запит природною мовою
(`searchInterpret` лише розкладає на фільтри), фільтри ТЗ, контекст і
пояснення, перехід у редактор, збережені запити.
Готово: критерій приймання сторінки 1.
Залежить: Т1.2, Т0.8.

**Т1.4 · Сторінка 2 — Story Graph (M).** React Flow з розкладкою, кольори
реєстру, підписи зв'язків, пошук і фільтр вузлів, картка сутності,
довантаження сусідів, ручні зв'язки й підтвердження за правами.
Готово: клік по героєві — підтверджені зв'язки першого рівня з джерелами.
Залежить: Т0.8, Т1.1.

**Т1.5 · Сторінка 3 — Профіль персонажа і Profile Builder (L).** Канон
автора і `suggested` окремо; затверджене заблоковане для AI; появи, події,
рішення за сюжетом; арка; «стан на главі N» без спойлерів. Profile Builder
(AI-2) — доказовий профіль зі статусами confirmed / suggested /
contradicted / unknown; спільний для сторінки й агентів ТЗ-H.
Перейменування героя (П6): старе ім'я лишається псевдонімом, система
показує всі входження й пропонує пакетну заміну окремо в тегах і в тексті.
Готово: критерій сторінки 3 + профіль показує джерело кожного твердження
(критерій ТЗ-H №1).
Залежить: Т1.1, Т0.8.

**Т1.6 · Технічний прототип Harness + Jev — FLC етап 0 (M).** За офіційною
документацією перевірити контракти Harness і Jev, закріпити версії; через
наші адаптери пройти **один повний цикл** retrieval → профіль → Jev Choice →
Harness/LLM → чернетка (на тестовій книзі, дані — з наявних тегів, без
очікування ядра); mock-Jev і fallback на структуровану відповідь LLM з
першого дня; з'ясувати, чи можна викликати Harness з бекенда й вимкнути
йому файли/оболонку/мережу (рішення Н1–Н2).
Готово: звіт прототипу — версії, схеми, обмеження, затримка й вартість
одного циклу, рішення, чи Harness іде в MVP.
Залежить: нічого з ядра — паралельно з Т1.1–Т1.5.

### Етап 2 — Аналіз історії (сторінки 6, 4, 7, 8, причинність) + допит живого персонажа і перевірка якості

**Т2.1 · Сторінка 6 — Хронологія (M).** `story_time_points` (точні, приблизні,
інтервали, невизначені); шкали `story_time` і `narrative_order`; флешбеки й
паралельні лінії; `precedes` / `follows` / `overlaps`; попередження про
часові суперечності. Плюс **знання героя в часі**: `/revelation` «кому й коли»
→ що герой міг знати до сцени (потрібне ТЗ-H для knowledge boundary).
Готово: критерій сторінки 6; для героя й сцени сервер повертає список
відомих йому фактів.
Залежить: Т1.1.

**Т2.2 · Сторінка 4 — Емоційний монітор (M).** `emotion_points` (основні,
другорядні, приховані; інтенсивність, майстерність передачі, вплив на
сюжет; джерело й статус); графіки за сценами, главами, часом світу; мітки
подій; ручне коригування; «недостатньо даних».
Готово: критерій сторінки 4.
Залежить: Т2.1 (вісь часу світу), Т1.1.

**Т2.3 · Сторінка 7 — Бібліотека ілюстрацій (M).** Прив'язка до сутностей,
версії зовнішності за віком чи етапом, AI-3 (ознаки, предмети, локації →
`suggested`), порівняння з описом, генерація лише за командою, ліцензія й
авторство, історія.
Готово: критерій сторінки 7 (портрет у профілі героя й пов'язаних розділах).
Залежить: Т1.5.

**Т2.4 · Сторінка 8 — Безперервність і Knowledge Check (L).**
`continuity_issues` з двома доказами й п'ятьма статусами; правила без AI
(предмет, знання, місце, вік, час) + AI-2 по розділах зі станом світу;
повторна перевірка змінених місць. Той самий перевіряльник працює над
чернеткою симуляції (епістемічні суперечності ТЗ-H: герой знає те, чого не
міг знати).
Готово: критерій сторінки 8; перевірка чернетки знаходить навмисний
«витік знання».
Залежить: Т2.1, Т1.5.

**Т2.5 · Адаптери, контракти й три рівні Jev (L).**
`ai/adapters/{jev,harness,llm}`, `ai/contracts/*.schema.json`;
`AgentRuntime` (наша реалізація + Harness за рішенням Т1.6);
`JevDecisionAdapter` з **трьома рівнями** (FLC 2.0 §3): стратегічний
(довгі цілі, конфлікт мотивів, допустимі траєкторії — після значущих подій
чи зміни канону), сценічний (мотиви, емоції, ризики, доступні дії — перед
сценою чи після повороту), тактичний (наступна дія з дозволеного списку —
перед ходом); атомарні Choice/Score/Noul, кеш кожного рівня до своєї події,
збереження з `snapshot_hash`, версією моделі й підставами; нормалізована
відповідь (`selected_action`, `scores`, `raw_distributions`, `confidence`,
`decision_trace_id`); серверний валідатор жорстких обмежень; fallback на
LLM чи рішення автора з позначкою в журналі; mock-Jev.
Готово: критерії ТЗ-H №2 і №9; тест — стратегічний рівень не
перераховується без значущої події.
Залежить: Т1.6, Т0.9.

**Т2.6 · CharacterMemoryService — довготривала суб'єктивна пам'ять (L).**
Таблиці `character_agents`, `character_memories`, `character_states`,
`character_decisions`, `canon_proposals`. Види записів (FLC 2.0 §2):
світовий факт, знання героя, переконання (зокрема хибне), суб'єктивний
спогад (як герой витлумачив подію), наслідок (як змінились довіра, страх,
цілі, стосунки); `world_truth` / `character_belief` / `reader_knowledge`
окремо. Кожен запис: `source_event_id`, `story_time`, `character_id`,
`belief_status`, `visibility`, `simulation_id` **або** `canon_revision`.
Незатверджені спогади одного прогону не бачить інший; **інвалідація за
ревізією**: зміна вихідної сцени (через `core_sync`, Т0.6) позначає залежні
спогади «перевірити». `CharacterSnapshotBuilder` — знімок героя станом на
сцену з профілю (Т1.5), знань у часі (Т2.1) і пам'яті, компактний під ліміт
Jev.
Готово: критерій ТЗ-H №5; приклад FLC 2.0 (сварка Сергія й Анни — дві різні
інтерпретації одного факту) відтворюється тестом; правка сцени інвалідує
залежні спогади.
Залежить: Т1.5, Т2.1, Т0.4, Т0.6.

**Т2.7 · Перший продукт «Допит живого персонажа» — FLC етап 1 (M).**
Перемикач «AI-персонаж» і рівень автономності в профілі героя; режим допиту
(API `…/agent`, `…/snapshot`, `…/interview`): Story Core → Profile Builder →
Jev (сценічний + тактичний рівні) → агент через LLM → відповідь від імені
героя; допит зберігається як дослідницька симуляція, не канон; автор приймає
окремі результати в канон і пам'ять (`canon_proposals`) або відхиляє.
Готово: критерії FLC 2.0 §7 — кожен факт профілю з джерелом або позначкою
гіпотези; немає майбутніх знань і чужих секретів; 10 послідовних відповідей
без втрати контексту й без зміни канону; вибіркове прийняття; інвалідація
пам'яті після зміни сцени; fallback без втрати стану.
Результати, що йдуть у книгу, приходять чистими, а теги — пропозиціями
(П7), які автор приймає з фрагментом або окремо.
Залежить: Т2.5, Т2.6.

**Т2.8 · Перевірка якості персонажа — FLC етап 2 (M).** Контрольний набір
сцен і запитань на тестовій книзі; регресійні перевірки: сталість характеру,
точність пам'яті, ізоляція знань, відсутність спойлерів, різноманітність
реакцій, відповідність стилю автора; порівняння з Jev і без Jev; час і
вартість.
Готово: звіт з числами для кожного виміру; набір запускається однією
командою (`test:living-characters`) і входить у перевірку перед кожною
зміною агентів.
Залежить: Т2.7.

**Т2.9 · CausalityService — причинно-наслідковий контроль (M).** Поверх
`entity_relations` (`caused_by`, `triggers`, `prevents`, `leads_to`,
`consequence`): для рішення чи події — чи мав герой достатні знання для дії,
чи є зрозумілий причинний перехід, чи не суперечить подія затвердженому
канону; допускає випадковість, хибні припущення й навмисну загадку
(попередження, не заборона). Спільний для безперервності (сторінка 8),
залежних сутностей гілки (сторінка 5) і симуляцій.
Готово: на тестовому ланцюжку FLC 2.0 §5 (Анна приховала доказ → Сергій
помічає суперечність → розслідування → нове зіткнення) — перехід без
причини чи дія без знання дають попередження з доказами.
Залежить: Т2.1, Т2.4.

### Етап 3 — Розвиток і виробництво (сторінки 10, 11, 5, 9) + автономні персонажі

**Т3.1 · Сторінка 10 — Спільна робота (L).** Ролі ТЗ-11 на рівні проєкту;
права на глави, сутності, матеріали; коментарі; завдання з виконавцями,
термінами, сповіщеннями; історія й відновлення; для спільних проєктів
сервер — джерело істини, запис лише патчами дозволених розділів (К2-Б);
права на симуляції й AI-персонажів.
Готово: критерій сторінки 10 (негативні тести API і WS).
Залежить: Т0.1, Т0.8.

**Т3.2 · Сторінка 11 — Контекстний переклад (M).** Мови, `translations` на
рівні абзацу, глосарій із поширенням перейменування, профілі й мовлення
героїв у контексті моделі, затвердження й версії, «потребує оновлення»,
експорт; наявний `contentEn` → мова `en`; інваріант збереження тегів (П4).
Готово: критерій сторінки 11.
Теги в перекладі (П5): ключ той самий, значення — за глосарієм
(`[/character:Serhii]` — та сама сутність, що й `[/character:Сергій]`).
Залежить: Т1.5, Т3.1 (роль перекладача).

**Т3.3 · Сторінка 5 — Гілки та ізольовані чернетки (L).** Гілка поверх
основи (основа й канон не змінюються), залежні сутності (CausalityService,
Т2.9), суперечності (Т2.4), кілька гілок, архів, історія, порівняння,
перенесення після підтвердження; **той самий механізм ізольованої чернетки
й покрокового затвердження фрагментів** — для допиту й Magic Scene.
Готово: критерій сторінки 5 (хеші основи до й після однакові).
Залежить: Т2.4, Т2.9.

**Т3.4 · Сторінка 9 — Персональні вправи (M).** Вправи з прив'язкою до
абзацу, сцени, героя; план навчання; короткі й поглиблені; дві редакції
автора поруч; історія й прогрес; наставник із запитаннями; готова правка —
лише за явною дією (К4); відхилення вправи.
Готово: критерій сторінки 9.
Залежить: Т1.5.

**Т3.5 · Два-три автономні персонажі — Magic Scene, FLC етап 3 (L).**
Режисер сцени (порядок ходів, межі, умови завершення, дозволені зміни);
2–3 агенти з окремими контекстами й послідовними ходами (іншим агентам —
лише спостережувані події); API `…/scenes/:sid/simulations`,
`simulations/:id/step|pause|events|draft|approve`; автор сцени (LLM у стилі
автора); перевірки безперервності й причинності чернетки (Т2.4, Т2.9); diff
і затвердження кожного фрагмента (Т3.3); **безперервне життя**: після
затвердження — оновлення канону й пам'яті кожного учасника (Т2.6), і
наступна сцена стартує з нового стану; UI з вкладками «Протокол агентів» і
«Літературний текст».
Готово: критерії ТЗ-H №4, №5, №8; дві сцени поспіль — друга враховує
затверджені спогади першої; набір Т2.8 не погіршився.
Теги в чернетці — пропозиціями (П7).
Залежить: Т2.7, Т2.8, Т3.3, Т2.9.

**Т3.6 · Skills і tools ТЗ-H (M).** `ai/skills/*/SKILL.md` (character-arc,
dialogue-craft, emotion-dynamics, interrogation, mystery-foreshadowing,
author-style, continuity-check); `ai/tools/*` лише серверні, з
`project_id`, `actor_id`, `character_id`, `scene_id`, `simulation_id`;
запис у канон агентам заборонено без автора; жодних файлів, оболонки,
мережі.
Готово: тест — агент не може викликати tool поза своїм героєм і сценою;
`propose-canon-change` створює лише пропозицію.
Залежить: Т2.5. Робиться разом із Т3.5.

### Етап 4 — Секрети, прискорення, приймання

**Т4.1 · Три типи секретів, Secret Vault і Mystery Director — FLC етап 4 (L).**
Типи (FLC 2.0 §4): **особистий секрет** (знає герой, автор за вибором ні),
**хибне переконання** (герой щиро вірить; ніколи не пишеться як
`world_truth`), **світова таємниця** (не знає жоден герой; нею керує окремий
Mystery Director — докази, натяки, умови розкриття, доступ лише до
потрібних таємниць). Vault: шифрування (ключ поза базою, Н4), фіксація до
першого використання, commitment hash, незмінний журнал; межі від автора;
Emergency Reveal (з підтвердженням), Freeze, Regenerate Before First Use,
Archive Simulation; заборона ретроспективної зміни використаного секрету;
правила розкриття через CausalityService (Т2.9); таємниці поза пошуком,
векторами, клієнтськими журналами й телеметрією; **до розкриття — жодного
сліду в рукописі**, після розкриття система пропонує тег `/revelation` у
сцені розкриття (П8); чесне повідомлення про доступ адміністраторів.
Готово: критерії ТЗ-H №6, №7 (тести витоку в усіх каналах; повторна
генерація не змінює зафіксований секрет); тест — хибне переконання не
потрапляє в світові факти; агент бачить лише знання свого героя.
Залежить: Т3.5, Т2.9.

**Т4.2 · Прискорення й масштабування — FLC етап 5 (M).** Інкрементальний
аналіз; кеш знімків за `character_id + book_revision + story_time` і кеші
трьох рівнів Jev; паралельна підготовка профілів, послідовні ходи; потокова
чернетка без обходу перевірки; бюджети токенів; черга задач; метрики
(затримка, токени, вартість, виклики Jev, частка fallback, порушення
безперервності й причинності, відхилені рішення) без приватного вмісту в
журналах.
Готово: повторна сцена на незміненій книзі не перебудовує профілі; метрики
видно в адмінці; набір Т2.8 не погіршився.
Залежить: Т3.5.

**Т4.3 · Приймання всіх ТЗ (M).** Чекліст ТЗ-11 §15, ТЗ-H §15 і FLC 2.0 §7:
адаптивність і теми сторінок, негативні тести безпеки, контрактні тести
адаптерів після оновлення Harness/Jev (ТЗ-H №10), набір якості Т2.8.
Готово: кожен пункт чеклістів має тест або прогін із записом у журналі.
Залежить: усі попередні.

## 6. Послідовність і паралельність

```
Т0.1 Т0.2 Т0.10 Т0.11 ───────────────────────────────────────── (одразу)
Т0.3 → Т0.4 → Т0.5 → Т0.6 → Т1.1 → Т1.2 → Т1.3      (Т0.10 → Т0.6, Т1.1)
        │        Т0.7 ┘        ├──→ Т1.4
        └→ Т0.8, Т0.9          └──→ Т1.5 → Т2.3, Т3.2, Т3.4
Т1.6 прототип (паралельно) ──→ Т2.5 ──┐
Т1.1 → Т2.1 → Т2.2                    ├→ Т2.7 допит → Т2.8 якість ─┐
        ├→ Т2.4 → Т2.9 причинність ─┐ │                           ├→ Т3.5 сцена → Т4.1, Т4.2
        │                            └→ Т3.3 гілки ────────────────┘   ↑ Т3.6
Т1.5 + Т2.1 + Т0.6 → Т2.6 пам'ять ──┘
Т0.1 + Т0.8 → Т3.1 → Т3.2
```

Разом **35 задач**: етап 0 — 11, етап 1 — 6, етап 2 — 9, етап 3 — 6,
етап 4 — 3; з них десять — лише Fusion Living Characters (Т1.6, Т2.5–Т2.9,
Т3.5, Т3.6, Т4.1, Т4.2), решта — ТЗ-11 або спільні.

## 7. Ризики

- **Harness — Developer Preview.** Несумісні зміни; задуманий для агентів з
  доступом до файлів і оболонки. Протидія: адаптер `AgentRuntime`, закріплені
  версії, контрактні тести, власна оркестрація як основна (Н1).
- **Jev не пише текст і має ліміт ~32 тис. токенів.** Знімок героя мусить
  бути компактним і структурованим; текст пишуть LLM Студії.
- **Витік таємниць** — найчутливіше місце ТЗ-H. Ізоляція на сервері, окремі
  контексти агентів, тести витоку в кожному каналі; чесно: адміністратор
  інфраструктури технічно має доступ.
- **Обсяг.** Етап 0 — передумова всього; без нього і сторінки, і агенти
  писалися б двічі.
- **Персонаж «пливе» від сцени до сцени.** Протидія — набір якості Т2.8 як
  ворота перед автономними сценами й після кожної зміни агентів.
- **Пам'ять розходиться з каноном після правок.** Протидія — інвалідація за
  ревізією (Т2.6 через `core_sync`) і заборона бачити незатверджені спогади
  іншого прогону.
- **Зміни тегів ламають розбір чи експорт.** Протидія — лише зворотно сумісні
  розширення (Т0.10), один розбір для всіх споживачів, корпус тегів для
  регресії.
- **ШІ губить теги в правках** (П4 не прийнято). Ризик прийнято власником;
  для перекладу теги обробляються за П5.
- **Підтвердження пропозиції одразу пише в рукопис** (П3). Протидія — запис
  лише в той абзац, лише після кліку автора, через редактор (історія версій і
  «скасувати» працюють як для ручної правки).
- **Витрати.** Бюджети на проєкт і симуляцію (Т0.7), облік у «Тарифах ШІ»,
  Jev дешевий, основна вартість — LLM-репліки й автор сцени.
