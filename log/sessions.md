# Журнал сесій — і єдиний дім записів #114–#118

# Журнал сесій

## Сесія 15.09.2026 #01 — записи #173, #218, #219, #220, #221, #222, #223 і #224

**Тема:** спершу меблеві та деревʼяні вироби на вітрині (план вбудування
сторінки товару, потім — за порядком власника — частина адмінпанелі, частина
вітрини й тестовий прогін публікації на 10 фото власника); пізніше, у тому ж
сеансі, — три правки медіатеки (сортування фото й відео, видалення з
підтвердженням і дві дії на кожному фото), полагодження відмови «модель
перевантажена» в описі персонажа, вибір моделі ядра AI для опису фото просто
у вікні медіатеки, правдива підтримка зображень для ВСІХ рушіїв зі списку ядра
AI (зір як властивість моделі, а не рушія) і, наостанок, передача опису в
конкретну главу книги з підтвердженням, переходом у «Книга та текст» і
прийняттям/відхиленням вставленого.

**Записи:** #173 (план + частини А, Б, Г), #218 (сортування медіатеки),
#219 (видалення з попередженням), #220 (кнопки «Генерувати відео» й
«Описати ШІ» + передача тексту в книгу, інструкцію або курс), #221 (сирий JSON
від Gemini і повтор при перевантаженні), #222 (модель для опису фото),
#223 (усі рушії списку працюють із фото), #224 (вибір глави, підтвердження,
перехід і чернетка з прийняттям).

**Зроблено.**
- **План** занесено в `log.md` (запис #173) і `task.md` — макет, склад робіт,
  розвилки, порядок.
- **Частина А (адмінпанель Студії):** новий редактор картки виробу
  (`FurnitureProductEditor.tsx`) за макетом `fusion_lab_studio` — **нічна тема
  як у макеті + денна, додана самостійно з тим самим змістом**, перемикач;
  модель/валідація (`furnitureProduct.ts`); вбудова в «Управління товарами»
  (кнопка «Додати виріб», перелік чорнеток); серверні маршрути
  (`furnitureProductRoutes.ts`, чорнетки в `meta`) і клієнт моста
  (`publishProductToMarketplace`, `attachProductMediaToMarketplace`).
- **Частина Б (маркетплейс, окремий репозиторій):** колонка `attributes Json?`
  + міграція; приймач `/bridge/products` (+`/media`, GET/DELETE) у NestJS;
  `ProductAttributes` у shared-types; секції товару на сторінці
  `catalog/[slug]` (характеристики, зони, LED, кольори, тони дерева) + ключі
  i18n uk/en.
- **Частина Г:** `test-furniturePublish.mts` читає **всі 10 фото** з
  `Рисунки\Товари\Органайзер`, жене картку через міст із мок-приймачем,
  перевіряє 10/10 завантажень і правильність атрибутів.

**Перевірено.** tsc 0 (Студія й маркетплейс окремо); build OK;
`test:admin-os` 48/48, `test:bridge` 87/87 (+10 нових), `test:furniture-product`
17/17, `test:book-author` 20/20; `live:furniture-editor` 7/7 (Chrome, прод-код);
`test:furniture-publish` 28/28; `journal:check` 173 записи.

**Не зроблено і чому.**
- **Реального прогону публікації немає — «чекає на зовнішнє»**: приймач
  `/bridge/products` не задеплоєний, ключа мосту немає, а Docker Desktop був
  вимкнений, тож локальний Postgres із приймачем не підняти. Частина Г
  перевірена проти **мока контракту**.
- **Міграція `product_attributes` не застосована** на жодній базі (лише
  `prisma generate` її прийняв) — «чекає на деплой».
- **Сторінку товару у вітрині не бачив у браузері** (маркетплейс не
  запускався) — перевірка лише типами.
- **Реальний multipart-розбір фото** (busboy/multer) не перевірено — у моку
  FormData розбиралась напряму.
- **Денну тему** перевірено на факт перемикання, не на вигляд.
- **Не комічено й не запушено** — власник у цьому завданні коміту не просив.
  → **Виправлено того ж тижня:** 16.09.2026 усе закомічено й запушено в обидва
  remote (`da082f6`…`2b956ba`), гілки зійшлися. Пункт лишається тут як
  історія стану на момент запису, а не як борг.
- Повний `npm test` не гнав (давні Windows-провали на `/bin/sh` лишаються).

**Наступній сесії.** Порядок деплою обох репозиторіїв — у `log.md`, розділ
«Що не запушено»: спершу маркетплейс, потім Студія.

### Правки після РЕАЛЬНИХ прогонів (16.09.2026) — та сама сесія, запис #173

Власник задеплоїв маркетплейс і спробував справжню публікацію. Три вади,
яких перевірка не покривала, усі — в моєму коді:

1. **Тизер довший за 300 символів** → приймач відкинув публікацію з HTTP 400
   (`subtitle must be shorter than or equal to 300 characters`), а Студія цієї
   межі не показувала. Тепер межі приймача живуть у `furnitureProduct.ts`, у
   редакторі — лічильник і відмова українською ДО моста. Дорогою закрито
   сусідню ловушку: дробовий `stock` ламав би `@IsInt()`.
2. **Фото не доїхали у вітрину** — `attachProductMediaToMarketplace` було
   написано, але не під'єднано до публікації. Тепер `uploadProductMedia()`
   чистить набір і везе банер + галерею; на боці маркетплейсу галерея
   додається, а не замінює попереднє, і з'явився `DELETE .../media`.
3. **«Немає в наявності» на виробі під замовлення** — порожній залишок ставав
   нулем. Тепер `stock: number | null`, де `null` = «не обліковується».

Перевірено після правок: `tsc` 0 в обох застосунках; `test:furniture-publish`
37/37; `test:furniture-product` 29/29; `test:bridge` 90/90; `test:admin-os`
48/48; `live:furniture-editor` 14/14; `build` OK. Не покрито: реальний
multipart busboy/multer і вигляд сторінки товару з фото в браузері.

### Запис #218 — сортування медіатеки (19.09.2026, та сама сесія)

**Постановка:** «щоб відео в медіатеці показувало окремо та сортувалось від
першої генерації та закінчувалось останньою… додати для відео та фото
декілька методів сортування».

**Зроблено.** Новий модуль `src/utils/mediaSort.ts` — пʼять способів
(від першої генерації — типовий, від останньої, за форматом, за назвою, за
розміром); у галереї — стан `sort`, селект у тулбарі, плашка з датою появи
на мініатюрі; ключі i18n uk/en; четвертий крок туру; `test-mediaSort.mts`
(27 перевірок, у ланцюгу `npm test`) і `live-mediaSort.mts` — живий прогін
Chrome на прод-збірці. Дата для карток із книги дістається за URL із
серверної медіатеки, файли без дати йдуть у кінець в обох напрямках.

**Не зроблено і чому (важливіше за «зроблено»).**
- **Окремих блоків «фото» / «відео» в режимі «Всі медіа» немає** —
  «вирішено не робити»: вкладка «Відео» вже відділяє кліпи, а змішаний
  порядок за датою показує хронологію краще. Якщо власник мав на увазі два
  блоки — це переробка, а не недогляд, і про неї треба сказати прямо.
- **Спосіб сортування не запамʼятовується** між сеансами — «вирішено не
  робити»: просили методи, не збереження вибору.
- **Вікна вставки зображень у текст не чіпав** — там лишається
  `compareByImageFormat`; узгодження порядку між галереєю й вставкою тепер
  розійшлося (галерея типово хронологічна) — свідома межа, про неї знати.
- **Повний `npm test` не гнав** — «чекає на зовнішнє» від самої природи
  набору на Windows: частина тестів іде через `/bin/sh` і падає давно й не
  через цю роботу.
- **Коміт і пуш — на прохання власника, окремим кроком** (не «не зроблено»):
  спершу `tsc` / `build` / обидва набори тестів / живий прогін, потім коміт коду
  й тестів, потім документації, і аж тоді обидва remote.

### Запис #219 — видалення з попередженням (19.09.2026, та сама сесія)

**Постановка:** «Додай можливість видалення відео та фото з медіатеки за
значком видалення. Поставь попередження, про неможливість відновлення
видаленого».

**Зроблено.** Кошик (доданий іншою сесією того ж дня, запис #217) більше не
видаляє файл відразу — він відкриває діалог із попередженням; кошик додано й
у лайтбокс; кошик на картці перестав ховатися за ховер, бо саме через це
видалення було невидимим. Два різні тексти попередження — для файлу сховища
(«байти стерто назавжди; місце в тарифі при видаленні не звільняється») і
для посилання з книги («з книги прибрано назавжди; копія в сховищі, якщо є,
лишиться») — бо наслідки справді різні. Виправлено й неправдивий коментар із
#217 про «звільнення лічильника сховища».

**Не зроблено і чому (важливіше за «зроблено»).**
- **Кошика-архіву немає й не планувався** — «вирішено не робити»: власник
  просив протилежне (попередити, що відновлення неможливе). Мʼяке видалення —
  окрема велика задача зі своїм сховищем і термінами зберігання.
- **Ліміт тарифу при видаленні не звільняється** — не недогляд, а свідома
  політика `mediaStorage.ts`; змінено лише формулювання в коді й у діалозі.
- **Підтвердження в решті екранів** (ілюстрації, персонажі, картки товарів)
  немає — «вирішено не робити» в межах цього завдання; там досі видаляється
  одним кліком, і це варто привести до того ж вигляду окремо.
- **Повний `npm test` не гнав** — «чекає на зовнішнє» від природи набору на
  Windows: частина тестів іде через `/bin/sh` і падає давно й не через цю роботу.
- **Коміт і пуш — на прохання власника, окремим кроком** (не «не зроблено»):
  спершу `tsc` / `build` / живий прогін, потім коміт коду й тестів, потім
  документації, і аж тоді обидва remote.

### Запис #220 — дві дії на фото: кадр для відео й «Описати ШІ» (19.09.2026, та сама сесія)

**Постановка:** дві кнопки на КОЖНОМУ фото в медіатеці — «Генерувати відео»
(узяти фото стартовим кадром) і «Описати ШІ» (ШІ описує персонажа, якого
справді бачить на зображенні, на основі даних ядра письменника) + вікно з
текстом і дві кнопки передачі: у книгу, інструкцію або курс — самим текстом або
разом із фото.

**Зроблено.** Обидві кнопки в рядку «ЕКСПОРТ:» на картках фото (на відео їх
немає); панель генерації приймає фото стартовим кадром і перемикається в режим
«відео»; новий вид роботи `kind: 'character'` у наявному механізмі «текст за
зображенням» + власний промпт (заборона вигадувати, абзац «чого не видно»);
маршрут `POST /api/ai/describe-character-from-image`; «ядро письменника» (назва,
жанр, аудиторія, синопсис, персонажі) їде разом із запитом; вікно опису з
редагованим текстом; передача в книгу (новий розділ, за потреби з `<img>`), у
«Базу знань» чернетки інструкції та в новий курс; 66 юніт-перевірок і живий
прогін на 22 перевірки.

**Не зроблено і чому (важливіше за «зроблено»).**
- **Якості відповіді ШІ не перевірено** — «чекає на зовнішнє»: живого виклику зі
  справжнім ключем немає (і не мало бути — це гроші). Перевірено, що промпт несе
  всі правила й дані, і що без ключа автор бачить причину, а не порожнечу.
- **Промпт не редагується в «Ядрі AI»** — «вирішено не робити» в межах задачі:
  реєстрація нового модуля тягне QuickAiModal, словники й тести реєстру.
- **Передача в інструкцію працює лише за наявної чернетки** — «чекає на
  зовнішнє» від архітектури: конструктор інструкцій живе в `localStorage`, без
  серверного сховища. Без чернетки вікно не закривається й каже причину.
- **Фото в книзі — тегом `<img>`, без `book.illustrations`** — «вирішено не
  робити»: співвідношення сторін невідоме, вигадувати його гірше, ніж не мати.
- **Курс створюється новий**, а не дописується в наявний — «вирішено не
  робити»: дописування в чужий модуль без вибору курсу ризикує структурою.
- **Повний `npm test` не гнав** — давні Windows-провали на `/bin/sh`.

### Запис #221 — сирий JSON від Gemini і повтор при перевантаженні (21.09.2026, та сама сесія)

**Постановка:** власник приніс із «Студії» те, що побачив очима автора: у вікні
«Описати ШІ» замість пояснення стояв сирий JSON від Google —
`{"error":{"code":503,...,"status":"UNAVAILABLE"}}`.

**Зроблено.** Новий вид відмови `busy` в `server/textFromImage.ts` і перевірка
перевантаження ПЕРШОЮ в `classifyGenericError()` (`503`, `unavailable`,
`high demand`, `overloaded`, `temporarily`, `deadline exceeded`); обидва рушії в
`generateTextFromImage()` ідуть через `withBusyRetry()` — три запити загалом із
паузами 700 і 1800 мс; `humanizeEngineMessage()` прибирає з вікна будь-який
JSON-подібний рядок; `busyMessage()` радить перемкнути рушій; усі три маршрути
читання тексту із зображення віддають `busy` як **503** (раніше — `500`);
кнопка у вікні опису після відмови підписана «Спробувати ще раз». У
`test-describeCharacter.mts` — 24 нові перевірки (90 загалом), серед них
справжній шлях `generateTextFromImage()` із підставним рушієм, який двічі кидає
той самий 503; у живому прогоні — 2 нові (24 загалом): кнопка справді каже
«Спробувати ще раз» і сирого JSON у вікні немає.

**Не зроблено і чому (важливіше за «зроблено»).**
- **Справжнього 503 від Google у тестах немає** — «чекає на зовнішнє»: ключа в
  тестах немає, а штучно перевантажити Google неможливо. Форма відмови взята зі
  справжнього вікна власника й відтворена в тестах дослівно; перевірено логіку,
  а не те, що Google повторить саме цей текст.
- **`quota` (429) і далі остаточна відмова** — «вирішено не робити»: ліміт не
  минає за секунду, повтор його лише погіршує. Повторюється лише `busy`.
- **Решта ШІ-модулів не зачеплена** — «вирішено не робити»: у тексту книги й
  ілюстрацій свої рушії та своя обробка помилок; ця робота — про читання
  зображення, і розширювати її без потреби означало б ризикувати робочим кодом.
- **Повний `npm test` не гнав** — давні Windows-провали на `/bin/sh`.

### Запис #222 — модель ядра AI для опису фото в медіатеці (21.09.2026, та сама сесія)

**Постановка:** «Необхідно щоб аналіз фото також проводив один з підключених
моделей… додати в медіотеку випадаючий список про вже вибрану модель та
запропонувати за бажанням змінити модель для опису… (перевір чи може відео
завантажити)».

**Зроблено.** «Описати ШІ» більше не має власного світу з двох рушіїв:
маршрут `/api/ai/describe-character-from-image` з `modelId` у тілі йде через
ядро (`generateAiText`), тож працюють усі vision-моделі (Gemini, GPT,
Claude), власний ключ автора, повтор при тимчасовій відмові й спільне
логування витрат; модель без зору відхиляється з поясненням (400),
невлаштований провайдер — 503. У вікні опису замість кнопок «Gemini/GPT»
випадаючий список із `/api/chat/models`, типово — `book.preferredAiModelId`
(та сама модель, що в «Книга та текст»), моделі без ключа позначені й
недоступні. Старий контракт (лише `engine`) збережено. 10 нових живих
перевірок (34 загалом), серед них — тіло запиту з обраною моделлю.

**Перевірено.** `tsc` 0, `build` OK, `live:media-describe` 34/0,
`test:describe-character` 90/0, `test:core-module-models`, `test:media-sort`.

**Не зроблено і чому (важливіше за «зроблено»).**
- **Завантажити відео з диска НЕ можна** — «вирішено не робити» в межах цієї
  задачі: пікер приймає лише зображення, `decodeImagePayload` пропускає лише
  зображення (з прямою приміткою, чому), а `video/mp4` у таблиці типів існує
  для ЗГЕНЕРОВАНИХ відео Leonardo.Ai. Маршрут приймає base64 у JSON, і межа
  тіла 25 МБ робить завантаження відео майже безглуздим — це окрема задача з
  окремими рішеннями, а не дрібниця.
- **Друге вікно «AI текст за фото» (вкладка «Ілюстрації»)** і далі пропонує
  лише Gemini/GPT — «вирішено не робити» в межах задачі: у нього власний
  маршрут і інша робота (художній текст сцени, а не опис персонажа).
- **Якості опису від нових моделей не перевірено** — «чекає на зовнішнє»:
  живий прогін безкоштовний (усі три ключі порожні), тому перевірено
  проводку та чесні відмови, а не те, як Claude чи Gemini опишуть фото.
- **Список vision-рушіїв продубльовано на клієнті** (`VISION_ENGINES_FRONT`)
  — давня межа проєкту: спільного імпорту між сервером і клієнтом для цієї
  сталої немає, і `EditorView` уже робить так само.
- **Повний `npm test` не гнав** — давні Windows-провали на `/bin/sh`.

### Запис #223 — усі рушії зі списку ядра AI працюють із фото (21.09.2026, та сама сесія)

**Постановка:** «Пропиши апі всіх рушіїв зі списку для правдивої роботи» — з
прямим посиланням на мій же звіт за #222, де було написано, що DeepSeek, Llama
й Mistral у списку опису фото відсутні, бо вони текстові.

**Зроблено.** Зір став властивістю КОНКРЕТНОЇ моделі: `vision: boolean` у
реєстрі `CHAT_MODELS` (17 рядків), `modelSupportsVision()`, переклад усіх
рушійних воріт на модельні в чотирьох місцях (чат із вкладеннями, опис
персонажа за фото, «текст книги за фото», тест модуля ядра), нова перевірка на
адмінській привʼязці «модуль → модель», тарифи по моделях, клієнтські списки
фільтруються прапорцем із сервера. Мертвий id `deepseek-chat` перекладено на
`deepseek-flash` (і в сесіях, і перед самим запитом до провайдера). Додано
`deepseek-flash` (Vision ✓), `deepseek-v4-pro` (✗), Llama 4 Scout у Groq (✓),
Mistral Large (✓).

**Перевірено.** `tsc` 0, `build` OK; `test:chat-providers` 49/0 (було 13),
`test:chat` 152/0 (було 147), `test:api-keys` 57/0, `test:describe-character`
90/0, `test:core-module-models` 45/0, `live:media-describe` 39/0 (було 34).

**Не зроблено і чому (важливіше за «зроблено»).**
- **Живого виклику DeepSeek, Groq і Mistral немає — «чекає на зовнішнє»:**
  ключів цих трьох провайдерів у розробці немає, а прогін іде з порожніми
  ключами, щоб не коштувати. Перевірено форму запиту й чесні відмови, а не те,
  що провайдер справді приймає `image_url`.
- **Ідентифікатор Llama 4 Scout у Groq не підтверджений живим запитом** —
  «чекає на зовнішнє»: консоль Groq не читається зовні (`ERR_BLOCKED_BY_CSP`).
  Це найризикованіше місце правки, воно підписане прямо в коді; якщо імʼя інше,
  Groq відповість 404 і правити треба один рядок.
- **Тариф Groq для Scout лишається `null`** — «чекає на зовнішнє»: підставити
  ціну OpenRouter замість Groq означало б показати число, за яким власника не
  виставлять. Ціну вписати після перевірки на `groq.com/pricing`.
- **Старий маршрут `/api/ai/generate-book-text-from-image`** (лише
  `engine: gemini|gpt`) лишився двoрушійним — «вирішено не робити»: це шлях для
  клієнтів старої редакції, і зачіпати його без причини означало б ризикувати
  робочим кодом.
- **Вікно «AI текст за фото» (вкладка «Ілюстрації»)** і далі пропонує лише
  Gemini/GPT — спадок #222, «не почато», окрема задача.
- **Повний `npm test` не гнав** — давні Windows-провали на `/bin/sh`.

**Окремо — урок про запис у журнал.** Запис #223 з першої спроби дописували
через консоль (`Add-Content` із here-string), і PowerShell зʼїв частину
кириличних великих літер («Мій» → «ій», «Перевірка» → «еревірка»). Запис
відкотили з резервної копії й вписали файловим редактором. Довгі кириличні
тексти через консоль не писати.

### Запис #224 — опис із медіатеки: глава, підтвердження, перехід і чернетка (21.09.2026, та сама сесія)

**Постановка:** «натиснув кнопку "передати текст в книгу", але він не показав
куди передав і чи передав взагалі, повідомлення не зʼявилось… вибір глави…
після підтвердження глави передати, та відкрити в кінці главу в розділі "Книга
та текст" зі вставленим текстом… прийняти або відхилити вставлений текст —
виділяється окремим кольором. Після прийняття — текст стає стандартним чорним
по білому».

**Зроблено.** Три вади, а не одна: (1) главу вибирав код (завжди остання);
(2) у книгу писався HTML `<p>…</p>` — редактор показував теги літерально;
(3) не було ні переходу до місця вставки, ні прийняття/відхилення. Тепер:
вибір глави у вікні опису, крок підтвердження з назвою глави, вставка в КІНЕЦЬ
останньої секції вибраної глави блоком `[AI-DRAFT]` (тобто бурштинова рамка з
кнопками ✓/⊘ — »прийняти» тепер видиме, а не лише в контекстному меню),
фото — записом книги (`book.illustrations`) із маркером `[IMG: id]` замість
`<img>`, і перехід у «Книга та текст» на цю главу з підсвічуванням вставленого.

**Перевірено.** `tsc` 0, `build` OK; `test:describe-character` 115/0 (було 90),
`live:media-describe` 57/0 (було 39), `test:manuscript-doc` 50/0,
`test:book-text` 26/0, `test:chat` 152/0, `test:media-sort` 27/0,
`test:core-module-models` 45/0.

**Не зроблено і чому (важливіше за «зроблено»).**
- **Співвідношення сторін фото в книзі — типове 16:9** — «не почато»: справжнє
  для файлу з медіатеки тут невідоме (немає розмірів), і так само робить шлях
  завантаження файлу. Щоб зробити точно, треба віддавати розміри з медіатеки.
- **Главу «Опис персонажів» для книги без глав** перевірено лише юнітом —
  «не почато»: у живому прогоні книга вже має глави.
- **`findBlockRangeWithText` без юніт-тесту** — «вирішено не робити»: функція
  працює з `PMNode`, а в Node-тестах немає DOM (jsdom у проєкті немає). Покрита
  живим прогоном.
- **Крок підтвердження лише для книги** — «вирішено не робити» для інструкції
  та курсу: там місце одне й фіксоване.
- **Давній Windows-провал `test:media-library` (58/1)** — «не почато»:
  перевірка «файл усередині media/» порівнює шлях із `${DIR}/media/`, а Windows
  дає зворотні слеші. До #224 не стосується (перевірено `git diff --stat`).
- **Повний `npm test` не гнав** — давні Windows-провали на `/bin/sh`.

**Урок сесії (дорогий).** Дві вади знайшов ЛИШЕ живий прогін, обидві — у наявному
коді, не в новому: прапорець підсвічування знімався до того, як підсвічування
застосовано (через що воно не працювало взагалі), і арифметичне відображення
зміщень у реальному розділі книги повертало `null` (756 позицій документа проти
775 у рядку-маркерах). Обидва випадки — «усе виглядає правильно, поки не
запустиш у справжньому браузері».

## Сесія 13.09.2026 #12 — записи #169, #170, #171 і #172

**Тема:** «PDF на вітрину опублікувався без картинок» — знайти причину,
порівнявши два файли, і полагодити так, щоб картинки з рукопису друкувались на
своєму місці; далі — перевірити живим прогоном другий рушій PDF (Chromium),
перш ніж деплоїти.

**Записи:** #169 (картинки з рукопису друкуються на своєму місці), #170
(живий прогін рушія PDF «Chromium» локально, перед деплоєм), #171 (тулбар
«Книга та текст» — лише іконки з підказками; карта адмінпанелі на неоновому
макеті; новий розділ «Модерація») і #172 (вузол «Управління товарами»,
повернення до мапінгу в кожному розділі, справжнє авторство книги,
однорядкова шапка застосунку).

**Зроблено.** Причина видна просто у файлі, який власник завантажив: у
надрукованому PDF замість малюнка стояв сам маркер `[IMG: docx-img-N "…"]` —
на 39 сторінках із 373, — а всі 42 ілюстрації лежали купою на сторінках 6–41.
Розбір маркерів жив лише на клієнті, серверні рушії вважали його текстом, а
коментар у коді стверджував, що місця вставки в книзі немає. Тепер одне місце
правди — `src/utils/imageMarkers.ts`; власна верстка Nova малює картинку на
місці маркера, Chromium і pandoc отримують `![підпис](…)` там само, а
ілюстрації без маркера йдуть сумісним шляхом (кінець глави). Дорогою знайдено
ще дві причини, чому з 42 картинок не їхали дві: pdf-lib читає підпис формату
через `new DataView(imageData.buffer)` **без** `byteOffset`, а
`Buffer.from(base64)` для картинок до 32 КБ віддає вікно в спільному пулі
(50 провалів із 50 на буфері зі зсувом, 0 — на копії з нульовим зсувом);
і гонка в майстрі .docx брала індекс до `push` у `.then()`. Лікують
`tightImageBytes()`, `imageMimeFromBytes()` і `createDocxImageSlots()`.
Живий прогін на справжньому рукописі (489 стор., 42 картинки) — **26/26**;
до правок він падав із «SOI not found in JPEG» у двох прогонах із пʼяти.

**Підтвердження власника (14.09.2026).** «Все працює, молодець!» —
перепублікована книга на вітрині має картинки на своїх місцях. Перевірено
**лише рушій Nova**; Chromium і pandoc — наступним завданням.

**Тієї ж сесії, після підтвердження (#170).** Власник: «Chromium — давай
локально, а потім деплой.» Сесію при цьому перервало перезавантаження
компʼютера («Сталось перезавантаження компа. Продовжуй завдання.»), тому
номер сесії лишається той самий — він ставиться за часом ПОЧАТКУ.

Щоб прогнати другий рушій, довелось полагодити дві речі **в самому скрипті**,
а не в рушії: помилку часу життя (TDZ), лишену перерваною сесією (`bookLike`
брав змінні, оголошені нижче у файлі), і падіння обходу сторінок після
успішної верстки — `PDFDict.lookup()` у pdf-lib **кидає** виняток, якщо
`/XObject` немає, а в браузерній верстці сторінки без обʼєктів бувають
(додано перевірку `has()`). У Nova ця вада спостерігача не могла проявитись.

Результат на справжньому рукописі: **уся книга — 470 стор., 37.9 МБ, верстка
21.5 с, 26/26 перевірок**; жодної втраченої картинки, 42 сторінки з картинками
проти 42 ілюстрацій, розкид від с. 2 до с. 469 із 470, жодного надрукованого
маркера, останній (`docx-img-41`, «Слушайте музыку») — на с. 469 із 470.
Для порівняння: власна верстка Nova дає 489 сторінок на тому ж рукописі — це
різні моделі розриву сторінок, а не втрата вмісту. Отже з трьох рушіїв
перевірені живим прогоном **два (Nova і Chromium)**; лишився pandoc.

**Далі тією ж сесією (#171) — два завдання власника.** Спершу борг: він
справедливо спитав «Чому не виконано?» про оптимізацію тулбара «Книга та
текст». Відповідь була проста: того разу він просив **план**, і план було
видано — реалізації ніхто не замовляв. Тепер реалізовано: у блоці швидкої
типографіки видимі підписи знято з **десяти** кнопок, лишились іконки 18px
(акцентна — 20px), а текст переїхав у `title` (підказка при наведенні) і
`aria-label` (щоб кнопки не стали беззмістовними для скрін-рідера й щоб ключі
словника не стали мертвими). Верхній рядок («Зміст», «Пошук», «Читач»,
«Показники», «Перекласти», «Зберегти») не чіпано — пряме рішення власника.

Друге: стартовий екран адмінпанелі переведено на наданий неоновий макет —
електричні пігулки з конічним градієнтом у рамці, SVG-провідники від ядра,
ядро з обертовими дугами й лого, смуга «Памʼять і сутності». Розкладка карти
3/4/4 переїхала в реєстр (`slot`), тож доданий вузол більше не зсуває сусідів у
чужу колонку. Заглушок більше немає: **модальні вузли стали сторінками** —
«Провайдери ШІ» показує `ApiKeysView`, «Промти ядра» — справжній конструктор
(`CoreAiPanel`; доти вузол відсилав текстом «відкривається кнопкою "Швидкий AI"»),
а **новий розділ «Модерація»** винесено окремо з-під «Моста до вітрини». Дорогою
прибрано два вигадані числа інспектора («9 модулів ядра» → реальні 19, «7 ролей і
6 на роль» → з коду прав) і смуга сутностей тепер бере всі вісім чисел із живих
маршрутів. «Сонечко» не чіпав зовсім: воно глобальне й на цій сторінці працює як
усюди; неон замінює лише акцент самого інтерфейсу адмінки.

**Перевірка #171.** `tsc --noEmit` 0, `npm run build` OK. Нові набори
`test:admin-os` **43/43** і `test:editor-toolbar` **59/59** (обидва в `npm test`).
Найцінніше — **новий живий прогін `npm run live:admin-map` 32/32**: піднімає
справжній `dist/server.mjs` на тимчасовому сховищі, створює сесію адміністратора
тим самим сховищем, що й сервер, відкриває headless Chrome і тисне **кожну з 11
пігулок**, перевіряючи саме той екран, а не «на сторінці щось є»; той самий
прогін заходить і в редактор, щоб побачити тулбар очима — усі вісім кнопок
блоку без видимого підпису, з `title` та іконкою. Суміжні набори (7 штук) —
0 провалів. Запушено в обидва remote (`3447065`, `f188bfb`, `3edd56d`).

**Далі тією ж сесією (#172) — чотири задачі власника.** Він попросив: вузол
«Управління товарами», кнопку «Повернутися до мапінгу адмін панелі» в кожному
розділі, справжнє авторство книги замість «Невідомий автор» і підняти чотири
надписи в «Книзі та текст», щоб канва стала максимально високою.

Зроблено: дванадцятий вузол «Управління товарами» з власною сторінкою (перелік
того, що стоїть у вітрині, і зняття лістинга; кнопки «додати» немає свідомо —
товар додається публікацією зі Студії, і це прямо написано на екрані);
кнопка повернення до мапінгу над вмістом кожного розділу; нова чиста функція
`resolveBookAuthor()` з ефектом у `App.tsx` — службове «Невідомий автор»
замінюється іменем з обкладинки книги, а якщо й там порожньо — імʼям
залогіненого користувача, і одразу зберігається (інакше публікація у вітрину
пішла б зі службовим рядком); самі майстри більше не створюють цього рядка.

А в четвертій задачі виявилась справжня причина, чому канва не росла: висота
поверхні редактора була магічним `calc(100vh - 105px)` і від шапки не залежала.
Поки шапка була 86px — збігалося; коли вона розрослася до 128px, поверхня
перестала вміщатися в екран, і 23px канви опинялися за його межею. Шапку
згорнуто в один рядок (`display: contents`), заміряно 128px → 86px, а висота
поверхні тепер виводиться з `--app-header-h`. Чесно: сама канва (605px) не
збільшилась — її висоту визначають смуги ВСЕРЕДИНІ редактора, і це вже окреме
завдання, яке без прямого прохання я не брав.

**Не зроблено і чому.**
- **Нічого з #172 не закомічено й не запушено** — чекає на пряме прохання
  власника.
- **Канва не стала вищою в пікселях** — вирішено не робити: її висоту визначають
  смуги ВСЕРЕДИНІ редактора (тулбар у два рядки, вкладки, формат), а не шапка.
  Шапку згорнуто (128px → 86px) і прибрано магічне `100vh - 105px`, через яке
  23px канви опинялися за межею екрана; далі треба чіпати смуги, і це окреме
  завдання, якого ніхто не замовляв.
- **Емблему не зменшував** — вирішено не робити: власник раніше прямо просив
  тримати її на 80% висоти шапки, і саме вона зупинила шапку на 86px.
- **Зняття товару з вітрини** живим кліком не перевірено — чекає на справжню
  вітрину: у тимчасовому сховищі міст не налаштований, перелік порожній.
- **Заміну авторства** живим прогоном не перевірено — чекає на підтвердження
  власника: у headless-профілі книга демонстраційна, і в неї автор справжній,
  тож ефект у неї не втручається. Самі правила покриті `test:book-author`.
- **Вузькі екрани** після згортання шапки не перевіряв — вирішено не робити:
  чіпи ховаються до `2xl`, кнопки переносяться, і це видно з коду.
- **Повний `npm test` не гнав** — прогнано цільовий підбір навколо змінених
  модулів.
- **Нічого з #171 не закомічено й не запушено** — чекає на пряме прохання
  власника (AGENTS.md: комітити лише коли попросили, пушити — лише на прохання).
  Отже в проді цього ще немає.
- **Натискання «Погодити»/«Відхилити» в «Модерації»** живим прогоном не
  перевірено — чекає на дані: у тимчасовому сховищі черга порожня, тож видно
  лише порожній стан. Код перенесено без змін.
- **`prefers-reduced-motion` і вузькі екрани** не перевірені в браузері —
  вирішено не робити: це статичний CSS (логіка видима з коду), а скріншот
  вузького екрана нічого б не додав до перевірки вмісту.
- **Повний `npm test` не гнав** — вирішено не робити цього разу: прогнано
  цільовий підбір навколо змінених модулів (7 наборів, 0 провалів), а давні
  Windows-провали `test:media-library` і `test:pdf-engines` на `/bin/sh` від
  цієї роботи не залежать.
- **Розділ «Управління товарами» з макета не створював** — рішення власника:
  новим розділом обрано «Модерацію».
- **Chromium живим прогоном не перевірений — а pandoc не перевірений і досі**
  (#170). Chromium **закрито тією ж сесією пізніше**: власник попросив
  («Chromium — давай локально, а потім деплой»), і прогін на справжньому
  рукописі дав 26/26 на всій книзі. **pandoc локально перевірити неможливо** —
  чекає на щось зовнішнє: у PATH немає ні `pandoc`, ні `xelatex` (перевірено),
  ланцюг XeLaTeX + Eisvogel живе лише в прод-образі. І це все одно рішення
  власника — «далі подивимося інші рушії пізніше».
- **Chromium у вітрину не публікувався** — чекає на деплой: публікація не
  рушієм за замовчуванням створює ОКРЕМЕ тестове видання з позначкою рушія в
  назві.
- **Цифри прогону — з машини власника, не з контейнера**: у прод-образі
  власний Chromium і інша памʼять, тож час і межі можуть відрізнятися.
- **У вітрину не публікував** — не моя зона: адреса й ключ мосту живуть у
  Railway, локально їх немає. Перепублікацію зробив сам власник і підтвердив,
  що картинки на місцях, — але **з рушієм Chromium у вітрину досі нічого не
  публікувалось** (див. вище).
- **Обкладинка, уривок і KDP-шлях не перевірялись живим прогоном** — не почато:
  вони не змінювались у цій роботі, і брати їх у той самий прогін означало б
  роздути його до години.
- **Обтікання `wrap=left/right/contour` у PDF не відтворюється** — вирішено не
  робити: у власній верстці `pdf-lib` не має поняття потоку навколо картинки,
  а це вже окремий проєкт верстки; місце картинки збігається, обтікання — ні.
- **Гонку pdf-lib бачив у Node, не в проді** — чекає на деплой: у браузері
  картинки читає сервер, шлях той самий, але саме вікно пулу я міряв локально.
- **`char-…` і `cover-front` у PDF покриті лише юніт-тестом** — у цьому
  рукописі таких маркерів немає, а синтетичну книгу з портретом героя я не
  збирав.
- **Повний `npm test` на Windows зеленим не буде** — давні `test:media-library`
  (58/59) і `test:pdf-engines` (129/130) падають на перевірці шляху `/bin/sh`.

---
## Сесія 13.09.2026 #11 — запис #168

**Тема:** три задачі власника одним пакетом — колір кнопок від «Сонечка» в
трьох розділах, статус і перебіг публікації у вітрину, і «книга без
фотографій».

**Записи:** #168.

**Зроблено.** Акцент сонця став дійсним для ВСІЄЇ студії: арифметику винесено
в `sunAccentVars.ts`, `SunLightingContext` ставить `--sun-acc*` на корінь
документа, у `index.css` з'явились семантичні класи (`.sun-accent-solid`
тощо), і всі блакитні акценти в «Публікації та експорті», «Форматуванні
файлу», «Аналітиці ринку Etsy» й шапці вікна Etsy замінено на змінні сонця
(позначки походження даних лишено смисловими). Публікація у вітрину тепер має
три кроки зі станами і часом, `aria-live`, прокрутку до блоку стану (без неї
звіт малювався нижче екрана — звідси скарга) і збереження звіту в
`localStorage`. Знайдено причину «без фотографій»: обкладинка книги Архата на
проді Є, а порожні картки — це курс і тестова книга; правило «без обкладинки
не публікуємо» існувало лише на шляху книги з браузера, тепер є спільний
`assertStorefrontCover()` у трьох місцях.

**Не зроблено і чому.**
- **Проведення охоронця обкладинки в трьох маршрутах не покрито тестом** — не
  почато: HTTP-тесту модерації курсу в наборі немає, а писати його з нуля
  означало б новий каркас; юніт-тест функції є (8 перевірок).
- **Статус публікації не перевірено живим натисканням** — чекає на щось
  зовнішнє: локально немає адреси й ключа мосту.
- **Акцент у світлій темі не бачено скріншотом** — вирішено не робити: це
  вимагало б живого прогону студії в браузері з підміненим кольором сонця.
- **Дві картки без обкладинки лишились у каталозі** — вирішено не робити:
  прод-база маркетплейсу не моя зона, курсу треба дати обкладинку й
  перепублікувати.
- **Що саме власник назвав «без фотографій», не підтверджено** — чекає
  уточнення: обидві редакції книги Архата мають і обкладинку в картці, і
  ілюстрації в PDF (4 і 5 зображень на перших 10 сторінках зразків).

---
## Сесія 13.09.2026 #10 — запис #167

**Тема:** живий прогін експорту книги у PDF для вітрини магазину на реальній
книзі (не на фікстурі) — і дві знахідки, які можна було побачити лише так.

**Записи:** #167.

**Зроблено.** Новий `scripts/live-storefrontExport.mts`
(`npm run live:storefront-export`) піднімає СПРАВЖНІЙ Express із
`registerPdfRoutes` + `registerBookRoutes`, підставляє `req.principal`
власника книги й проходить маршрути, які натискає автор: складання файлів
(`/api/books/:id/render`), читання їх назад, обкладинка
(`/api/books/:id/artifact/cover`) і публікація. Обкладинку вперше зібрано
БЕЗ браузера: `pdfjs-dist` + `@napi-rs/canvas` (той самий масштаб, що в
`coverScaleFor`). Прогін на книзі «Тіні Нео-Києва 2084»: електронна редакція
6 сторінок / 3.1 МБ / A5 148×210 мм, друкована 5 сторінок / 6×9 /
корінець 9.6 мм, уривок 5 сторінок, обкладинка 1128×1600 px, у PDF 3417
символів тексту й 1 вбудована ілюстрація. Дві знахідки: (1) передмова книги —
копірайт, присвята, епіграф, зміст — у PDF **не потрапляє**, бо серверна
верстка Nova малює лише титул і в `PdfLayoutSpec` для решти полів немає місця;
(2) `publishStored` кладе в картку **посилання** на обкладинку за публічною
адресою Студії, тож публікація з локальної машини дала б картку з
недосяжним `127.0.0.1` — у скрипті це стереже прапорець
`--allow-local-publish`.

**Не зроблено і чому.**
- **Реального відправлення у вітрину не було** — чекає на щось зовнішнє:
  адреса й ключ мосту живуть у Railway (`BRIDGE_API_KEY`), локально їх немає,
  а локальна публікація до того ж зіпсувала б картку (див. знахідку 2).
  Повний прогін можливий лише з задеплоєної Студії.
- **Книга, яку власник бачить в Opera, не використана** — не почато, бо
  взяти її нізвідки: локальний сервер Студії не запущений, у локальному
  сховищі вона одна (інша), вкладку чужого браузера я не бачу. Назви власник
  не назвав.
- **Прод-інстанція й маркетплейс не перевірені** — немає авторизації.
- **Знахідку про передмову не виправлено** — вирішено не робити в цьому
  завданні: це зміна вигляду всіх книг і нумерації сторінок, потрібне окреме
  рішення власника.
- **Одна книга — один набір чисел**: на довгому рукописі (300 сторінок)
  поведінка не перевірена.

---
## Сесія 13.09.2026 #09 — запис #166

**Тема:** фаза 4 плану лінійки — чесний зум (відсоток, який не бреше) з
горизонтальною прокруткою, режим «вмістити», спільний масштаб лінійки й
колонки; плюс закритий борг із #165 — абзацний відступ книги тепер доходить
до друкованої таблиці стилів.

**Записи:** #166.

**Зроблено.** `usePageScale` більше не стискає аркуш потай
(`resolveSheetScale(mode, zoomFactor, widthPx, availablePx)`: у `zoom` — рівно
заданий відсоток, у `fit` — `min(zoom, available / width)`; у `zoom` навіть
`ResizeObserver` не підключається). `PageColumn` став `overflow-y-auto`, і
горизонтальна лінійка переїхала ВСЕРЕДИНУ нього першим `sticky top-0` рядом,
а смуга вертикальної лінійки — у `sticky left-0`: один контейнер, один
масштаб, одне прокручування. `PageRuler` бере той самий хук і той самий режим,
що й колонка, і міряє текстову зону. `EditorView` тримає `zoomToFit` у
`localStorage` і передає `zoomFactor`/`scaleMode`/`ruler` усім пʼятьом панелям
рендера. У друк пішло `firstLineIndentPt`/`paragraphSpacingPt` із
`PdfLayoutSpec`. Жива перевірка у Chrome (`npm run live:ruler-shot`) дала
**±0.00 px** між зоною тексту на лінійці та колонкою (у #161 це було ±3 px).

**Не зроблено і чому.**
- **Розрив усередині абзацу, вищого за аркуш, усе ще не зроблено** — те саме
  «потрібен віджет у інлайн-позиції + `caretRangeFromPoint`/`posAtDOM`», що й у
  #165: без живого TipTap це неперевірювано, а помилка ламає письмо. Єдиний
  сигнал — бурштинова зона на вертикальній лінійці.
- **Горизонтальну прокрутку чесного зуму не прокручували** — перевірено числа
  `resolveSheetScale`, структуру DOM і збіг меж лінійки з колонкою; саме
  «аркуш ширший за панель, тягнемо смугу» в живому застосунку не відкривалось.
- **Режим «вмістити» в живому редакторі не відкривався** — лише арифметика й
  наявність перемикача в розмітці.
- **Живий TipTap не запускався** — знімок рендерить ту саму композицію
  (`PageColumn` + `PageRuler`) у чистому HTML.
- **Друк не прогнано** — `test:pdf-engines` бачить числа в таблиці стилів, але
  PDF не генерувався; `DejaVu Serif` на цій машині немає.
- **Повний `npm test` на Windows зеленим не буде** — давня `test:media-library`
  (58/59) і, як зʼясувалось цієї сесії, `chromiumAvailableAt('/bin/sh')` у
  `test:pdf-engines` (104/1), яка на Windows не проходить за побудовою.
- **`PdfEditorView` зі своїми `?? 20/20/18/15` і іконковий тулбар «Книга і
  текст»** — свідомо поза межами цієї сесії.

---

## Сесія 13.09.2026 #08 — запис #165

**Тема:** фаза 3 плану лінійки — канва й «Розворот книги» міряють ту саму
сторінку; вперше за весь ланцюг фаз зроблено ЖИВИЙ замір розкладки в
справжньому Chrome.

**Записи:** #165.

**Зроблено.** Знайдено справжній Chrome на цій машині + `puppeteer-core` у
залежностях → `scripts/live-layoutProbe.mts` (`npm run live:layout-probe`)
міряє той самий текст у трьох контекстах і падає з кодом 1, якщо канва й вимір
розійшлись. Замір одразу показав: канва давала 13 сторінок проти 11 у вимірі,
бо кегль рахувався як `fontSizePt * 1.3` px (а не × 96/72), абзацний відступ
був жорстко `0.9em` (а `firstLineIndentMm`/`paragraphSpacingMm`/`textAlign` із
«Верстка & Поля» редактор не читав узагалі), а обгортка тексту мала `p-4` —
16 px з кожного боку, тобто рядки переносились на 32 px вужче за аркуш.
Новий `utils/typography.ts` дає одну геометрію абзацу на всі місця, спільний
клас `.nova-manuscript-blocks` (редактор + вимір + превʼю) тримає їх разом,
`useRealBookPages` перейшов на той самий алгоритм (межі блоків +
`keepWithNext`), а переповнена сторінка тепер видима на вертикальній лінійці
бурштиновим. Після правок A = B: однакові межі, однакові розриви, 11 сторінок.

**Не зроблено і чому.**
- **Розрив усередині абзацу не зроблено** — технічно це віджет у інлайн-позиції
  всередині абзацу + `caretRangeFromPoint`/`posAtDOM`. Перевірити це без
  живого TipTap неможливо, а помилка ламає саме письмо, тому замість
  неперевіреного коду зроблено видимий прапорець переповнення.
- **Живий редактор (TipTap) у браузері не запускався** — замір відтворює його
  CSS-контекст у чистому HTML, а не сам ProseMirror.
- **Друк у продакшні не зміряний** — там інша гарнітура (DejaVu Serif) і
  переноси; локально шрифта немає, тож числа будуть інші.
- **`bookHtml.ts` досі тримає жорстке `text-indent: 1.2em`** замість
  `firstLineIndentMm` — це вже продакшн-конвеєр PDF, правка навпомацки.
- **Повний `npm test` не прогнано** — ланцюжок упирається в давню помилку
  `test:media-library` на Windows (58/59).

---

## Сесія 13.09.2026 #07 — запис #164

**Тема:** термінове виправлення регресії з #163 — `computeBreaks` перестав
накопичувати висоту й не давав жодного розриву сторінок.

**Записи:** #164.

**Зроблено.** Знайдено під час підготовки фази 3, на першому живому замірі
розкладки у справжньому Chrome: «Розворот книги» показував по одній сторінці на
розділ, бо в `computeBreaks` не було гілки `else { currentHeight += h; }` — її
загубив мій же запис #163 під час заміни цілої функції. Гілку повернуто; додано
8 перевірок на цю функцію в `test:image-wrap` (вона жила без жодного тесту —
саме тому й не помітили ні типи, ні збірка).

**Не зроблено і чому.**
- **У браузері «Розворот книги» не перевірено** — перевірено, що функція знову
  дає розриви, а не як сторінки виглядають на екрані.
- **Замір розкладки не закомічено** — він ще чорновий, зʼявиться в записі #165.

---

## Сесія 13.09.2026 #06 — запис #163

**Тема:** фаза 2 плану лінійки — вертикальна лінійка міряє сторінки, а не всю
висоту рукопису.

**Записи:** #163.

**Зроблено.** `PaginationPlugin` віддає назовні відрендерні межі сторінок
(`onMeasured`, вимір ПІСЛЯ `dispatch` — інакше координати не враховують смуги
розривів). Чиста арифметика знімка — `buildPaginationSnapshot`,
`paginationSnapshotsEqual` у `pageBreaker.ts`. `PageColumn` малює вертикальну
лінійку як світлі зони тексту зі своєю шкалою `0…contentHeightMm` на кожній
сторінці; бюджет узято зі знімка, а не з геометрії. `EditorView` тримає два
знімки (UA/EN) з порівнянням перед `setState`. Супутньо виправлено давню
проблему: пагінація перераховувалась лише на правку тексту, тож після зміни
формату розриви (і лінійка) лишались старими — тепер є явний сигнал
`paginationRescanKey`. `formatMm` переїхав у `mmUnits.ts`. Додано 15 перевірок
вертикальної лінійки на рендері реального `PageColumn` і 18 перевірок знімка.

**Не зроблено і чому.**
- **Браузерного прогону не було** — і саме тут найтонше місце: перевірено, що
  лінійка правильно рахує те, що їй дають, але НЕ перевірено, що `nodeDOM`
  після `dispatch` дає правильні числа на живому DOM.
- **Верхнє й нижнє поле на лінійці не показані** — свідомо: канва не малює
  ані вертикальних полів, ані справжніх аркушів (розрив — 15 px, а не 40 мм).
  Малювати 297 мм із вписаними навскоси полями було б брехнею в міліметрах.
- **Недозаповнена остання сторінка обрізається** смугою (висота смуги = висота
  вмісту) — видно з підказки, але не зі шкали.
- **Фази 3–4 плану не почато** — свідомо поетапно: фаза 3 (розриви всередині
  абзацу + узгодження живої пагінації з експортною) найризикованіша.
- **`getVerticalMarginsMm` у плагіні досі не читається** — мертвий код, не чіпав.
- **Повний `npm test` не прогнано** — ланцюжок упирається в давню помилку
  `test:media-library` на Windows (58/59), не повʼязану з цією роботою.

---

## Сесія 13.09.2026 #05 — запис #162

**Тема:** фаза 1 плану лінійки — горизонтальна лінійка міряє аркуш, а не
текстову колонку.

**Записи:** #162.

**Зроблено.** `PageRuler.tsx` переписано: сіра смуга полів від краю до краю,
світла зона тексту, шкала `0…210` для A4 (43 поділки, 22 цифри), ручки — на
межах текстової зони, підпис ширини тексту в мм. Масштаб узято від текстової
колонки (той самий `usePageScale`), тому зона тексту на лінійці стоїть над
текстом; уся арифметика винесена в `buildRulerSheetLayout` (`mmUnits.ts`) і
перевіряється числом. Поля тепер мають межі руху — `clampMarginMm` у
`pageGeometry.ts` (мінімум 5 мм, текстовій зоні ≥ 20 мм). У `EditorView.tsx`
пʼять місць виклику перейшли з інлайнових полів на одну `resolvePageGeometry`
на рендер. Два нові набори тестів у ланцюжку: `test:ruler-layout` (16) і
`test:page-ruler` (27, рендер реального компонента без браузера).

**Не зроблено і чому.**
- **Браузерного прогону не було** — головна прогалина: фаза 1 змінює вигляд,
  а перевірено її типами, збіркою, математикою й HTML-розміткою. Зокрема не
  бачено на очах режим «UA | EN» (два різні контейнери) і перетягування ручок.
- **Фази 2–4 плану не почато** — свідомо поетапно. Фаза 2 (вертикальна лінійка
  по висоті АРКУША) вимагає, щоб `PaginationPlugin` віддавав позиції розривів
  назовні; це найризикованіша частина, бо торкається пагінації.
- **Залишкова розбіжність 3 px** між лінійкою й колонкою, коли аркуш не
  вміщається: `PageColumn` має `overflow-y-auto` (смуга прокрутки 6 px входить
  у доступну ширину), лінійка — ні. Прибирати це треба разом із фазою 4
  (спільний масштаб), окремо — зайва ламкість.
- **`PdfEditorView.tsx` не уніфіковано** — далі має власні `?? 20/20/18/15`,
  хоч тепер міг би взяти `MIN_MARGIN_MM` зі спільного модуля.
- **Повний `npm test` не прогнано** — ланцюжок упирається в давню помилку
  `test:media-library` на Windows (58/59, порівняння шляху зі зворотними
  слешами), не повʼязану з цією роботою. Дотичні набори перевірено вручну.
- **Окремий план іконкової панелі «Книга і текст» не реалізовано** — власник
  вибрав лінійку.

---

## Сесія 13.09.2026 #04 — запис #161

**Тема:** фундамент плану лінійки — геометрія аркуша з одного місця
(`src/utils/pageGeometry.ts`).

**Записи:** #161.

**Зроблено.** Два плани (іконкова панель і лінійка/розбиття) складено, і
власник вибрав почати з фази 0. Новий чистий модуль `resolvePageGeometry` дає
`pageWidthMm/pageHeightMm/margins` і похідні `contentWidthMm/contentHeightMm`;
`EditorView` звів три колбеки до одного `getPageGeometry()`;
`useRealBookPages` перейшов на ту саму геометрію (там був справжній баг:
книга без `pageWidthMm` давала `NaN` і мовчки ламала пагінацію «Розвороту
книги»); новий тест `test:page-geometry` 21/21 у ланцюжку `npm test`.

**Не зроблено і чому.**
- **Фази 1–4 плану лінійки не почато** — свідомо поетапно: фаза 1 (горизонтальна
  лінійка по аркушу) наступна, фази 2–3 (вертикальна по сторінках + розриви
  всередині абзацу) найризикованіші, бо торкаються пагінації.
- **Окремий план іконкової панелі не реалізовано** — власник вибрав фазу 0.
- **`PdfEditorView.tsx` не уніфіковано** з новою геометрією — у нього власні
  `?? 20/20/18/15` для полів; окремий редактор, окрема черга.
- **Пʼять місць `PageRuler` і далі передають поля інлайном** — зникне у фазі 1.
- **Браузерного прогону не було**: фаза 0 за визначенням не змінює вигляд, але
  саме тому рендер колонки ніхто не дивився — перевірено типами, збіркою й
  тестом чистої математики.
- **Виправлення `NaN` у `useRealBookPages` перевірено логікою, не відтворенням**:
  книг без `pageWidthMm` у наявних даних немає (усі створюються з `initialBook`).

---

## Сесія 13.09.2026 #03 — запис #160

**Тема:** смужка стану в майстрі перенесення книги — щоб під час читання
`.docx` і збереження картинок було видно, що робота йде.

**Записи:** #160.

**Зроблено.** У `ImportMaterialsWizardModal.tsx` два стани (`busyLabel`,
`busyProgress`) і одна смужка на весь візард (`role="status"`,
`aria-live="polite"`) у тілі, поза умовами кроку; `readManuscriptFile` отримав
колбек `onImagesRead`, який кличеться зсередини вже наявного `convertImage` —
тому лічильник показує справжню кількість прочитаних картинок. Три написи:
читання файлу → розбір `.docx` → збереження в медіатеку з `N / M`. Три ключі
i18n uk/en.

**Не зроблено і чому.**
- **Смужку не бачив жоден браузер** — ні скріншота, ні кліку. Причина:
  для цього потрібна робоча сесія в студії; перевірено лише механізм
  лічильника (42 виклики за 1472 мс, 42 різні мітки часу) і збірку. Саме
  це варто глянути власнику на проді.
- **Тривалість самого збереження 42 картинок не міряно** — залежить від
  мережі й квоти.
- **Кроки «Зображення» і «3D-моделі» смужки не мають** — там `isBusy` без
  тексту. Свідомо: локальні файли читаються швидко, скарга була про рукопис.
- **Знаменник `N / 42` для розбору .docx** не робився: mammoth не віддає
  кількість картинок наперед, а для цього треба окремо розпаковувати zip.

---

## Сесія 13.09.2026 #02 — запис #159

**Тема:** картинки з `.docx` у книзі й медіатеці; розділи медіатеки по книгах;
два дефекти поділу на глави, знайдені живим прогоном на рукописі власника.

**Записи:** #159.

**Зроблено.** Новий `src/utils/docxManuscript.ts` (конвертер mammoth-HTML →
рукопис із маркерами `[IMG: docx-img-N "підпис"]`, HTML лишається 0.6 МБ
замість 52 МБ); візард витягує картинки з `.docx` і кладе їх у медіатеку під
тими самими id, що в маркерах; новий `GET /api/media/sections` (розділ — група
файлів за `book_id`, без міграції схеми); селектор книги й групування карток
у «Медіатеці»; виправлено два дефекти поділу: жирні заголовки
(`**Глава 1.**`) і зникнення тексту перед першим заголовком (глава «Вступ»);
чотири нові тести (82 перевірки) і живий прогін `live:agni-import` на
справжньому рукописі (21/21).

**Не зроблено і чому.**
- **Браузерного прогону не було** — ні кліків по візарду, ні селектора
  розділів. Причина: щоб дійти до цих екранів, потрібна робоча сесія в
  студії; перевірено типами, збіркою, HTTP-тестом і живим прогоном даних.
  Це перше, що варто зробити власнику — на проді після деплою.
- **Завантаження 37.4 МБ у сховище не робилось** — цим рукописом
  вичерпується понад третина безкоштовного ліміту (100 МБ). Шлях відмови
  квоти (маркер прибирається, цифра йде в тост) перевірено лише читанням
  коду.
- **Таблиці й зноски .docx** лишаються текстом — окрема задача.
- **Кодування не поширено** на «Форматування файлу» й «Базу знань»
  (`ManuscriptFormatterView.tsx`, `KnowledgeView.tsx`) — там і далі
  `extractRawText` без картинок і `file.text()` як UTF-8.
- **Міграції старих файлів без `book_id`** у розділи по книгах немає —
  вони показуються окремою групою «Без книги».
- **`test:media-library` має одне давнє падіння на Windows** — перевірка
  «файл усередині `media/`» порівнює шлях із прямими скісними рисками. Не
  цієї роботи й не полагоджено.

---

## Сесія 13.09.2026 #01 — запис #158

**Тема:** майстер «Перенесення книги з іншого сервісу» — визначення
кодування .txt (укр./рос./англ.), імпорт .docx, зображення в медіатеку
користувача замість data-URL у книзі.

**Записи:** #158.

**Зроблено.** Новий `src/utils/textEncoding.ts` (BOM → строгий UTF-8 →
однобайтові кандидати з частотною оцінкою якості); візард читає .docx
через mammoth і .txt через нове визначення кодування; зображення на
завершенні вантажаться в медіатеку (`POST /api/media/upload`) з
fallback на data-URL; i18n uk/en; новий тест `test:text-encoding` 18/18,
доданий у `npm test`.

**Не зроблено і чому.**
- **Живого прогону в браузері не було** — ні реального .docx, ні
  справжньої віддачі `/api/media/upload` із сесією. Перевірено типами,
  юніт-тестом кодування й читанням коду. Причина: сесія на пристрої
  власника без запущеного dev-сервера й тестового акаунта під рукою;
  за потреби перевіряється одним прогоном візарда.
- **Зображення всередині .docx не витягуються** — `mammoth.extractRawText`
  дає лише текст. Вирішено не робити: окрема робота з власним ризиком,
  а скарга стосувалась тексту.
- **Кодування не розширено на «Форматування файлу» й «Базу знань»**
  (`ManuscriptFormatterView.tsx`, `KnowledgeView.tsx`) — там `.txt` і
  далі читається як UTF-8. Причина: завдання було про візард
  перенесення; зміна цих екранів — окреме рішення.
- **Уже збережені книги не перекодовуються** — виправлено лише шлях
  читання файлу. Причина: перекодування сховища наосліп ризикованіше за
  саму ваду.

---

## Сесія 10.09.2026 #01 — запис #123

**Тема:** редизайн світлої теми «Книга і текст» за макетом FusionWrite —
liquid-glass + вода на фоні, повна панель «Вода і відблиски», нові
формат-функції (Colors/Highlight/Link/Divider), косметичне перегрупування
тулбару в «Текст»/«AI Асистент».

**Зроблено.** Усе перелічено в записі #123 вище: дизайн-токени й фон,
`WaterCausticsCanvas`/`WaterCausticsPanel`, чотири нові Tiptap-позначки
проведені через увесь конвеєр (парсинг → серіалізація → HTML-рендер →
«Розворот книги»), UI+i18n, вкладки тулбару, housekeeping-фікс (стороння
тека `Claude outputs/`).

**Не зроблено і чому.**
- Таблиця / Зміст книги / Розрив сторінки / Формула / H1-H3 — свідомо
  відкладено: набагато ширший blast radius (StarterKit, PDF-пагінація,
  і головне — DOCX/EPUB-експортер узагалі не парсить маркери). Не
  поспішав з ризикованою реалізацією нашвидкуруч.
- Публікація тестової книги на ЖИВІЙ вітрині — не виконана автономно:
  це публічна дія на реальному зовнішньому маркетплейсі, потребує свіжого
  явного «так» власника в чаті, а не інтерпретації давнього схвалення
  обсягу задачі. Конвеєр рендеру перевірено безпечно (функція +
  «Розворот книги» з реальним збереженим контентом, усі маркери разом).
- Мобільна/планшетна ширина нового дизайну не перевірялась.
- Коміт і перенесення на пристрій власника (`D:/Rama/Book_Creality`) —
  роблю одразу після цього запису журналу, тож на момент читання цього
  абзацу наступною сесією вони вже мають бути позначені нижче в «Що не
  запушено» або вже запушені.

---

## Сесія 08.09.2026 #04 — запис #122 (Codex)

**Тема:** проміжна презентаційна сторінка курсу мебелярів за PDF.

**Записи:** #122 — перевірка попередніх напрацювань, реалізація та перевірка.

**Зроблено:** локальна сторінка огляду у marketplace, перехід із картки мебелярів, перевірка типів/лінт і браузерна перевірка desktop/mobile. Завдання та обмеження записані у task.md.

**Не зроблено і чому:** деплой і перевірка реальної оплати/платного відео не початі — користувач попросив зберегти на диску й GitHub, але не просив розгортати production. Попереднього автора окремої сторінки встановити не вдалося: такої сторінки немає у перевірених робочих деревах. Чужі незакомічені файли не змінювалися. На чинній сторінці деталей у localhost зафіксовано Failed to fetch відгуків, причина не досліджувалась у межах цього завдання. Код сторінки запушено в marketplace комітом `53bb86a`; журнал Book_Creality оновлено й запушено окремо.


Найновіші зверху. «Не зроблено» — не докір, а вхідні дані для наступної
сесії: причина визначає, чи можна за це братися взагалі (немає ключа —
чекаємо; немає даних у джерела — не братися ніколи).

---

## Сесія 08.09.2026 #03 — запис #121

**Тема:** рушій ядра в тулбарі (надпис + перевірка доступу), кнопка
«Шлях героя» з модалкою 3 вкладок, фільтр моделей генерації героя.

**Зроблено.** Надпис «Обрано тепер в ядрі сайта модель: …» над селектором;
кнопка «Шлях героя» + новий `HeroJourneyModal` (Конструктор / Крива сюжету
з повзунками / Підсумок); фільтр image-моделей у `GenerateCharacterModal` за
`/api/ai/image-engines`; `analyze-emotional-arc` приймає `modelId` і йде в
ядро (мультипровайдер).

**Перевірено.** `tsc --noEmit` чистий (лише попередні `Claude outputs/`);
`vite build` і `esbuild server.ts` чисті.

**Відповідь власнику (доступ).** Змінювати рушій можуть лише admin і writer:
API `/api/chat/models` вимагає `canUseAi` (7 ролей, reader/guest — ні), а UI
додатково `disabled` для всіх без `canEditContent` (тільки admin + writer).

**Не зроблено і чому.**
- Візуально в браузері не дивився; мобільна/планшетна ширини — ні.
- Живий ШІ-виклик `analyze-emotional-arc` з modelId не ганяв на реальному ключі.
- «Конструктор» — проста форма 13 кроків, не покроковий майстер.

---

## Сесія 08.09.2026 #02 — запис #120

**Тема:** блок «Мої книги» на дашборді (створити/видалити книгу) + акцент
«Сонечка» в панелях і тулбарі.

**Зроблено.** Кнопка «Створити нову книгу» з вибором «візард / з нульової
планки», «Видалити книгу» з підтвердженням; `deleteBook` у сховищі,
`handleCreateBlankBook`/`handleDeleteBook` в App; спільний хук
`useSunAccentVars` і переведення amber/cyan/emerald акцентів на змінні сонця
в EditorView (тулбари, дерево розділів, панель «Персонажі і сцена», градієнти,
тости) та SidebarNav. Рівень письменника не чіпався.

**Перевірено.** `tsc --noEmit` чистий для моїх файлів (тільки попередні
`Claude outputs/lab-hero.tsx`); `vite build` чистий.

**Не зроблено і чому.**
- **Візуально в браузері не дивився** — кольори звірено лише за класами;
  мобільна/планшетна ширини — ні.
- **Анулювання «з нульової планки» мінімальне** (герої, сценарій, синопсис,
  теги, ілюстрації, сноски) — власник додатково вкаже, що ще обнулити.
- **Акцент сонця тільки в EditorView + SidebarNav** — решта екранів на своїх
  акцентах (постановка охоплювала лише панелі/тулбар).
- **Не закомічено** — прохання про коміт не було.

---

## Сесія 08.09.2026 #01 — запис #119

**Тема:** тренажер «Стиль письменника» (третя вкладка в розділі
«Тренажери», першою перед «Персонаж» і «Діалог»).

**Зроблено.** Дані 8 компонентів × 3 вправи (`styleTrainerData.ts`), сам
тренажер `StyleTrainer.tsx` (питання ШІ на основі уривка з книги, оцінка за
3 критеріями, XP), модалка додавання відповіді в рукопис (за тегом / у
кінець глави), вставка за тегом через нову `insertTextAfterTagInBook`,
розширення `/api/ai/evaluate-trainer` (8 компонентів `style-*`) і
нормалізація балів 0–100, ключі i18n uk/en, тест `test:style-trainer`.

**Перевірено.** Юніт-тест 15/15; живий сервер у демо-режимі — усі 8
`style-*` повертають правильні критерії (overall=78, xp=61), без регресії
character/dialogue, невідомий тип → 400; `vite build` і `esbuild server.ts`
чисті; `tsc --noEmit` чистий для моїх файлів.

**Не зроблено і чому.**
- **Живий Gemini-шлях 0–100 не доведено до кінця** — free-tier 429 (ліміт
  5 RPM) під час тесту; нормалізація перевірена на демо-шляху й код-ревʼю.
- **`tsc --noEmit` падає на чужому** `Claude outputs/lab-hero.tsx`
  (untracked Next.js-файл: `next-intl`, `@/i18n/navigation`, `./light-rays`)
  — це перешкода для `npm run lint`, не чіпав (не мій файл).
- **Візуально в браузері не дивився**; мобільна/планшетна ширини — ні.
- **Не закомічено** — прохання про коміт не було; пуш — з терміналу
  власника.

---

## Сесія 05.09.2026 #06 — запис #118

**РІШЕННЯ ВЛАСНИКА: від Gamma відмовляємось. Рушіїв лишається два робочі.**

**Чому — і чому це з'ясувалось аж тепер.** Перед самим запуском платної
генерації агент показав ціну (`/api/gamma/estimate`, безкоштовно): верстка
книги — **120–720 кредитів**, бо `DEFAULT_CARDS = 40`, а головний витратник —
зображення, які Gamma малює сама. Тоді ж в інтерфейсі став видно власний
опис рушія: «корисно для роздаткових матеріалів курсу, **а не для самої
книги**… **Переписує текст, а не верстає його**».

Тобто за 120–720 кредитів автор отримав би не свою книгу в іншій верстці, а
переказ свого тексту з чужими ілюстраціями — власні ілюстрації туди не
передаються взагалі. Рушій чесно писав це у своєму `limitUk` від самого
початку (#96–#99); ніхто просто не читав цей рядок уголос поруч із ціною.
Жодного кредита не витрачено.

**Що зроблено (`479cb04`).**
- Рушій **знято з реєстрації** в `registry.ts` — це той самий шов, заради
  якого реєстр і робився: чого немає в словнику, того немає ні для вибору,
  ні для маршруту, ні для тексту помилок.
- Прибрано вкладку «Матеріали (Gamma)» з Хабу публікації.
- Прибрано весь механізм `fixAction` і вікно `GammaKeyModal` (#115, #117):
  єдиним постачальником `fixAction` була Gamma, без неї це мертвий код.
- `.env.example`: блок Gamma лишився, але позначений як вимкнений — щоб
  не з'ясовувати ті самі межі вдруге.

**Що НЕ видалено і чому.** `server/gamma/*` (клієнт, конфіг, вартість,
сховище, ключі), `gammaRoutes.ts`, `GammaPanel.tsx` і всі 56 тестів
`test:gamma` лишились на місці. Власник сказав «поки що», тож повернення
має бути дешевим: розкоментувати рядок у `registry.ts` та імпорт над ним.

**Тести пішли за рішенням, а не за кодом.** Перелік рушіїв тепер
СТВЕРДЖУЄ відсутність Gamma (тихе повернення означало б, що автор знову
бачить цю кнопку); перевірка «недоступний рушій відмовляє як слід»
переїхала на chromium — він зареєстрований, але бінарника тут немає; і
додано випадок «запит знятого рушія» → 400 з переліком того, що лишилось,
бо в автора може бути збережений старий вибір.

Підсумки після правки: `pdf-routes` 57, `pdf-engines` 99, `pdf` 66,
`gamma` 56 (підсистема ціла), `publishing` 164, `publishing-routes` 89 —
0 провалено. Тайпчек чистий.

**Хвости, які лишились свідомо.**
- Ключ `GAMMA_API_KEY` у `.env` власника нікуди не дівся й ні на що не
  впливає. Підписка Gamma оплачена на місяць — це втрата, але вона вже
  сталась і рішення від неї не залежить.
- Тести №1 (автор без ключа) і №3 (автор із власною підпискою) **скасовані
  разом із темою** — перевіряти більше нічого.
- Живий виклик до Gamma (#116) лишається доведеним фактом: ключ і тариф
  робочі. Це знадобиться, якщо рушій колись повертатимуть.

---

## Сесія 05.09.2026 #05 — запис #117

**Тема:** тарифів насправді два, а вікно називало один.

**Знайдено при відповіді на питання власника**, а не тестом. `/api/pdf/engines`
стоїть лише під `requireAuth`, а всі `/api/gamma/*` — ще й під
`requirePlanAtLeast(['pro','ultra'])`. Тобто автор на безкоштовному тарифі
Студії бачив у виборі рушія «потрібен ваш ключ», ішов у Gamma по ключ,
оформлював ТАМ підписку, вставляв цілком дійсний ключ — і отримував 403 про
тариф, якого йому ніхто не називав. Глухий кут рівно для тієї людини, заради
якої вікно й робилось.

Плутанина тут природна: підписок справді дві й вони про різне. Тариф **Gamma**
(Pro/Ultra/Teams/Business) відкриває їхній API. Тариф **Студії** (Pro/Ultra)
відкриває сам рушій.

- Вимога тарифу Студії тепер названа в вікні ДО того, як автор піде по ключ,
  із переходом на екран підписки.
- Відмова сервера з `kind: 'plan_required'` кладе кнопку «Перейти до
  підписки» просто в текст помилки.
- `VitrynaPanel` отримала `onNavigateToTab` — раніше вона не мала чим
  перевести автора на підписку.

**Чому це не спливло раніше.** Адміністратор не бачить нічого з цього:
`requirePlanAtLeast` пропускає його без перевірки (`subscriptions.ts:363`), а
`resolveGammaKey` віддає йому ключ студії. Обидві гілки, які ламаються, — не
його.

**Гість цей шлях не відкриє взагалі:** `/api/pdf/engines` віддає гостю 401, а
`requirePlanAtLeast` відсікає гостя першим же рядком. Перевірити вікно можна
лише справжнім зареєстрованим автором без власного ключа — і, щоб дійти до
кінця, з тарифом Pro/Ultra.

**Не зроблено.** `303ddab` і цей запис не запушено. Вікна живцем усе ще ніхто
не бачив — потрібен той самий другий обліковий запис.

---

## Сесія 05.09.2026 #04 — запис #116

**Тема:** живий виклик до Gamma — ключ нарешті перевірено по-справжньому.

**КЛЮЧ ПРАЦЮЄ.** `GET /api/gamma/settings` на живому локальному сервері
повернув 50 тем із `public-api.gamma.app` (Alien, Ash, Ashrose, Atacama,
Atmosphere…, `hasMore: true`). Отже, і ключ дійсний, і тариф дає доступ до
API. Кредитів не витрачено: перелік тем безкоштовний. Види генерації, які
відкриті: `course_deck`, `landing`, `social`, `document`, `cover_art`.

Перелік рушіїв на тому ж прогоні: `nova` і `gamma` доступні, `chromium`
(«не знайдено за шляхом /usr/bin/chromium») і `pandoc` — ні, бо в
Windows-середовищі власника цих бінарників немає. Обидва без `fixAction`,
тобто лишаються вимкненими — саме як задумано.

**Як це вдалося зробити, попри дві перешкоди.** Керування комп'ютером
власник вмикати не став, а мережа з VM до gamma.app закрита. Спрацював
третій шлях: власник підняв `npm run dev` однією командою, а перевірку
провів агент через **вбудований браузер** — той працює на машині власника,
тож бачить і `localhost:3000`, і справжню мережу. Гостьовий режим для цього
не годиться: `/api/pdf/engines` під `requireAuth` віддає 401, потрібен
справжній вхід (його робив власник — пароль агент не вводить).

**#116 — причина невдачі більше не глушиться.**

Дорогою знайшлась вада, яка мало не зірвала всю перевірку. `/api/gamma/settings`
ловив помилку тем порожнім `catch` і віддавав `themes: null`. Через це
«ключ мертвий», «тариф без API», «Gamma не відповіла» і навіть «усе добре,
але форма відповіді не та» виглядали ЗЗОВНІ ОДНАКОВО — а саме цим
маршрутом ключ і перевіряють перед першою генерацією. Тепер поруч із
порожніми темами їде `themesErrorUk`; самі теми лишились прикрасою, екран
від їхньої відсутності не ламається.

**Помилка агента, варта запису.** Побачивши `themes`, що не є масивом, агент
оголосив виклик невдалим — і лише потім глянув на справжню форму відповіді.
А Gamma віддає `{ data: [...], hasMore, nextCursor }`, тобто виклик увесь
цей час був успішним. Урок простий: перевіряти форму відповіді, а не
припускати її.

**Не зроблено / відкрите.**
- `f081e03` і цей запис **не запушено** — потрібен термінал власника.
- **Гілка відмови в `themesErrorUk` не покрита тестом.** Живцем її не
  відтворити (для цього потрібен свідомо неробочий ключ), а route-тестів
  для `gammaRoutes` не існує взагалі — знадобився б новий каркас на зразок
  `test-pdfRoutes.mts` (express на випадковому порту, підставний
  `makeClient`, обхід `requirePlanAtLeast`). Це окрема робота, і робити її
  нашвидкуруч заради десяти рядків не варто — але покриття маршрутів Gamma
  лишається справжньою прогалиною.
- **Вікна підключення ключа так і не видно живцем**: власник — адміністратор,
  тож Gamma в нього доступна, і `fixAction` не спрацьовує. Щоб побачити
  вікно, потрібен звичайний автор без власного ключа.
- Жодної генерації ще не робилось — конвеєр Gamma наскрізь не проходили.

---

## Сесія 05.09.2026 #03 — запис #115

**Тема:** ключ Gamma — перевірка того, що є, і шлях до нього в інтерфейсі.

**Перевірено (мокованим набором, без мережі).** `test:gamma` 56,
`test:pdf-engines` 98, `test:publishing` 164, `test:publishing-routes` 89,
`test:pdf` 66, `test:pdf-routes` 54 — 527 тестів, 0 провалено.

Щоб вони взагалі запустились із VM, довелось обійти перешкоду, про яку
варто знати наступній сесії: **у `node_modules` лежить Windows-збірка
esbuild**, а `device_bash` працює в Linux, тож будь-який `tsx` там падає
з `TransformError`. Обхід — linux-збірка esbuild того ж номера версії
(0.28.2) у `/tmp` і `ESBUILD_BINARY_PATH` на неї; репозиторій і
`node_modules` при цьому не чіпаються. `tsc --noEmit` працює й без цього:
він чистий JS.

**Живого прогону НЕМАЄ і не було.** Мережу до `public-api.gamma.app` з
цієї VM закриває проксі сесії (403), тож ані ключ, ані тариф досі не
перевірені справжнім викликом. `test:gamma` цього не замінює: він
повністю на підставному `fetch`. Перевірка ключа лишається за власником.

**#115 — підключення ключа з вибору рушія.**

Вибір рушія був глухим кутом: недоступна Gamma малювалась вимкненою
кнопкою, причина лежала в підказці й рядком під переліком, а поле для
ключа — на іншому екрані. Автор мав сам здогадатись, що «не підключено» —
це те, що він може полагодити.

- У `PdfEngineAvailability` додано `fixAction` поряд із людським `fixUk`.
  Інтерфейс тепер відрізняє «натисніть і підключіть» від «немає бінарника
  в образі», не розбираючи текст причини й не питаючи `engine.id` — тобто
  без того розповзання назв рушіїв, проти якого існує реєстр. Ставить
  його лише гілка «немає ключа автора»; ненастроєний клієнт лишається
  нашою справою.
- Полагоджуваний рушій тепер жива кнопка з підписом «потрібен ваш ключ»,
  і вона відкриває `GammaKeyModal`.
- У вікні сказано, **чому ключ саме власний** (генерація списує кредити з
  власника ключа), що з ключем станеться — перевірка справжнім викликом
  до збереження, AES-256-GCM у спокої, назад не віддається ніколи,
  відключення однією кнопкою — і **окремо межа**: сервер розшифровує ключ
  у момент виклику, тож це захист від витоку бази, а не таємниця від
  студії. Плюс вимога тарифу і ціна генерації, бо без цього і те, і те
  з'ясовується вже після відмови.
- Посилання на кабінет спершу було вигаданим глибоким URL; замінено на
  `gamma.app` і той самий шлях «Settings → API», який уже вживає сервер.
- Тести на обидва боки `fixAction`: є там, де автор може діяти, немає
  там, де не може, і доходить до інтерфейсу через `listPdfEngines`.

**Не зроблено / відкрите.**
- `e86ec1a` і цей запис **не запушено** — потрібен термінал власника.
- Вікно не бачив у браузері жодного разу: без мережі до Gamma і без
  можливості підняти `npm run dev` з VM перевірено лише тайпчеком і
  тестами. Вигляд і поведінку варто глянути живцем.
- Поле для ключа лишилось і в `GammaPanel` — те саме, що у вікні, але
  своїм кодом. Не об'єднував: панель працює, а зайвий рефакторинг у
  чужій гілці — привід для конфлікту.

---

## Сесія 05.09.2026 #02 — запис #114

**Тема:** пуш накопиченого і полагодження лендінга за маркетплейсом.

**Зроблено.**
- **Пуш обох тек.** Book_Creality: `ad9a48d..14e5b44`. Маркетплейс не
  пішов з першого разу — поки готувався фікс, в origin приїхав
  `c88df14` (Phase B wave-4). Перебазував `950f330` на нього
  (`2745de3`, той самий діф 13/16), конфлікту не було: `c88df14`
  `lab-hero.tsx` не чіпає. Купу з ~266 «змінених» файлів (самі лише
  закінчення рядків) не чіпав — відклав у stash на час ребейзу і
  викинув; у коміт вона не потрапила.
- **#114 — лендінг за `/studio/about` не відкривався.** Виглядало як
  «сторінка не запускається»: у браузері єдине посилання «Fusion Lab
  Studio» і нескінченні запити на ту саму адресу.

  Причин виявилось дві, одна під одною.

  1. **Дефолтний trailingSlash-редирект Next.js** спрацьовує раніше за
     `beforeFiles`-rewrite, тож `/studio/about/` 308-редиректило на
     `/studio/about` — назад на перехідну сторінку, і по колу.
     Лікується `skipTrailingSlashRedirect: true` у `next.config.ts`
     маркетплейсу (`6409911`) — штатний обхід Next.js саме для
     rewrite на чужий origin.
  2. Після цього стало видно справжню причину: **rewrite
     `/studio/:path*` з'їдає кінцеву скісну**, тож `/studio/about` і
     `/studio/about/` приходять в Express однаково як `/about`.
     Розрізнення за `req.path` (#113) під маркетплейсом непрацездатне
     в принципі: перехідна сторінка віддавалась і на адресу зі
     скісною, а її відносний перехід на «about/» нарощував шлях —
     `about/about/`, `about/about/about/`… Прибрав перехідну сторінку
     зовсім: файл віддається на обидві адреси, а скісну доводить до
     ладу скрипт на початку `<head>` самої сторінки — браузер єдиний
     бачить справжню адресу, і умова стоїть на ній, а не на відповіді
     сервера, тож спрацьовує щонайбільше раз (`9568e86`).

**Перевірка.**
- `tsc --noEmit` — чисто (три помилки лишились тільки в невідстежуваній
  теці `Claude outputs/`, вона до проєкту не належить).
- Обидві причини підтверджені **на живому проді**, а не міркуванням:
  мережевий лог браузера показав спершу десятки однакових запитів на
  `/studio/about`, а після викладки `6409911` — ланцюжок
  `/studio/about` → `/studio/about/` → `/studio/about/about/`, який і
  вказав на з'їдену скісну.

**Підтверджено на живому проді після викладки обох комітів.**
- `/studio/about` → `/studio/about/` і зупиняється; `about.css` і
  `logo.png` тягнуться з `/studio/about/…` (200/304). Циклу немає.
- **Шрифти й іконки нарешті побачив** — Inter в інтерфейсі, курсивний
  Source Serif 4 у заголовку, Material Symbols справжніми гліфами, а не
  словами. Це перше відкриття сторінки з живими `fonts.googleapis.com`.
- Дрібниця, не помилка: на адресі без скісної браузер спекулятивно тягне
  `./about.css` і `./logo.png` (стають `/studio/about.css`,
  `/studio/logo.png`) ще до того, як скрипт встигне перекинути — два
  запити, відповіді відкидаються при переході. Прибрати їх можна лише
  розрізненням на сервері, якого під rewrite не існує.

**Не зроблено / відкрите.**
- Прибирання `D:\Rama\furnivision-architecture\fls-landing` і
  `Downloads\fls-landing.zip` — власник ще не підтвердив.

---

## Сесія 05.09.2026 — записи #111–#113

**Тема:** довести публікацію рушіями до живого прогону, перевести бренд з
«NOVA STUDIO» на «Fusion Lab Studio» і зробити Студії власний лендінг.

**Зроблено.**
- **#111** — ілюстрації в рушії Nova та вибір рушія публікації на бекенді.
- **#112** — лого й напис у шапці Студії (коміт `e0a4a75`), ребрендинг
  тексту в 12 файлах (`ffd5bfb`), символ на екрані входу (`bf0d2b7`);
  чотири тестові ілюстрації прибрано з реальної книги `BK-ETSY-SALES`;
  живий тест публікації рушіями Chromium і pandoc на тестовій книзі
  `BK-2084-CYBER` (реальних лістингів не торкався).
- **#113** — лендінг Студії на `/studio/about/`: макет Stitch перенесено зі
  збереженням дизайну й шрифтів, тарифи підставлено реальні, Tailwind CDN
  замінено скомпільованим CSS, переходи з лендінгу ведуть у сам застосунок.

**Не зроблено і чому.**
- **Лендінг закомічено, але не запушено** — коміт зроблено в теці власника
  на його прохання; пуш можливий лише з його терміналу (ні в пісочниці, ні
  в VM для `device_bash` немає облікових даних GitHub).
- **Шрифти лендінгу жодного разу не бачив живими** — хмарна пісочниця не
  пускає `fonts.googleapis.com`. Це не «чекає на щось», а межа перевірки:
  типографіку побачить власник із першого відкриття.
- **`PublishingHubView.tsx` і далі не вміє вибирати рушій публікації**
  (знахідка запису `#112`): бекенд це вміє з `#111`, інтерфейс — ні.
  Свідомо не займав: окремої постановки не було.
- **Обидві гілки не запушені** — по одному коміту в Book_Creality (лендінг)
  і в маркетплейсі (маска hero-заливки) чекають на `git push` власника.

---

## Сесія 03.09.2026 #02 — запис #102

**Тема:** довершити вибір рушія PDF в інтерфейсі — те, що сесія #01 почала
й лишила незакоміченим і неперевіреним.

**Зроблено.**
- **#102** — вибір рушія доведено до робочого стану. Маршрут переліку й
  гілка перегляду вперше покриті тестами (+19 у `test-pdfRoutes.mts`,
  35 → 54); закрито стан «перелік не дійшов» в інтерфейсі, який показував
  підпис «Чим верстати» без жодної кнопки під ним; і вперше за всі сесії
  цього конвеєра **пройшла справжня збірка `vite build`** — на диску
  власника вона неможлива (`node_modules` з-під Windows, у `rollup` немає
  linux-бінарника), тож зроблена у свіжому клоні репозиторію в хмарі.

**Не зроблено і чому.**
- **Коміту немає** — за AGENTS.md коміт робиться лише на прохання власника.
  Робота лежить у робочій теці; перелік файлів — у розділі «Що не запушено».
- **`.git/index.lock` від обірваної сесії лишився на місці** — видалення
  файлів із мого боку заборонене. Це перше, що треба прибрати перед комітом.
- **Жоден зовнішній рушій не зібрав справжнього PDF.** Chromium, pandoc і
  Gamma перевірені лише з боку маршруту: що обрано правильний, що відмова
  приходить із причиною й кодом, що друк під KDP вони чесно не беруть. Чи
  виходить із них гарна книга — покаже тільки зібраний образ.
- **Живої взаємодії з вибором рушія не бачив ніхто.** Кнопки, підказки,
  теми Chromium і попередження «у вітрину публікується власна верстка»
  перевірені типами й збіркою, не оком.
- **i18n нових написів немає** — текст вибору рушія жорстко українською, як
  і в перенесених вкладках Etsy (#84). Це той самий борг, не новий.

---

## Сесія 03.09.2026 #01 — записи #83–#101

**Тема:** Etsy-аналітика: інтеграція набору, чесність чисел, реальні дані.

**Зроблено.**
- **#83** — сторінку «Аналітика ринку Etsy» розбито на вкладки; перенесено
  калькулятор комісій і SEO-переклади; заведено модуль ядра `etsyAdvisor` і
  маршрут `/api/market/advisor`, тож виклики моделі повернулись в облік
  `usage_log` (у наборі вони били в Gemini напряму, повз облік).
- **#84** — «Аудит лістинга» **написаний наново**: перенесений екран брав
  частоту запиту з `Math.random()`, конкуренцію виводив із довжини тега, а
  три з пʼяти складників бала були константами.
- **#85** — «Динаміка ніші» на збережених прогонах скринінгу замість
  вигаданих трендів.
- **#86** — розбір усіх пʼяти вкладок, 11 знайдених дефектів, план на 5
  етапів. *(черга)*
- **#87** — аналіз реальних даних Etsy; знайдено, що офіційний збирач уже
  написаний у модулі публікації; складено Етап 0. *(черга)*
- **#88** — Етап 0 виконано: адаптер `etsyScreen`, чесний прапорець джерела,
  `competition` із реального `count`, вибір джерела без тихого відкоту.
- **#89** — «Аналітику ринку Etsy» перенесено у плаваюче вікно поверх студії
  (перетягування, зміна розміру, розмитий фон) на наявному
  `DraggablePanel`.
- **#90** — F12 у редакторі: повноекранний режим замінено плаваючим вікном
  на тому ж `DraggablePanel`; заразом звірка ключів словника `editor.*`.
- **#91** — обкладинка картки у вітрині: перша сторінка PDF → PNG у браузері,
  кнопки у «Вітрині» й у панелі мосту. Закриває пункт, що висів із #82;
  конвеєр публікації тепер повний.
- **#92** — обкладинка стала умовою публікації (без неї у вітрині не
  зʼявляється нічого), плюс безкоштовний уривок на 5–10 сторінок; правки в
  приймачі — новий вид медіа `sample` і кнопка «Читати уривок» на сторінці
  товару.
- **#93** — посилання на уривок перенесено під обкладинку (за вказівкою
  власника), підпис приведено до дії «завантажити»; заразом виправлено
  групування уривка в кабінеті продавця й `MediaKind` у спільних типах.
- **#101** — **чотири рушії PDF на вибір письменника.** Перевірка джерел до
  роботи зняла один із двох запропонованих пакетів: `markdown-pdf` тягне
  PhantomJS, депрекейтнутий з 2017 і не збирається на Alpine. Модель
  «markdown → CSS вирішує вигляд» збережено через headless Chromium
  (рішення власника). Eisvogel беремо як є. **Рішення власника: pandoc і
  TeX Live — в один образ Nova**, з відомою ціною в кілька гігабайтів на
  КОЖЕН деплой. *(у роботі)*
- **#100** — **медіатека на сервері.** Опис у базі, байти в `DATA_DIR`,
  у книзі — коротке посилання замість мегабайтів base64. Заразом виправлено
  наслідок мого ж #94: дзеркалення книги тягло весь альбом на кожне
  збереження. **Перенесення наявних картинок з браузерів — НЕ зроблено:**
  впирається в подвійне списання довічного ліміту місця, рішення за
  власником (три варіанти в записі).
- **#99** — **Gamma під власною підпискою автора.** Виправляє хибу мого ж
  рішення: перша версія списувала кредити з рахунку власника за кожного
  автора. Ключ студії тепер не підміняє відсутній ключ автора.
- **#98** — Gamma API вивчено цілком; **виправлено мою помилку з #96**:
  окрема генерація зображень у API Є, тож арт обкладинки повернуто в план і
  реалізовано. Додано оцінку вартості за документованими ставками й вимикач
  зображень — головної статті витрат.
- **#97** — третя гілка публікації «Матеріали (Gamma)»: чотири види
  матеріалів, текст складається з книги, вартість показана до натискання.
  Свідомо НЕ названа третім майданчиком продажу — на Gamma не купують.
- **#96** — інтеграція Gamma: клієнт, сховище задач, облік кредитів,
  маршрути за ґейтом тарифу. Дорогою зʼясувалось, що **окремої генерації
  зображень у публічному API немає** — арт обкладинки лишається ручною дією.
- **#95** — Gamma: дві пробні генерації (обкладинка + курс-дек українською)
  і план інтеграції. Головне з проби — **лишилося ~137 кредитів**, тобто
  масової функції для всіх авторів із цього не вийде. *(черга)*
- **#94** — **книга тепер живе на сервері**: джерело з охороною ревізій,
  зверстані файли на диску, складання й публікація у вітрину без браузера.
  Закриває найсерйознішу відкладену проблему — рукопис існував лише в
  IndexedDB одного браузера.

**Не зроблено і чому.**
- **Живий прогін скринінгу через Etsy API — немає ключа.** Увесь шлях
  написаний і покритий тестами на підставній відповіді, але на справжніх
  даних не працював жодного разу. Заявку на `ETSY_API_KEY` подає власник; до
  того решта Етапу 0 лишається неперевіреною в бою.
- **Збагачення відгуками, рейтингом і назвою крамниці** (Етап 0, п. 3) —
  залежить від ключа: це додаткові виклики, які без ключа нічим перевірити.
- **Плановий збір прогонів** (Етап 0, п. 7) — не почато. Поки його немає,
  «Динаміка ніші» майже завжди показуватиме «збережено лише один прогін»:
  зрізи зʼявляються тільки коли автор сам натиснув скринінг.
- **Вкладка «Конкуренти» з набору — не робитиметься взагалі.** Etsy не
  публікує ані продажів, ані доходу чужого магазину; у наборі ці числа
  беруться з `Math.random()`. Чесного варіанту без окремого джерела не існує,
  тож це закрите рішення, а не борг (#86).
- **«Радар трендів» у вигляді з набору — відхилено** з тієї ж причини;
  замість нього зроблено «Динаміку ніші» на реальних прогонах (#85).
- **`vite build` не запускався.** `node_modules` встановлені під Windows, у
  `rollup` немає linux-бінарника. Перевірено `tsc` (0 помилок) і тестами;
  складання перевіряє власник у себе.
- **Пуш не робився** — за правилом AGENTS.md, лише за прямою вказівкою
  власника. Локально 7 комітів: `6a01a6c`, `13b93e7`, `d1e309e`, `280ddc0`,
  `2203c60`, `d293830`, `bb5f667`.
- **i18n трьох перенесених вкладок** — борг: текст інтерфейсу жорстко
  українською, у словник винесені лише назви вкладок (#84).

- **Живої взаємодії з вікнами не перевіряв** (#89, #90): перетягування,
  зміну розміру мишею й поведінку модалок редактора всередині вікна тут
  відтворити нічим — потрібне око власника.
- **Інтерфейсу для серверної публікації ще немає** (#94): маршрути готові й
  покриті тестами, але «Вітрина» досі публікує старим шляхом (книга в тілі
  запиту). Живого прогону нових маршрутів не було жодного.
- **Бік маркетплейсу не перевірявся взагалі** (#92): міграція застосовується
  на деплої, `prisma generate` локально заблокований, збірки Next тут немає.
  **Порядок увімкнення важливий:** спершу деплой API з міграцією, потім
  публікація з Nova — інакше приймач відхилить `sample` і покладе уривок за
  платну стіну.

**Тести після сесії:** `test-gamma` 56/0 (новий), `test-market` 120/0,
`test-etsy-screen` 50/0 (новий),
`test-listingAudit` 43/0 (новий), `test-pdf-cover` 19/0 (новий),
`test-pdfRenderer` 66/0, `test-pdfRoutes` 29/0, `test-coreAiRegistry` 52/0,
`tsc` — 0.

---

## Сесія 02.09.2026 #02 — записи #65–#82

**Тема:** King Market Intelligence і повний конвеєр публікації книги у
вітрину, включно з власним рушієм PDF.

**Зроблено.** Модуль аналітики Etsy за ТЗ v1.0 (#65, #66); `.gitattributes`
проти CRLF-шуму (#67); дві вади режиму JSON для Claude, знайдені живим
прогоном (#68); міст до вітрини — перевірка ключа, порожня адреса, причина
відмови, рівень slug (#69–#71, #73, #74); прогін конвеєра однією кнопкою
(#72); канал передачі файла книги (#75); **рушій верстки PDF** із вбудованим
шрифтом (#76) і два джерела макета — з книги та від моделі (#77); конвеєр
замкнено: макет → PDF → лістинг → файл (#78); **Amazon KDP** з дзеркальними
полями й корінцем за обсягом (#79); ручне зняття з вітрини (#80); тестова
книга зі справжнім вмістом і каналом для обкладинки (#82).

**Не зроблено і чому.**
- ~~**Клієнтський рендер обкладинки**~~ — **закрито в сесії 03.09.2026 #01**
  (запис #91): перша сторінка PDF → PNG у браузері, кнопки у «Вітрині» й у
  панелі мосту.
- **Попередження `pg` про `client.query()`** — діагностовано, не виправлено
  **свідомо**. Механізм знайдено, але місце виклику не встановлюється
  читанням, а `prisma generate` локально не запускається (заблокований
  `binaries.prisma.sh`). Замість здогадної правки поставлено
  `--trace-deprecation`: наступний деплой дасть стек (#72).
- **Комітів у репозиторії маркетплейсу немає.** Станом на 03.09.2026 там
  **46 файлів зі справжніми змінами** (перевірено `git diff
  --ignore-cr-at-eol`) — правки мосту, `prisma.service.ts`, `Dockerfile` і
  раніший набір Фази B5. Останній коміт гілки — `1fac6ce`.
- **`BRIDGE_SELLER_SLUG` на Railway** — має виставити власник у змінних
  середовища сервісу API.

---

## Сесія 02.09.2026 #01 — записи #61–#64

**Тема:** виправлення застарілих вад і хвиля 2 плану «18 навичок».

**Зроблено.** Відсутній `renderCoreTemplate` для трьох модулів `/diagn`
(#61); ґейт дозволів для `/api/ai/analyze-text-competences`, якого не було
(#62); показники глави й сила початку — задачі 6 і 8 плану «18 навичок»
(#63); модуль публікації KDP/Etsy проведено через журнальований процес
перевірки (#64).

**Не зроблено.** У записах цієї сесії окремо незакритих пунктів не названо.

---
