# Журнал сесій — і єдиний дім записів #114–#118

# Журнал сесій

## Сесія 10.09.2026 #01 — запис #123

**Тема:** редизайн світлої теми «Книга і текст» за макетом FusionWrite —
liquid-glass + вода на фоні, повна панель «Вода і відблиски», нові
формат-функції (Colors/Highlight/Link/Divider), косметичне перегрупування
тулбару в «Текст»/«AI Асистент».

**Зроблено.** Усе перелічено в записі #123 вище: дизайн-токени й фон,
`WaterCausticsCanvas`/`WaterCausticsPanel`, чотири нові Tiptap-позначки
проведені через увесь конвеєр (парсинг → серіалізація → HTML-рендер →
«Розворот книги»), UI+i18n, вкладки тулбару, housekeeping-фікс (стороння
тека `Claude outputs/`).

**Не зроблено і чому.**
- Таблиця / Зміст книги / Розрив сторінки / Формула / H1-H3 — свідомо
  відкладено: набагато ширший blast radius (StarterKit, PDF-пагінація,
  і головне — DOCX/EPUB-експортер узагалі не парсить маркери). Не
  поспішав з ризикованою реалізацією нашвидкуруч.
- Публікація тестової книги на ЖИВІЙ вітрині — не виконана автономно:
  це публічна дія на реальному зовнішньому маркетплейсі, потребує свіжого
  явного «так» власника в чаті, а не інтерпретації давнього схвалення
  обсягу задачі. Конвеєр рендеру перевірено безпечно (функція +
  «Розворот книги» з реальним збереженим контентом, усі маркери разом).
- Мобільна/планшетна ширина нового дизайну не перевірялась.
- Коміт і перенесення на пристрій власника (`D:/Rama/Book_Creality`) —
  роблю одразу після цього запису журналу, тож на момент читання цього
  абзацу наступною сесією вони вже мають бути позначені нижче в «Що не
  запушено» або вже запушені.

---

## Сесія 08.09.2026 #04 — запис #122 (Codex)

**Тема:** проміжна презентаційна сторінка курсу мебелярів за PDF.

**Записи:** #122 — перевірка попередніх напрацювань, реалізація та перевірка.

**Зроблено:** локальна сторінка огляду у marketplace, перехід із картки мебелярів, перевірка типів/лінт і браузерна перевірка desktop/mobile. Завдання та обмеження записані у task.md.

**Не зроблено і чому:** деплой і перевірка реальної оплати/платного відео не початі — користувач попросив зберегти на диску й GitHub, але не просив розгортати production. Попереднього автора окремої сторінки встановити не вдалося: такої сторінки немає у перевірених робочих деревах. Чужі незакомічені файли не змінювалися. На чинній сторінці деталей у localhost зафіксовано Failed to fetch відгуків, причина не досліджувалась у межах цього завдання. Код сторінки запушено в marketplace комітом `53bb86a`; журнал Book_Creality оновлено й запушено окремо.


Найновіші зверху. «Не зроблено» — не докір, а вхідні дані для наступної
сесії: причина визначає, чи можна за це братися взагалі (немає ключа —
чекаємо; немає даних у джерела — не братися ніколи).

---

## Сесія 08.09.2026 #03 — запис #121

**Тема:** рушій ядра в тулбарі (надпис + перевірка доступу), кнопка
«Шлях героя» з модалкою 3 вкладок, фільтр моделей генерації героя.

**Зроблено.** Надпис «Обрано тепер в ядрі сайта модель: …» над селектором;
кнопка «Шлях героя» + новий `HeroJourneyModal` (Конструктор / Крива сюжету
з повзунками / Підсумок); фільтр image-моделей у `GenerateCharacterModal` за
`/api/ai/image-engines`; `analyze-emotional-arc` приймає `modelId` і йде в
ядро (мультипровайдер).

**Перевірено.** `tsc --noEmit` чистий (лише попередні `Claude outputs/`);
`vite build` і `esbuild server.ts` чисті.

**Відповідь власнику (доступ).** Змінювати рушій можуть лише admin і writer:
API `/api/chat/models` вимагає `canUseAi` (7 ролей, reader/guest — ні), а UI
додатково `disabled` для всіх без `canEditContent` (тільки admin + writer).

**Не зроблено і чому.**
- Візуально в браузері не дивився; мобільна/планшетна ширини — ні.
- Живий ШІ-виклик `analyze-emotional-arc` з modelId не ганяв на реальному ключі.
- «Конструктор» — проста форма 13 кроків, не покроковий майстер.

---

## Сесія 08.09.2026 #02 — запис #120

**Тема:** блок «Мої книги» на дашборді (створити/видалити книгу) + акцент
«Сонечка» в панелях і тулбарі.

**Зроблено.** Кнопка «Створити нову книгу» з вибором «візард / з нульової
планки», «Видалити книгу» з підтвердженням; `deleteBook` у сховищі,
`handleCreateBlankBook`/`handleDeleteBook` в App; спільний хук
`useSunAccentVars` і переведення amber/cyan/emerald акцентів на змінні сонця
в EditorView (тулбари, дерево розділів, панель «Персонажі і сцена», градієнти,
тости) та SidebarNav. Рівень письменника не чіпався.

**Перевірено.** `tsc --noEmit` чистий для моїх файлів (тільки попередні
`Claude outputs/lab-hero.tsx`); `vite build` чистий.

**Не зроблено і чому.**
- **Візуально в браузері не дивився** — кольори звірено лише за класами;
  мобільна/планшетна ширини — ні.
- **Анулювання «з нульової планки» мінімальне** (герої, сценарій, синопсис,
  теги, ілюстрації, сноски) — власник додатково вкаже, що ще обнулити.
- **Акцент сонця тільки в EditorView + SidebarNav** — решта екранів на своїх
  акцентах (постановка охоплювала лише панелі/тулбар).
- **Не закомічено** — прохання про коміт не було.

---

## Сесія 08.09.2026 #01 — запис #119

**Тема:** тренажер «Стиль письменника» (третя вкладка в розділі
«Тренажери», першою перед «Персонаж» і «Діалог»).

**Зроблено.** Дані 8 компонентів × 3 вправи (`styleTrainerData.ts`), сам
тренажер `StyleTrainer.tsx` (питання ШІ на основі уривка з книги, оцінка за
3 критеріями, XP), модалка додавання відповіді в рукопис (за тегом / у
кінець глави), вставка за тегом через нову `insertTextAfterTagInBook`,
розширення `/api/ai/evaluate-trainer` (8 компонентів `style-*`) і
нормалізація балів 0–100, ключі i18n uk/en, тест `test:style-trainer`.

**Перевірено.** Юніт-тест 15/15; живий сервер у демо-режимі — усі 8
`style-*` повертають правильні критерії (overall=78, xp=61), без регресії
character/dialogue, невідомий тип → 400; `vite build` і `esbuild server.ts`
чисті; `tsc --noEmit` чистий для моїх файлів.

**Не зроблено і чому.**
- **Живий Gemini-шлях 0–100 не доведено до кінця** — free-tier 429 (ліміт
  5 RPM) під час тесту; нормалізація перевірена на демо-шляху й код-ревʼю.
- **`tsc --noEmit` падає на чужому** `Claude outputs/lab-hero.tsx`
  (untracked Next.js-файл: `next-intl`, `@/i18n/navigation`, `./light-rays`)
  — це перешкода для `npm run lint`, не чіпав (не мій файл).
- **Візуально в браузері не дивився**; мобільна/планшетна ширини — ні.
- **Не закомічено** — прохання про коміт не було; пуш — з терміналу
  власника.

---

## Сесія 05.09.2026 #06 — запис #118

**РІШЕННЯ ВЛАСНИКА: від Gamma відмовляємось. Рушіїв лишається два робочі.**

**Чому — і чому це з'ясувалось аж тепер.** Перед самим запуском платної
генерації агент показав ціну (`/api/gamma/estimate`, безкоштовно): верстка
книги — **120–720 кредитів**, бо `DEFAULT_CARDS = 40`, а головний витратник —
зображення, які Gamma малює сама. Тоді ж в інтерфейсі став видно власний
опис рушія: «корисно для роздаткових матеріалів курсу, **а не для самої
книги**… **Переписує текст, а не верстає його**».

Тобто за 120–720 кредитів автор отримав би не свою книгу в іншій верстці, а
переказ свого тексту з чужими ілюстраціями — власні ілюстрації туди не
передаються взагалі. Рушій чесно писав це у своєму `limitUk` від самого
початку (#96–#99); ніхто просто не читав цей рядок уголос поруч із ціною.
Жодного кредита не витрачено.

**Що зроблено (`479cb04`).**
- Рушій **знято з реєстрації** в `registry.ts` — це той самий шов, заради
  якого реєстр і робився: чого немає в словнику, того немає ні для вибору,
  ні для маршруту, ні для тексту помилок.
- Прибрано вкладку «Матеріали (Gamma)» з Хабу публікації.
- Прибрано весь механізм `fixAction` і вікно `GammaKeyModal` (#115, #117):
  єдиним постачальником `fixAction` була Gamma, без неї це мертвий код.
- `.env.example`: блок Gamma лишився, але позначений як вимкнений — щоб
  не з'ясовувати ті самі межі вдруге.

**Що НЕ видалено і чому.** `server/gamma/*` (клієнт, конфіг, вартість,
сховище, ключі), `gammaRoutes.ts`, `GammaPanel.tsx` і всі 56 тестів
`test:gamma` лишились на місці. Власник сказав «поки що», тож повернення
має бути дешевим: розкоментувати рядок у `registry.ts` та імпорт над ним.

**Тести пішли за рішенням, а не за кодом.** Перелік рушіїв тепер
СТВЕРДЖУЄ відсутність Gamma (тихе повернення означало б, що автор знову
бачить цю кнопку); перевірка «недоступний рушій відмовляє як слід»
переїхала на chromium — він зареєстрований, але бінарника тут немає; і
додано випадок «запит знятого рушія» → 400 з переліком того, що лишилось,
бо в автора може бути збережений старий вибір.

Підсумки після правки: `pdf-routes` 57, `pdf-engines` 99, `pdf` 66,
`gamma` 56 (підсистема ціла), `publishing` 164, `publishing-routes` 89 —
0 провалено. Тайпчек чистий.

**Хвости, які лишились свідомо.**
- Ключ `GAMMA_API_KEY` у `.env` власника нікуди не дівся й ні на що не
  впливає. Підписка Gamma оплачена на місяць — це втрата, але вона вже
  сталась і рішення від неї не залежить.
- Тести №1 (автор без ключа) і №3 (автор із власною підпискою) **скасовані
  разом із темою** — перевіряти більше нічого.
- Живий виклик до Gamma (#116) лишається доведеним фактом: ключ і тариф
  робочі. Це знадобиться, якщо рушій колись повертатимуть.

---

## Сесія 05.09.2026 #05 — запис #117

**Тема:** тарифів насправді два, а вікно називало один.

**Знайдено при відповіді на питання власника**, а не тестом. `/api/pdf/engines`
стоїть лише під `requireAuth`, а всі `/api/gamma/*` — ще й під
`requirePlanAtLeast(['pro','ultra'])`. Тобто автор на безкоштовному тарифі
Студії бачив у виборі рушія «потрібен ваш ключ», ішов у Gamma по ключ,
оформлював ТАМ підписку, вставляв цілком дійсний ключ — і отримував 403 про
тариф, якого йому ніхто не називав. Глухий кут рівно для тієї людини, заради
якої вікно й робилось.

Плутанина тут природна: підписок справді дві й вони про різне. Тариф **Gamma**
(Pro/Ultra/Teams/Business) відкриває їхній API. Тариф **Студії** (Pro/Ultra)
відкриває сам рушій.

- Вимога тарифу Студії тепер названа в вікні ДО того, як автор піде по ключ,
  із переходом на екран підписки.
- Відмова сервера з `kind: 'plan_required'` кладе кнопку «Перейти до
  підписки» просто в текст помилки.
- `VitrynaPanel` отримала `onNavigateToTab` — раніше вона не мала чим
  перевести автора на підписку.

**Чому це не спливло раніше.** Адміністратор не бачить нічого з цього:
`requirePlanAtLeast` пропускає його без перевірки (`subscriptions.ts:363`), а
`resolveGammaKey` віддає йому ключ студії. Обидві гілки, які ламаються, — не
його.

**Гість цей шлях не відкриє взагалі:** `/api/pdf/engines` віддає гостю 401, а
`requirePlanAtLeast` відсікає гостя першим же рядком. Перевірити вікно можна
лише справжнім зареєстрованим автором без власного ключа — і, щоб дійти до
кінця, з тарифом Pro/Ultra.

**Не зроблено.** `303ddab` і цей запис не запушено. Вікна живцем усе ще ніхто
не бачив — потрібен той самий другий обліковий запис.

---

## Сесія 05.09.2026 #04 — запис #116

**Тема:** живий виклик до Gamma — ключ нарешті перевірено по-справжньому.

**КЛЮЧ ПРАЦЮЄ.** `GET /api/gamma/settings` на живому локальному сервері
повернув 50 тем із `public-api.gamma.app` (Alien, Ash, Ashrose, Atacama,
Atmosphere…, `hasMore: true`). Отже, і ключ дійсний, і тариф дає доступ до
API. Кредитів не витрачено: перелік тем безкоштовний. Види генерації, які
відкриті: `course_deck`, `landing`, `social`, `document`, `cover_art`.

Перелік рушіїв на тому ж прогоні: `nova` і `gamma` доступні, `chromium`
(«не знайдено за шляхом /usr/bin/chromium») і `pandoc` — ні, бо в
Windows-середовищі власника цих бінарників немає. Обидва без `fixAction`,
тобто лишаються вимкненими — саме як задумано.

**Як це вдалося зробити, попри дві перешкоди.** Керування комп'ютером
власник вмикати не став, а мережа з VM до gamma.app закрита. Спрацював
третій шлях: власник підняв `npm run dev` однією командою, а перевірку
провів агент через **вбудований браузер** — той працює на машині власника,
тож бачить і `localhost:3000`, і справжню мережу. Гостьовий режим для цього
не годиться: `/api/pdf/engines` під `requireAuth` віддає 401, потрібен
справжній вхід (його робив власник — пароль агент не вводить).

**#116 — причина невдачі більше не глушиться.**

Дорогою знайшлась вада, яка мало не зірвала всю перевірку. `/api/gamma/settings`
ловив помилку тем порожнім `catch` і віддавав `themes: null`. Через це
«ключ мертвий», «тариф без API», «Gamma не відповіла» і навіть «усе добре,
але форма відповіді не та» виглядали ЗЗОВНІ ОДНАКОВО — а саме цим
маршрутом ключ і перевіряють перед першою генерацією. Тепер поруч із
порожніми темами їде `themesErrorUk`; самі теми лишились прикрасою, екран
від їхньої відсутності не ламається.

**Помилка агента, варта запису.** Побачивши `themes`, що не є масивом, агент
оголосив виклик невдалим — і лише потім глянув на справжню форму відповіді.
А Gamma віддає `{ data: [...], hasMore, nextCursor }`, тобто виклик увесь
цей час був успішним. Урок простий: перевіряти форму відповіді, а не
припускати її.

**Не зроблено / відкрите.**
- `f081e03` і цей запис **не запушено** — потрібен термінал власника.
- **Гілка відмови в `themesErrorUk` не покрита тестом.** Живцем її не
  відтворити (для цього потрібен свідомо неробочий ключ), а route-тестів
  для `gammaRoutes` не існує взагалі — знадобився б новий каркас на зразок
  `test-pdfRoutes.mts` (express на випадковому порту, підставний
  `makeClient`, обхід `requirePlanAtLeast`). Це окрема робота, і робити її
  нашвидкуруч заради десяти рядків не варто — але покриття маршрутів Gamma
  лишається справжньою прогалиною.
- **Вікна підключення ключа так і не видно живцем**: власник — адміністратор,
  тож Gamma в нього доступна, і `fixAction` не спрацьовує. Щоб побачити
  вікно, потрібен звичайний автор без власного ключа.
- Жодної генерації ще не робилось — конвеєр Gamma наскрізь не проходили.

---

## Сесія 05.09.2026 #03 — запис #115

**Тема:** ключ Gamma — перевірка того, що є, і шлях до нього в інтерфейсі.

**Перевірено (мокованим набором, без мережі).** `test:gamma` 56,
`test:pdf-engines` 98, `test:publishing` 164, `test:publishing-routes` 89,
`test:pdf` 66, `test:pdf-routes` 54 — 527 тестів, 0 провалено.

Щоб вони взагалі запустились із VM, довелось обійти перешкоду, про яку
варто знати наступній сесії: **у `node_modules` лежить Windows-збірка
esbuild**, а `device_bash` працює в Linux, тож будь-який `tsx` там падає
з `TransformError`. Обхід — linux-збірка esbuild того ж номера версії
(0.28.2) у `/tmp` і `ESBUILD_BINARY_PATH` на неї; репозиторій і
`node_modules` при цьому не чіпаються. `tsc --noEmit` працює й без цього:
він чистий JS.

**Живого прогону НЕМАЄ і не було.** Мережу до `public-api.gamma.app` з
цієї VM закриває проксі сесії (403), тож ані ключ, ані тариф досі не
перевірені справжнім викликом. `test:gamma` цього не замінює: він
повністю на підставному `fetch`. Перевірка ключа лишається за власником.

**#115 — підключення ключа з вибору рушія.**

Вибір рушія був глухим кутом: недоступна Gamma малювалась вимкненою
кнопкою, причина лежала в підказці й рядком під переліком, а поле для
ключа — на іншому екрані. Автор мав сам здогадатись, що «не підключено» —
це те, що він може полагодити.

- У `PdfEngineAvailability` додано `fixAction` поряд із людським `fixUk`.
  Інтерфейс тепер відрізняє «натисніть і підключіть» від «немає бінарника
  в образі», не розбираючи текст причини й не питаючи `engine.id` — тобто
  без того розповзання назв рушіїв, проти якого існує реєстр. Ставить
  його лише гілка «немає ключа автора»; ненастроєний клієнт лишається
  нашою справою.
- Полагоджуваний рушій тепер жива кнопка з підписом «потрібен ваш ключ»,
  і вона відкриває `GammaKeyModal`.
- У вікні сказано, **чому ключ саме власний** (генерація списує кредити з
  власника ключа), що з ключем станеться — перевірка справжнім викликом
  до збереження, AES-256-GCM у спокої, назад не віддається ніколи,
  відключення однією кнопкою — і **окремо межа**: сервер розшифровує ключ
  у момент виклику, тож це захист від витоку бази, а не таємниця від
  студії. Плюс вимога тарифу і ціна генерації, бо без цього і те, і те
  з'ясовується вже після відмови.
- Посилання на кабінет спершу було вигаданим глибоким URL; замінено на
  `gamma.app` і той самий шлях «Settings → API», який уже вживає сервер.
- Тести на обидва боки `fixAction`: є там, де автор може діяти, немає
  там, де не може, і доходить до інтерфейсу через `listPdfEngines`.

**Не зроблено / відкрите.**
- `e86ec1a` і цей запис **не запушено** — потрібен термінал власника.
- Вікно не бачив у браузері жодного разу: без мережі до Gamma і без
  можливості підняти `npm run dev` з VM перевірено лише тайпчеком і
  тестами. Вигляд і поведінку варто глянути живцем.
- Поле для ключа лишилось і в `GammaPanel` — те саме, що у вікні, але
  своїм кодом. Не об'єднував: панель працює, а зайвий рефакторинг у
  чужій гілці — привід для конфлікту.

---

## Сесія 05.09.2026 #02 — запис #114

**Тема:** пуш накопиченого і полагодження лендінга за маркетплейсом.

**Зроблено.**
- **Пуш обох тек.** Book_Creality: `ad9a48d..14e5b44`. Маркетплейс не
  пішов з першого разу — поки готувався фікс, в origin приїхав
  `c88df14` (Phase B wave-4). Перебазував `950f330` на нього
  (`2745de3`, той самий діф 13/16), конфлікту не було: `c88df14`
  `lab-hero.tsx` не чіпає. Купу з ~266 «змінених» файлів (самі лише
  закінчення рядків) не чіпав — відклав у stash на час ребейзу і
  викинув; у коміт вона не потрапила.
- **#114 — лендінг за `/studio/about` не відкривався.** Виглядало як
  «сторінка не запускається»: у браузері єдине посилання «Fusion Lab
  Studio» і нескінченні запити на ту саму адресу.

  Причин виявилось дві, одна під одною.

  1. **Дефолтний trailingSlash-редирект Next.js** спрацьовує раніше за
     `beforeFiles`-rewrite, тож `/studio/about/` 308-редиректило на
     `/studio/about` — назад на перехідну сторінку, і по колу.
     Лікується `skipTrailingSlashRedirect: true` у `next.config.ts`
     маркетплейсу (`6409911`) — штатний обхід Next.js саме для
     rewrite на чужий origin.
  2. Після цього стало видно справжню причину: **rewrite
     `/studio/:path*` з'їдає кінцеву скісну**, тож `/studio/about` і
     `/studio/about/` приходять в Express однаково як `/about`.
     Розрізнення за `req.path` (#113) під маркетплейсом непрацездатне
     в принципі: перехідна сторінка віддавалась і на адресу зі
     скісною, а її відносний перехід на «about/» нарощував шлях —
     `about/about/`, `about/about/about/`… Прибрав перехідну сторінку
     зовсім: файл віддається на обидві адреси, а скісну доводить до
     ладу скрипт на початку `<head>` самої сторінки — браузер єдиний
     бачить справжню адресу, і умова стоїть на ній, а не на відповіді
     сервера, тож спрацьовує щонайбільше раз (`9568e86`).

**Перевірка.**
- `tsc --noEmit` — чисто (три помилки лишились тільки в невідстежуваній
  теці `Claude outputs/`, вона до проєкту не належить).
- Обидві причини підтверджені **на живому проді**, а не міркуванням:
  мережевий лог браузера показав спершу десятки однакових запитів на
  `/studio/about`, а після викладки `6409911` — ланцюжок
  `/studio/about` → `/studio/about/` → `/studio/about/about/`, який і
  вказав на з'їдену скісну.

**Підтверджено на живому проді після викладки обох комітів.**
- `/studio/about` → `/studio/about/` і зупиняється; `about.css` і
  `logo.png` тягнуться з `/studio/about/…` (200/304). Циклу немає.
- **Шрифти й іконки нарешті побачив** — Inter в інтерфейсі, курсивний
  Source Serif 4 у заголовку, Material Symbols справжніми гліфами, а не
  словами. Це перше відкриття сторінки з живими `fonts.googleapis.com`.
- Дрібниця, не помилка: на адресі без скісної браузер спекулятивно тягне
  `./about.css` і `./logo.png` (стають `/studio/about.css`,
  `/studio/logo.png`) ще до того, як скрипт встигне перекинути — два
  запити, відповіді відкидаються при переході. Прибрати їх можна лише
  розрізненням на сервері, якого під rewrite не існує.

**Не зроблено / відкрите.**
- Прибирання `D:\Rama\furnivision-architecture\fls-landing` і
  `Downloads\fls-landing.zip` — власник ще не підтвердив.

---

## Сесія 05.09.2026 — записи #111–#113

**Тема:** довести публікацію рушіями до живого прогону, перевести бренд з
«NOVA STUDIO» на «Fusion Lab Studio» і зробити Студії власний лендінг.

**Зроблено.**
- **#111** — ілюстрації в рушії Nova та вибір рушія публікації на бекенді.
- **#112** — лого й напис у шапці Студії (коміт `e0a4a75`), ребрендинг
  тексту в 12 файлах (`ffd5bfb`), символ на екрані входу (`bf0d2b7`);
  чотири тестові ілюстрації прибрано з реальної книги `BK-ETSY-SALES`;
  живий тест публікації рушіями Chromium і pandoc на тестовій книзі
  `BK-2084-CYBER` (реальних лістингів не торкався).
- **#113** — лендінг Студії на `/studio/about/`: макет Stitch перенесено зі
  збереженням дизайну й шрифтів, тарифи підставлено реальні, Tailwind CDN
  замінено скомпільованим CSS, переходи з лендінгу ведуть у сам застосунок.

**Не зроблено і чому.**
- **Лендінг закомічено, але не запушено** — коміт зроблено в теці власника
  на його прохання; пуш можливий лише з його терміналу (ні в пісочниці, ні
  в VM для `device_bash` немає облікових даних GitHub).
- **Шрифти лендінгу жодного разу не бачив живими** — хмарна пісочниця не
  пускає `fonts.googleapis.com`. Це не «чекає на щось», а межа перевірки:
  типографіку побачить власник із першого відкриття.
- **`PublishingHubView.tsx` і далі не вміє вибирати рушій публікації**
  (знахідка запису `#112`): бекенд це вміє з `#111`, інтерфейс — ні.
  Свідомо не займав: окремої постановки не було.
- **Обидві гілки не запушені** — по одному коміту в Book_Creality (лендінг)
  і в маркетплейсі (маска hero-заливки) чекають на `git push` власника.

---

## Сесія 03.09.2026 #02 — запис #102

**Тема:** довершити вибір рушія PDF в інтерфейсі — те, що сесія #01 почала
й лишила незакоміченим і неперевіреним.

**Зроблено.**
- **#102** — вибір рушія доведено до робочого стану. Маршрут переліку й
  гілка перегляду вперше покриті тестами (+19 у `test-pdfRoutes.mts`,
  35 → 54); закрито стан «перелік не дійшов» в інтерфейсі, який показував
  підпис «Чим верстати» без жодної кнопки під ним; і вперше за всі сесії
  цього конвеєра **пройшла справжня збірка `vite build`** — на диску
  власника вона неможлива (`node_modules` з-під Windows, у `rollup` немає
  linux-бінарника), тож зроблена у свіжому клоні репозиторію в хмарі.

**Не зроблено і чому.**
- **Коміту немає** — за AGENTS.md коміт робиться лише на прохання власника.
  Робота лежить у робочій теці; перелік файлів — у розділі «Що не запушено».
- **`.git/index.lock` від обірваної сесії лишився на місці** — видалення
  файлів із мого боку заборонене. Це перше, що треба прибрати перед комітом.
- **Жоден зовнішній рушій не зібрав справжнього PDF.** Chromium, pandoc і
  Gamma перевірені лише з боку маршруту: що обрано правильний, що відмова
  приходить із причиною й кодом, що друк під KDP вони чесно не беруть. Чи
  виходить із них гарна книга — покаже тільки зібраний образ.
- **Живої взаємодії з вибором рушія не бачив ніхто.** Кнопки, підказки,
  теми Chromium і попередження «у вітрину публікується власна верстка»
  перевірені типами й збіркою, не оком.
- **i18n нових написів немає** — текст вибору рушія жорстко українською, як
  і в перенесених вкладках Etsy (#84). Це той самий борг, не новий.

---

## Сесія 03.09.2026 #01 — записи #83–#101

**Тема:** Etsy-аналітика: інтеграція набору, чесність чисел, реальні дані.

**Зроблено.**
- **#83** — сторінку «Аналітика ринку Etsy» розбито на вкладки; перенесено
  калькулятор комісій і SEO-переклади; заведено модуль ядра `etsyAdvisor` і
  маршрут `/api/market/advisor`, тож виклики моделі повернулись в облік
  `usage_log` (у наборі вони били в Gemini напряму, повз облік).
- **#84** — «Аудит лістинга» **написаний наново**: перенесений екран брав
  частоту запиту з `Math.random()`, конкуренцію виводив із довжини тега, а
  три з пʼяти складників бала були константами.
- **#85** — «Динаміка ніші» на збережених прогонах скринінгу замість
  вигаданих трендів.
- **#86** — розбір усіх пʼяти вкладок, 11 знайдених дефектів, план на 5
  етапів. *(черга)*
- **#87** — аналіз реальних даних Etsy; знайдено, що офіційний збирач уже
  написаний у модулі публікації; складено Етап 0. *(черга)*
- **#88** — Етап 0 виконано: адаптер `etsyScreen`, чесний прапорець джерела,
  `competition` із реального `count`, вибір джерела без тихого відкоту.
- **#89** — «Аналітику ринку Etsy» перенесено у плаваюче вікно поверх студії
  (перетягування, зміна розміру, розмитий фон) на наявному
  `DraggablePanel`.
- **#90** — F12 у редакторі: повноекранний режим замінено плаваючим вікном
  на тому ж `DraggablePanel`; заразом звірка ключів словника `editor.*`.
- **#91** — обкладинка картки у вітрині: перша сторінка PDF → PNG у браузері,
  кнопки у «Вітрині» й у панелі мосту. Закриває пункт, що висів із #82;
  конвеєр публікації тепер повний.
- **#92** — обкладинка стала умовою публікації (без неї у вітрині не
  зʼявляється нічого), плюс безкоштовний уривок на 5–10 сторінок; правки в
  приймачі — новий вид медіа `sample` і кнопка «Читати уривок» на сторінці
  товару.
- **#93** — посилання на уривок перенесено під обкладинку (за вказівкою
  власника), підпис приведено до дії «завантажити»; заразом виправлено
  групування уривка в кабінеті продавця й `MediaKind` у спільних типах.
- **#101** — **чотири рушії PDF на вибір письменника.** Перевірка джерел до
  роботи зняла один із двох запропонованих пакетів: `markdown-pdf` тягне
  PhantomJS, депрекейтнутий з 2017 і не збирається на Alpine. Модель
  «markdown → CSS вирішує вигляд» збережено через headless Chromium
  (рішення власника). Eisvogel беремо як є. **Рішення власника: pandoc і
  TeX Live — в один образ Nova**, з відомою ціною в кілька гігабайтів на
  КОЖЕН деплой. *(у роботі)*
- **#100** — **медіатека на сервері.** Опис у базі, байти в `DATA_DIR`,
  у книзі — коротке посилання замість мегабайтів base64. Заразом виправлено
  наслідок мого ж #94: дзеркалення книги тягло весь альбом на кожне
  збереження. **Перенесення наявних картинок з браузерів — НЕ зроблено:**
  впирається в подвійне списання довічного ліміту місця, рішення за
  власником (три варіанти в записі).
- **#99** — **Gamma під власною підпискою автора.** Виправляє хибу мого ж
  рішення: перша версія списувала кредити з рахунку власника за кожного
  автора. Ключ студії тепер не підміняє відсутній ключ автора.
- **#98** — Gamma API вивчено цілком; **виправлено мою помилку з #96**:
  окрема генерація зображень у API Є, тож арт обкладинки повернуто в план і
  реалізовано. Додано оцінку вартості за документованими ставками й вимикач
  зображень — головної статті витрат.
- **#97** — третя гілка публікації «Матеріали (Gamma)»: чотири види
  матеріалів, текст складається з книги, вартість показана до натискання.
  Свідомо НЕ названа третім майданчиком продажу — на Gamma не купують.
- **#96** — інтеграція Gamma: клієнт, сховище задач, облік кредитів,
  маршрути за ґейтом тарифу. Дорогою зʼясувалось, що **окремої генерації
  зображень у публічному API немає** — арт обкладинки лишається ручною дією.
- **#95** — Gamma: дві пробні генерації (обкладинка + курс-дек українською)
  і план інтеграції. Головне з проби — **лишилося ~137 кредитів**, тобто
  масової функції для всіх авторів із цього не вийде. *(черга)*
- **#94** — **книга тепер живе на сервері**: джерело з охороною ревізій,
  зверстані файли на диску, складання й публікація у вітрину без браузера.
  Закриває найсерйознішу відкладену проблему — рукопис існував лише в
  IndexedDB одного браузера.

**Не зроблено і чому.**
- **Живий прогін скринінгу через Etsy API — немає ключа.** Увесь шлях
  написаний і покритий тестами на підставній відповіді, але на справжніх
  даних не працював жодного разу. Заявку на `ETSY_API_KEY` подає власник; до
  того решта Етапу 0 лишається неперевіреною в бою.
- **Збагачення відгуками, рейтингом і назвою крамниці** (Етап 0, п. 3) —
  залежить від ключа: це додаткові виклики, які без ключа нічим перевірити.
- **Плановий збір прогонів** (Етап 0, п. 7) — не почато. Поки його немає,
  «Динаміка ніші» майже завжди показуватиме «збережено лише один прогін»:
  зрізи зʼявляються тільки коли автор сам натиснув скринінг.
- **Вкладка «Конкуренти» з набору — не робитиметься взагалі.** Etsy не
  публікує ані продажів, ані доходу чужого магазину; у наборі ці числа
  беруться з `Math.random()`. Чесного варіанту без окремого джерела не існує,
  тож це закрите рішення, а не борг (#86).
- **«Радар трендів» у вигляді з набору — відхилено** з тієї ж причини;
  замість нього зроблено «Динаміку ніші» на реальних прогонах (#85).
- **`vite build` не запускався.** `node_modules` встановлені під Windows, у
  `rollup` немає linux-бінарника. Перевірено `tsc` (0 помилок) і тестами;
  складання перевіряє власник у себе.
- **Пуш не робився** — за правилом AGENTS.md, лише за прямою вказівкою
  власника. Локально 7 комітів: `6a01a6c`, `13b93e7`, `d1e309e`, `280ddc0`,
  `2203c60`, `d293830`, `bb5f667`.
- **i18n трьох перенесених вкладок** — борг: текст інтерфейсу жорстко
  українською, у словник винесені лише назви вкладок (#84).

- **Живої взаємодії з вікнами не перевіряв** (#89, #90): перетягування,
  зміну розміру мишею й поведінку модалок редактора всередині вікна тут
  відтворити нічим — потрібне око власника.
- **Інтерфейсу для серверної публікації ще немає** (#94): маршрути готові й
  покриті тестами, але «Вітрина» досі публікує старим шляхом (книга в тілі
  запиту). Живого прогону нових маршрутів не було жодного.
- **Бік маркетплейсу не перевірявся взагалі** (#92): міграція застосовується
  на деплої, `prisma generate` локально заблокований, збірки Next тут немає.
  **Порядок увімкнення важливий:** спершу деплой API з міграцією, потім
  публікація з Nova — інакше приймач відхилить `sample` і покладе уривок за
  платну стіну.

**Тести після сесії:** `test-gamma` 56/0 (новий), `test-market` 120/0,
`test-etsy-screen` 50/0 (новий),
`test-listingAudit` 43/0 (новий), `test-pdf-cover` 19/0 (новий),
`test-pdfRenderer` 66/0, `test-pdfRoutes` 29/0, `test-coreAiRegistry` 52/0,
`tsc` — 0.

---

## Сесія 02.09.2026 #02 — записи #65–#82

**Тема:** King Market Intelligence і повний конвеєр публікації книги у
вітрину, включно з власним рушієм PDF.

**Зроблено.** Модуль аналітики Etsy за ТЗ v1.0 (#65, #66); `.gitattributes`
проти CRLF-шуму (#67); дві вади режиму JSON для Claude, знайдені живим
прогоном (#68); міст до вітрини — перевірка ключа, порожня адреса, причина
відмови, рівень slug (#69–#71, #73, #74); прогін конвеєра однією кнопкою
(#72); канал передачі файла книги (#75); **рушій верстки PDF** із вбудованим
шрифтом (#76) і два джерела макета — з книги та від моделі (#77); конвеєр
замкнено: макет → PDF → лістинг → файл (#78); **Amazon KDP** з дзеркальними
полями й корінцем за обсягом (#79); ручне зняття з вітрини (#80); тестова
книга зі справжнім вмістом і каналом для обкладинки (#82).

**Не зроблено і чому.**
- ~~**Клієнтський рендер обкладинки**~~ — **закрито в сесії 03.09.2026 #01**
  (запис #91): перша сторінка PDF → PNG у браузері, кнопки у «Вітрині» й у
  панелі мосту.
- **Попередження `pg` про `client.query()`** — діагностовано, не виправлено
  **свідомо**. Механізм знайдено, але місце виклику не встановлюється
  читанням, а `prisma generate` локально не запускається (заблокований
  `binaries.prisma.sh`). Замість здогадної правки поставлено
  `--trace-deprecation`: наступний деплой дасть стек (#72).
- **Комітів у репозиторії маркетплейсу немає.** Станом на 03.09.2026 там
  **46 файлів зі справжніми змінами** (перевірено `git diff
  --ignore-cr-at-eol`) — правки мосту, `prisma.service.ts`, `Dockerfile` і
  раніший набір Фази B5. Останній коміт гілки — `1fac6ce`.
- **`BRIDGE_SELLER_SLUG` на Railway** — має виставити власник у змінних
  середовища сервісу API.

---

## Сесія 02.09.2026 #01 — записи #61–#64

**Тема:** виправлення застарілих вад і хвиля 2 плану «18 навичок».

**Зроблено.** Відсутній `renderCoreTemplate` для трьох модулів `/diagn`
(#61); ґейт дозволів для `/api/ai/analyze-text-competences`, якого не було
(#62); показники глави й сила початку — задачі 6 і 8 плану «18 навичок»
(#63); модуль публікації KDP/Etsy проведено через журнальований процес
перевірки (#64).

**Не зроблено.** У записах цієї сесії окремо незакритих пунктів не названо.

---
