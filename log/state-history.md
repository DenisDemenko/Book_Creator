# Історія розділу «Що не запушено» (до 25.09.2026)

> Архів. Сюди перенесено всі попередні стани гілок із `log.md` (розділ «Що не
> запушено»), щоб покажчик, який кожна сесія читає першим, лишався
> компактним (межа — 120 КБ, `test:journal`). Нічого не видалено й не
> переписано: текст — як був, у тому самому порядку (від новішого до
> старішого). У старих станах згадується `log/119-151.md` — 25.09.2026 його
> розділено на `log/119-199.md` і `log/200-251.md` (записи ті самі).

### Попередній стан — #248–#250 (Т0.7, Т0.6, Т0.9) запушено власником 24–25.09.2026 (`637b6d2`), деплой пройшов (схема ядра v5)

#### (було) Стан на 24.09.2026 (сесія #03, пізніше) — 6 комітів #248–#250

**Book_Creality (Студія), гілка `master`, понад `origin/master` =
`production/master` = `95b21ec`:**

- `6807a13` — #248, код: фонова черга задач ядра (`server/core/jobs/*`,
  `migrations/0003_core_jobs.sql`, `server/core/index.ts`,
  `test:core-jobs`);
- `bf733dd` — #248, журнал: запис і покажчик, `task.md` (Т0.7 ✅);
- `00c6a90` — #249, код: синхронізація книги з ядром (`server/core/sync.ts`,
  `migrations/0004_core_sync.sql`, сховище ядра й черга, `bookRoutes.ts`,
  `server.ts`, `coreEntities.parseEntityTagsInSource`, `test:core-sync`,
  `live:core-sync`);
- `7198926` — #249, журнал: запис і покажчик, `task.md` (Т0.6 ✅);
- `72c5a52` — #250, код: три ролі AI (`server/core/ai/*`,
  `migrations/0005_core_ai_roles.sql`, `coreAiRegistry.ts`, `QuickAiModal.tsx`,
  `server.ts`, залежність `ajv`, `test:core-ai-roles`, `live:core-ai-roles`);
- наступний коміт — #250, журнал: запис і покажчик (250 записів), `task.md`
  (Т0.9 ✅) і цей розділ.

Пуш — пізніше, за словом власника («Пуш буде пізніше»).

**Рядок для пуша:** `git push origin master && git push production master`.

**Що перевірити після деплою.** У логах деплою Студії: `[core] міграцію
0003_core_jobs накочено`, `… 0004_core_sync накочено`, `… 0005_core_ai_roles накочено`, `[core] PostgreSQL
готовий, схема v5`. Після того як автор збереже книгу, у базі `pgvector`
(схема `fusion_core`) з'являються рядки `paragraphs` і задача `core_sync`
зі статусом `succeeded`.

### Попередній стан — #247 запушено власником 24.09.2026 (`95b21ec`); на Railway ядро піднялось: «міграцію 0001/0002 накочено», «PostgreSQL готовий, схема v2»

#### (було) Стан на 24.09.2026 (сесія #03) — 2 коміти #247 (Т0.3, Т0.4)

**Book_Creality (Студія), гілка `master`, понад `origin/master` =
`production/master` = `8614841`:**

- `354de8e` — #247, код: семантичне ядро в PostgreSQL (`server/core/*`:
  запускач міграцій, `0001_core_init.sql`, `0002_core_schema.sql`, договір
  `CoreRepository`, правила, сховища PostgreSQL і в пам'яті, запуск і стан у
  `/api/health`; `server.ts`, `Dockerfile` — міграції в `dist/core-migrations`,
  `.env.example`, залежність `pg`; `test:core-db`, `core:migrate`);
- наступний коміт — #247, журнал: запис і покажчик (247 записів), `task.md`
  (Т0.3, Т0.4 ✅), журнал сесій і цей розділ.

**Рядок для пуша:** `git push origin master && git push production master`.

**Railway (зроблено власником 24.09.2026):** сервіс `pgvector`
(`pgvector/pgvector:pg18`, том `pgvector-volume`); у сервісі Студії
`CORE_DATABASE_URL = ${{pgvector.DATABASE_URL_PRIVATE}}` — внутрішня
мережа, без плати за трафік.

**Що перевірити після деплою.** У логах деплою Студії: `[core] міграцію
0001_core_init накочено`, `… 0002_core_schema накочено`, `[core] PostgreSQL
готовий, схема v2`; `/api/health` → `"core":"ready"`. Тести йшли на
PostgreSQL 16 — на Railway 18; якщо буде `[core] ядро не запустилось: …`,
Студія працює далі, а рядок з логу — для розбору.

### Попередній стан — #246 (Т0.5) запушено власником 24.09.2026: `origin` і `production` → `8614841`

#### (було) Стан на 24.09.2026 (сесія #02) — 2 коміти #246 (Т0.5)

**Book_Creality (Студія), гілка `master`, понад `origin/master` =
`production/master` = `7adaf25`:**

- `4bbe1d0` — #246, код: постійні номери абзаців (`src/utils/paragraphIds.ts`,
  `ParagraphIdExtension.ts`, `extensions.ts`, `manuscriptDoc.ts`:
  `tiptapDocToMarkerBlocks`, `EditorView.tsx`, `types.ts`, `bookDiff.ts`,
  `server.ts` — поля в `section:patch`; `test:paragraph-ids`,
  `live:paragraph-ids`);
- наступний коміт — #246, журнал: запис і покажчик (246 записів), `task.md`
  (Т0.5 ✅, вимога унікальності в Т0.6), журнал сесій і цей розділ.

**Рядок для пуша:** `git push origin master && git push production master`
(у сесії Claude пуш не вдався: на пристрої немає облікових даних GitHub —
пушить власник).

**Що перевірити після деплою.** Відкрити книгу, додати абзац Enter-ом посеред
тексту, перезавантажити: у DOM блоки мають `data-pid`, номер першої половини
той самий, у другої — новий; текст рукопису без змін; старі книги
відкриваються як раніше.

## Стан на 24.09.2026 (сесія #01, пуш зроблено) — ✅ нічого не лишилось: #234–#245 у `origin` і `production` (`1c5a91d`)

**Book_Creality (Студія), гілка `master` = `origin/master` = `production/master`
= `1c5a91d` (14 комітів понад `c0da350`):**

- `541d38c` — #234, код: зовнішній API WriterScan (`server/external/*`,
  таблиці в `server/db.ts`, `server.ts`, `ScanInboxPanel.tsx`, контракт
  `ARCHITECTURE_EXTERNAL_API_WRITERSCAN.md`, `test:external-api`);
- `73cbbae` — #234, журнал;
- `36da7ff` — #235, код: плаваючий блок інструментів «Книга та текст»
  (`FloatingToolDock.tsx`, `toolDockLayout.ts`, `EditorView.tsx`,
  `DockedEditorPanel.tsx`, `index.css`, `editor.ts`, `test:tool-dock`,
  `live:tool-dock`);
- `079fa07` — #235, журнал;
- `c92f6fb` — #236, код: «Сховати сутності» ховає тег разом із дужками
  (`EntityTagPlugin.ts`, `removeFormattedEntityTags` у `coreEntities.ts`,
  `pdfRenderer.ts`, `test:entity-tag-decorations`);
- `2264f82` — #237, код: сутності ядра в 18 тренажерах
  (`data/masteryEntities.ts`, `ExerciseEntityPanel.tsx`, `SkillDetailModal.tsx`,
  `BookContextBanner.tsx`, `server/masteryEntityPrompt.ts`, `server.ts`,
  `test:mastery-entities`, `live:mastery-entities`);
- `4aa12bc` — журнал #236–#237;
- `a84862d` — #238, код: зв'язки `follows` і `overlaps` у реєстрі (39 типів;
  `coreEntities.ts`, `coreEntityStore.ts`, тести);
- `c5adb03` — плани: `PLAN_ENTITY_FEATURES.md`, `PLAN_ROADMAP.md`,
  `TAGS_ANALYSIS.md`, першоджерела `docs/tz/`;
- `83716c0` — журнал #238–#241, дорожня карта в `task.md`;
- `49080fa` — #242–#243, код: WebSocket за квитком (`server/realtimeAuth.ts`,
  `server.ts`, `useRealtimeSync.ts`), право `canUseAi` на всіх 23 маршрутах ШІ
  (`server.ts`, `server/auth.ts`), `test:realtime-auth`, `live:realtime-auth`;
- `855e531` — #242–#243, журнал: записи про безпеку, `task.md` (Т0.1, Т0.2) і
  цей розділ;
- `94214a3` — #244–#245, код: «чиє» (`@Ім'я`) і поля значення
  (`coreEntities.ts`: `parseEntityValue`, `entityValueTooltip`;
  `EntityTagPlugin.ts`, `EntitySlashMenu.tsx`, `sutnosty.md`,
  `test:tag-corpus`) і «Знайти й замінити», що розрізняє текст і теги
  (`searchScope.ts`, `coreEntities.ts`: `entityTagSpans`, `BookSearchModal.tsx`,
  `test:book-search`), `live:core-entities` +13;
- `1c5a91d` — #244–#245, журнал: записи й покажчик (245 записів), цей розділ.
  Обидва коміти — робота сесії Claude 24.09.2026 #01, закомічена окремо від
  чужого (AGENTS.md) з трейлером `Co-Authored-By: Claude`. Робочого дерева не
  чіпано: код закомічено як є.

**Пуш зроблено 24.09.2026** (сесія GitHub Copilot, на пряме прохання власника):
`origin` → `c0da350..1c5a91d`, `production` → те саме. Хук перед пушем пройшов:
245 записів, 14 комітів, кожне посилання на журнал розвʼязується.

**Railway перезбере Студію сам.** Таблиці WriterScan створюються при старті —
окремої міграції не треба. Що перевірити на проді:

*#234 WriterScan*
1. Медіатека → «Скани з телефону» → «Підключення застосунку»: адреса API з
   `/studio`; токен створюється, показується один раз, копіюється, відкликається.
2. Одне реальне фото сторінки (застосунок або curl з `Authorization: Bearer`)
   → `recognized`, текст схожий на сторінку. **Справжнього розпізнавання
   моделлю ще не бачили.**
3. Після `submit` скан у «Вхідних»; «Передати в главу» відкриває «Книга та
   текст» з AI-чернеткою; «разом із фото сторінки» додає ілюстрацію.
4. Витрата «Скан сторінки з телефону (WriterScan)» у «Тарифах ШІ».

*#235 Блок інструментів*
5. «Книга та текст»: над канвою — нічого, блок «Інструменти» прилип згори.
6. Перетягнути в середину, звузити до колонки, піднести до правої панелі —
   прилипне; згорнути; перезавантажити — стан той самий.
7. **Не бачено живим прогоном:** фокус-вікно (F12) — блок має бути поверх
   вікна; світла тема; ширина телефона.

*#236 «Сховати сутності»*
8. Пофарбувати чи виділити жирним частину тега (`[/character:Олена]`) →
   «Сховати сутності» — у тексті не лишається ні тега, ні дужок; те саме для
   `[/персонаж:…]`. Експорт DOCX/PDF такого розділу — тега немає (живцем не
   перевірено, лише тестами).

*#237 Сутності в тренажерах*
9. «Майстерність» → будь-яка навичка → «AI Тренажер»: у вправі панель
   «Сутності вправи», клік вставляє тег, лічильник росте; у «Персонажах» —
   2 вправи, у «Практичній цінності» — 4.
10. «Отримати розбір AI Coach» із реальною моделлю: чи з'являється блок
    «Розмітка сутностями» і чи в «виправленій версії» для книги теги лишились
    на місці. **Відповіді справжньої моделі ще не бачили.**
11. Пікер тексту книги → «Обрати абзаци» → «З сутностями тренажера (N)».

*#238 Реєстр зв'язків*
12. Після деплою `/api/core-entities` (або панель сутностей) показує 39 типів
    зв'язків, серед них `follows` і `overlaps`; таблиця перезасівається сама.

*#242–#243 Безпека* — **найважливіше після цього деплою**
13. Індикатор синхронізації в редакторі — «з'єднано» (квиток іде через
    `/studio`, сокет — напряму на хост Nova, `VITE_NOVA_WS_URL`); дві вкладки
    того самого автора синхронізуються. Якщо «не з'єднано» — дивитись
    `POST /api/realtime/ticket` у вкладці «Мережа».
14. Запрошений учасник (перекладач/дизайнер) бачить книгу власника і правки
    в реальному часі; читач — бачить, але не змінює.
15. Без входу функції ШІ відповідають «доступні зареєстрованим користувачам»;
    з входом — працюють як раніше.
16. Необов'язково: змінна `REALTIME_TICKET_SECRET` на Railway (без неї ключ
    квитків випадковий на кожен запуск — цього достатньо для одного процесу).

*#244–#245 Теги й пошук*
17. Слеш-меню (`/emotion:` → другий крок) показує «Формат: … · чиє — @Ім'я»;
    у тексті `[/emotion:страх — 7 · @Анна]` підказка чипа містить «Чиє: Анна» і
    поля. Живцем це бачила лише сесія-автор (`live:core-entities` 43/0) — інші
    прогін не повторювали.
18. «Знайти й замінити» у книзі з тегами: лічильник каже, скільки збігів у
    тегах; типово замінюється **лише текст**, перемикач «лише теги»
    перейменовує тільки теги (без нього «Замінити все» псувало
    `[/character:Олена]`).

**Перед комітами прогнано:** `tsc` 0, `vite build` OK, `esbuild server.ts` OK,
`test:external-api` 100/0, `test:tool-dock` 59/0, `live:tool-dock` 35/0
(у хмарній пісочниці, Chromium), `test:editor-toolbar` 59/0,
`test:manuscript-doc` 50/0, `test:slash-trigger` 56/0,
`test:describe-character` 115/0, `test:media-library` 59/0, `test:auth` 48/0,
`test:core-module-models` 45/0. Для #236–#237: `tsc` 0, `vite build` OK,
`test:entity-tag-decorations` 23/0, `test:mastery-entities` 25/0,
`live:mastery-entities` 22/0 і `live:core-entities` 42/0 (Chromium, хмара),
`test:core-entities` 80/0, `test:core-entity-export` 32/0, `test:pdf` 82/0,
`test:core-ai` 52/0. **Перед пушем 24.09.2026 (сесія GitHub Copilot, для
#244–#245) прогнано:** `tsc --noEmit` 0, `npm run build` OK,
`test:tag-corpus` 72/0 (новий), `test:core-entities` 81/0,
`test:core-entity-export` 32/0, `test:slash-trigger` 56/0,
`test:entity-tag-hiding` 19/0, `test:entity-tag-decorations` 23/0,
`test:book-search` 16/0. `test:journal` 75/1 — відомий борг «покажчик
компактний»: `log.md` ≈ 202 КБ при межі 120 КБ (не від цієї роботи).

⚠️ **У робочій теці лишається НЕ моє:** `.vscode/settings.json` і некомічені
`tmp/`, `Claude outputs/`, `desktop.ini`. Не чіпав.

## Стан на 23.09.2026 (сесія #01) — ✅ запушено: довідка (#232) і фікс приховування (#233)

**Book_Creality (Студія), гілка `master` = `origin/master` = `production/master`.
Поїхали коміти #232 і #233:**

- `f5b722c` — код, тести й кадри: приховування сутностей тепер ховає САМ ТЕГ
  (`EntityTagPlugin.ts`, `src/utils/entityTagHiding.ts`,
  `src/index.css`), сторінка довідки (`src/components/HelpGuideView.tsx`,
  `src/i18n/dictionaries/helpGuide.ts`), точки входу (`src/App.tsx` — футер,
  `src/components/SidebarNav.tsx` — кнопка внизу навігації), шість скринь у
  `public/help/` із номерними мітками, прогін `scripts/live-helpScreens.mts` і
  тест `scripts/test-entityTagHiding.mts`, оновлений `package.json`;
- `61eb8f2` — журнал: записи #232 і #233, рядки в покажчику, блок сесії
  23.09.2026 #01 у `log/sessions.md`, `task.md`.

**Railway перезібере Студію автоматично на пуш.** Що перевірити на проді
(окремо від блоку #230 нижче):

1. Унизу кожної сторінки студії — смуга з кнопкою «Довідка», а в самому низу
   лівої навігації — пункт «Довідка» (він є навіть тоді, коли футер за межами
   екрана).
2. Сторінка довідки відкривається, кадри видно (не сірі рамки-заглушки),
   кнопка «English version» перемикає мову всієї студії.
3. Кнопка «Сховати сутності» прибирає теги З КАНВИ повністю — лишається тільки
   текст книги; натискання ще раз повертає теги.

**Перед пушем прогнано:** `npx tsc --noEmit` 0, `npm run build` OK,
`live:core-entities` 42/0, `live:help-screens` 37/0, `test:entity-tag-hiding`
19/0, `test:core-entities` 80/0, `test:slash-trigger` 56/0,
`test:editor-toolbar` 59/0, `test:readability` 15/0, `test:manuscript-doc` 50/0,
`test:chat` 152/0, `test:pdf` 82/0.

⚠️ **Важливий порядок для кадрів довідки.** Якщо їх треба перегенерувати:
спершу `npm run build`, потім `npm run live:help-screens`. Прогін працює проти
`dist/`, а Vite копіює `public/` у `dist/` лише під час збірки — тому прогін
наприкінці сам копіює свіжі кадри в `dist/help/`. Кадр самої сторінки
(`help-page.png`) лягає в `tmp/`, а не в репозиторій: він важить понад 600 КБ і
застосунку не потрібен.

⚠️ **У робочій теці лишається НЕ моє:** `.vscode/settings.json` і некомічені
`tmp/`, `Claude outputs/`, `desktop.ini`. Не чіпав.

## Стан на 23.09.2026 (сесія #01, запис #230) — ✅ запушено, деплой за власником

**Book_Creality (Студія), гілка `master` = `origin/master` = `production/master`
= `edd0ed9`.** Поїхали два коміти:

- `6ada3fb` — код і тести: дизайн канви (колір тексту замість тла), діалоги
  героя за тегом `/character:Ім'я:діалог` (третій стан слеш-меню, нове поле
  `Character.dialogueTemplates`, модал «Налаштувати діалоги героя», блок
  діалогів у поповері, прибраний старий жест `/Ім'я`+Enter), кириличні ключі
  (`герой`/`персонаж`/`емоція`), підказки з реєстру, панель сутностей у чаті,
  знімання тегів у DOCX/EPUB/TXT, чотири оновлені набори перевірок;
- `edd0ed9` — журнал: запис #230, розділ стану, `task.md`.

**Railway перезібере Студію автоматично на пуш.** Що перевірити на проді:

1. У канві книги тег має бути ПОФАРБОВАНИМ ТЕКСТОМ, а тло абзацу — білим.
2. Експорт DOCX / EPUB / TXT книги з тегами не має містити `[/…:…]`.
3. У чаті справа має бути панель сутностей; клік по сутності вставляє тег у
   поле вводу.
4. `/character:Ім'я:діалог` (і `/герой:Ім'я:діалог`) має відкривати список
   готових діалогів героя; Tab вставляє `[/character:Ім'я]` + `[/dialogue:…]`
   і текст репліки; список налаштовується кнопкою «Налаштувати діалоги героя»
   у вкладці «Персонажі сцени».
5. **Ще не зроблено:** скіл тегування відповіді ШІ та його показ у «промтах
   для ШІ» — сервер поки ігнорує прапорець `entityTagging`, який уже надсилає
   клієнт (див. «Не зроблено» в записі #230).

**Перед пушем прогнано:** `test:core-entities` 80/0,
`test:core-entities-store` 32/0, `test:core-entity-export` 32/0,
`test:slash-trigger` 56/0, `test:chat` 152/0, `test:pdf` 82/0,
`test:manuscript-doc` 50/0, `test:editor-toolbar` 59/0, `test:book-text` 26/0,
`test:character-codex` 28/0, `test:character-mentions` 27/0,
`test:behavior-drift` 28/0, `npm run build` OK,
`npm run live:core-entities` 40/0.

⚠️ **У робочій теці лишається НЕ моє:** `.vscode/settings.json` (+56 рядків) і
некомічені `tmp/`, `Claude outputs/`, `desktop.ini`. Не чіпав: AGENTS.md
забороняє змішувати в один коміт свою роботу й чуже.

⚠️ **Окремо для наступної сесії.** Правки журналу з цієї роботи частково
поїхали чужими комітами (`4e3fe9a`, `e46f21c` — сесія, що дописувала #231,
узяла з робочої теки і мої рядки в `log/sessions.md` і `log/119-151.md`).
Історію не переписував: це вже опубліковані коміти, і авторство в них
правдиве — просто змішане.

## Стан на 23.09.2026 (після пушу #227) — ✅ нічого не лишилось

**Book_Creality (Студія).** Гілка `master` = `origin/master` =
`production/master`. Поїхали два коміти #227:

- `da3f245` — код і тести: реєстр 118 сутностей (`src/utils/coreEntities.ts`),
  таблиці `core_entities` / `core_entity_relations` / `chat_message_entities`
  (`server/db.ts`), сховище й HTTP (`server/coreEntityStore.ts`,
  `server/coreEntityRoutes.ts`), панель (`src/components/CoreEntityPanel.tsx`),
  плагін канви (`src/components/manuscriptEditor/EntityTagPlugin.ts`),
  слеш-підбір (`src/components/EntitySlashMenu.tsx`), чат
  (`src/components/EntityChatPicker.tsx`, `src/components/QuickAiModal.tsx`),
  знімання тегів у трьох шляхах PDF, `sutnosty.md` і чотири нові набори
  перевірок (три тести + живий прогін);
- `d123377` — журнал: запис #227, рядок у покажчику, підрозділ сесії
  22.09.2026 #01, оновлений `task.md`.

⚠️ **У робочій теці лишається НЕ моє й не закомічене:** `.vscode/settings.json`
(+56 рядків правил автопідтвердження команд) і тека `tmp/` (чернетки — вона в
цьому репозиторії не комітилася ніколи). Обидва не чіпав: AGENTS.md забороняє
змішувати в один коміт свою роботу й чуже.

**Що перевірити після деплою (Railway перезібере Студію на пуш):**
1. У логах сервісу має бути відсутність скарг від `[db] Не вдалося засіяти
   реєстр сутностей ядра` — інакше нові таблиці не наповнились.
2. `GET /api/core/entities` під сесією автора має віддати 118 сутностей,
   37 зв'язків і 12 груп.
3. У «Книзі та текст» має зʼявитись вкладка «Сутності»; тег, поставлений із
   неї, має стати чипом, а тло абзацу — пофарбуватись.
4. PDF-експорт книги з тегами не має містити жодного `[/…:…]`.

## Стан на 22.09.2026 (після пушу #228–#229) — ✅ моє не лишилось

**Book_Creality (Студія).** Гілка `master` = `origin/master` =
`production/master` = `062aeeb`. Поїхали два коміти:

- `48b5261` — код і тести #228:
  `src/components/adminOs/FurnitureProductEditor.tsx` (банер став трьохтоновим:
  залитий зелений блок для успіху, червоний для відмови, ціановий для підказки,
  `data-message-tone`) і `scripts/live-furnitureEditor.mts` (три нові перевірки
  кольору — колір читається пікселем, не класом);
- `062aeeb` — журнал: записи #228 і #229, покажчик, розділ стану.

**Fusion-Lab-Marketplaes (маркетплейс).** Гілка `master` = `origin/master` =
`6132421` — один коміт #229:

- `apps/web/src/components/product-buy-panel.tsx` — нормалізація `stock`
  варіанта (`undefined` більше не читається як «розпродано»).

⚠️ **Роботу сесії 22.09.2026 #01 (запис #227) того ж дня закомічено й запушено
з тієї ж робочої теки** — див. блок «Стан на 23.09.2026» вище. Доти вона
справді лежала тут незакоміченою, і це було позначено, щоб наступна сесія не
вважала її своєю забутою (AGENTS.md прямо забороняє змішувати своє й чуже).

### ⚠️ Що треба зробити в дашбордах (не в коді)

1. **Задеплоїти `apps/web` на Vercel.** Зараз там збірка, старша за #225:
   картка каталогу показує «4100.00 грн» без «від», а на сторінці товару немає
   вибору варіанта — хоч поточна збірка, запущена локально проти того самого
   прод-API, малює `<select data-testid="variant-select">` з трьома пунктами й
   міняє ціну на вибір (запис #229, скриншот `tmp/local-product-picker.png`).
   Тобто **варіанти власник не побачив не через публікацію і не через
   недороблений код**, а тому що веб не оновлений. У тому ж деплої поїде й
   виправлення зі `stock` (`6132421`) — без нього кожен варіант писав би
   «немає в наявності». Після деплою: «від N ₴» у
   картці, вибір варіанта на сторінці товару, артикул у кошику й замовленні.
2. ~~Задеплоїти API (Railway)~~ — **зроблено автоматично на пуш 22.09.2026.**
   Перевірено живим запитом: `GET /catalog/<slug>` віддає медіа в порядку
   01..14, а це неможливо без колонки `sortOrder` — тобто міграція
   застосувалась сама, і окремих дій з базою не потрібно.
3. **Після деплою веба — перевірити вітрину:** картка каталогу («від»),
   сторінка товару (вибір варіанта, ціна й кнопка реагують на вибір), кошик
   (артикул). Порядок фото перевірено вже: він правильний, бо його сортує API,
   а не веб — деплой Vercel для нього не потрібен.

**Що НЕ покрито перевіркою (і чому).** `test/bridge-products.e2e-spec.ts`
компілюється й лінтується, але локально не запускався — немає Postgres; його
перший справжній прогін буде в CI. Вигляд вітрини CI не перевіряє взагалі: і
вибір варіанта, і порядок фото перевірено живим рендером на прод-даних, але не
автоматичним тестом (у `apps/web` немає тестового каркаса). Банер у ДЕННІЙ темі
перевірено читанням коду, не очима — живий прогін працює в нічній. Перестановка
фото й далі коштує повторного завантаження всіх файлів і обнуляє
`downloadCount` — окремий маршрут «змінити лише порядок» свідомо відкладено.

## Стан на 22.09.2026 (після пушу #226) — ✅ нічого не лишилось

**Book_Creality (Студія).** Гілка `master` = `origin/master` =
`production/master` = `64c1d5b`. Запушено два коміти:

- `c5bd58e` — код і тести #226: перетягування фото у редакторі виробу, вибір
  головного банера, передача порядку мостом. Файли:
  `src/components/adminOs/furnitureProduct.ts`,
  `src/components/adminOs/FurnitureProductEditor.tsx`,
  `server/marketplaceBridge.ts`, `server/furnitureProductRoutes.ts`,
  `scripts/test-furnitureMedia.mts`, `scripts/live-furnitureEditor.mts`.
- `64c1d5b` — журнал: запис #226, покажчик, цей розділ.

**Fusion-Lab-Marketplaes (маркетплейс).** Гілка `master` = `origin/master` =
`a370b87` — один коміт, усе, що стосується порядку фото:

- міграція `apps/api/prisma/migrations/20260922000001_media_sort_order/`;
- `apps/api/prisma/schema.prisma` (`MediaAsset.sortOrder`),
  `src/bridge/bridge.controller.ts`, `src/bridge/bridge.service.ts`,
  `src/catalog/listing.mapper.ts`;
- новий `apps/api/test/bridge-products.e2e-spec.ts` — перше e2e-покриття
  продуктової половини моста (варіанти + порядок медіа).

### ⚠️ Що треба зробити в дашбордах (не в коді)

1. **Задеплоїти `apps/web` на Vercel.** Зараз там збірка, старша за #225:
   картка каталогу показує «4100.00 грн» без «від», хоч API віддає
   `priceFrom: true` — тобто **варіанти власник не побачив не через публікацію**,
   а тому що веб не оновлений. Це перевірено живим Chrome 22.09.2026: у каталозі
   ціна без «від», на сторінці товару немає жодного вибору варіанта — при тому,
   що `product-buy-panel.tsx` малює вибір без умови на користувача, тож
   «не залогований» цього не пояснює. Після деплою: «від N ₴» у картці,
   випадаючий вибір варіанта на сторінці товару, артикул у кошику й замовленні.
2. ~~Задеплоїти API (Railway)~~ — **зроблено автоматично на пуш 22.09.2026.**
   Перевірено живим запитом: `GET /catalog/<slug>` віддає медіа в порядку
   01..14, а це неможливо без колонки `sortOrder` — тобто міграція
   застосувалась сама, і окремих дій з базою не потрібно.
3. **Після деплою веба — перевірити вітрину:** картка каталогу («від»),
   сторінка товару (вибір варіанта, ціна й кнопка реагують на вибір), кошик
   (артикул). Порядок фото перевірено вже: він правильний, бо його сортує API,
   а не веб — деплой Vercel для нього не потрібен.

**Що НЕ покрито перевіркою (і чому).** `test/bridge-products.e2e-spec.ts`
компілюється й лінтується, але локально не запускався — немає Postgres; його
перший справжній прогін буде в CI після пушу маркетплейсу. Вигляд вітрини
(випадаючий вибір, перетягування у браузері) CI не перевіряє взагалі.
Перестановка фото й далі коштує повторного завантаження всіх файлів і обнуляє
`downloadCount` — окремий маршрут «змінити лише порядок» свідомо відкладено.

## Стан гілок на 21.09.2026 (після пушу #225) — ✅ нічого не лишилось

**Book_Creality (Студія).** Гілка `master` = `origin/master` =
`production/master` = `9cb37cc`. Поїхали пʼять комітів: `2865a61` (код і тести),
`8b67a15` (запис #225, покажчик, «Що не запушено»), `80ad4ce` (стан після пушу)
і два уточнення — `8c8af04` (у CI маркетплейсу e2e не ганяється) та `9cb37cc`
(крок e2e додано; див. нижче). `journal-check` на кожному пуші пройшов —
225 записів, усі посилання на журнал розвʼязуються. Railway (сервіс
`Book_Creator`, гілка `master`) деплоїть автоматично → `app.fusionlab.in.ua/studio/`.

**Fusion-Lab-Marketplaes (маркетплейс).** Гілка `master` = `origin/master` =
`b578ebc`. Чотири коміти: `82ec818` (схема, міграція, API кошика й
замовлення, вітрина, кабінет продавця, i18n, e2e-тест варіантів), `61647e5`
(README + ADR 0008), `ea66d86` (крок e2e у CI) і `b578ebc` (заглушка Firebase
для тестів) — про два останні нижче.

### ⚠️ Прод: міграція йде САМА при деплої — але її невдача не зупиняє сервер

`apps/api/Dockerfile` виконує `prisma migrate deploy` у `CMD` перед запуском
сервера, тож міграція застосовується разом із деплоєм — руками нічого робити
не треба. **У попередній редакції цього розділу стояло «перед деплоєм коду API
виконати `npm run db:migrate` — саме міграцію ПЕРШОЮ». Це неправда про цей
проєкт, і вона виправлена 21.09.2026** — саме тому розділ переписано, а не
дописано: неправдива інструкція дорожча за відсутню.

**Пастка поруч:** команда обгорнута в `|| echo '!!! MIGRATE DEPLOY FAILED — …'`,
тобто невдала міграція НЕ валить запуск. Сенс цього свідомий (крамниця має
працювати, а відставання схеми ламає зазвичай одну можливість, не всі — так
було з `MediaKind.sample`), але наслідок такий: «сервер піднявся» НЕ означає
«схема догнала код». Тому після деплою треба читати рядок у Railway-логах, а
не припускати.

**Стан на 21.09.2026:** API в проде ВЖЕ з новим кодом — публічний
`GET /catalog?kind=product` віддає `priceFrom`, а це поле зʼявилося лише в
`82ec818`. Отже Railway задеплоїв на пуш, і `prisma migrate deploy` виконалася
на тому ж старті контейнера. Оскільки невдача міграції сервер не зупиняє,
факт старту доводить, що команда ЗАПУСКАЛАСЯ, а не що вона вдалася. Що
лишилось перевірити очима: рядок `MIGRATE DEPLOY FAILED` у логах деплою —
або, швидше, відкрити кошик залогіненим: якби колонки не було, `GET /cart`
падав би 500.

**Вітрина теж живе** (`www.fusionlab.in.ua` — Next.js на Vercel, відповідає; API
`/health` — `ok`, 4 користувачі). Але жоден із 4 опублікованих товарів варіантів
не має (перевірено `attributes` органайзера: ключа `variants` там немає), тож
поки у Студії не опублікують товар із варіантами, на вітрині нічого з нового не
зʼявиться — не тому, що код не задеплоєний, а тому що немає даних.


### ✅ E2E у CI: додано, перший прогін червоний, другий зелений

Доти в CI набору e2e не було взагалі: `npm test` у корені — це
`turbo run test` → `apps/api` `jest` із `testRegex: .*\.spec\.ts$` і
`rootDir: src`, тобто лише ~10 юніт-тестів, а повний набір жив в окремому
скрипті (`npm run test:e2e`, `test/jest-e2e.json`) і не викликався. Зелена
збірка нічого не казала про шлях грошей: кошик, оформлення, LiqPay, права
доступу, завантаження.

Крок доданий комітом `ea66d86` (`npm run test:e2e`,
`working-directory: apps/api`; окремого налаштування не треба, бо
`test/global-setup.ts` сам створює `fusionlab_test` і мігрує його). Він
зʼявився на пряму вказівку власника — після того, як я записав це як
пропозицію.

**Перший прогін того самого коміта впав: 15 наборів із 15, 120 тестів із 120.**
Причина виявилася не в 120 місцях, а в одному: повідомлення
`TypeError: Cannot read properties of undefined (reading 'close')` в `afterAll`
було каскадом — `beforeAll` не доїхав, тож `ctx` лишився `undefined`, і впало
вже прибирання. Справжня причина — `createTestApp()` падає на ініціалізації
модуля: Firebase Admin відмовляється стартувати без service account
(`src/auth/firebase-admin.provider.ts`), а в CI його немає (на машині
розробника він у `.env`, тому локально цього не видно). Виправлено комітом
`b578ebc`: `apps/api/test/env.ts` тепер генерує одноразовий RSA-ключ на кожен
прогін і віддає його як `FIREBASE_PRIVATE_KEY_B64`. Це безпечно саме тому, що
набір ніколи не ходить у Google — `utils/test-app.ts` підмінює `TokenVerifier`,
єдине місце, яке справді перевіряє ID-токен. Ключ навмисно НЕ комітиться:
приватний ключ у репозиторії — це приватний ключ, який згодом виявиться
справжнім.

**Другий прогін — ✅ зелений.** Що цим доведено вперше:
- міграція застосовується на чистій базі (усі 14 міграцій, включно з новою);
- **кошик і замовлення з варіантом справді працюють**: обидва варіанти одного
  товару лежать у кошику двома рядками, дорожчий споживається за власною ціною,
  невідомий артикул відмовляє, замовлення зберігає знімок артикула, а залишок
  зменшується саме у ВАРІАНТА й не чіпає залишок товару;
- резервування залишку через `SELECT … FOR UPDATE` виконалося на справжньому
  Postgres, а не лише прочитане очима (до того це була найризикованіша
  неперевірена ділянка цієї правки);
- решта 14 наборів (права, промокоди, виплати, реферали, розклад, команди,
  чат, відгуки) теж проходять — тобто вони не «зламані здавна», як можна було
  припустити за червоним першим прогоном, а просто ніколи не запускалися в CI.

Чого це НЕ доводить: нічого про **прод** (там міграції ще немає, і кошик у
продазі варіантів не бачив) і нічого про **вигляд вітрини** — випадаючий вибір
варіанта в браузері CI не перевіряє, бо там немає ні сторінки Next.js, ні
покупця з мишкою. Це ручна перевірка після деплою.

**Прод:** окремих дій не потрібно — `apps/api/Dockerfile` виконує
`prisma migrate deploy` на старті контейнера (див. вище). Студія й маркетплейс
сумісні в обидва боки: міст не шле `variants`, коли їх немає, а маркетплейс не
чіпає наявний список, якщо поля немає в payload-і. Тобто порядок деплою між
репозиторіями не критичний.

**Що перевірити після деплою** — у блоці «Стан на 21.09.2026 (після запису
#225)» нижче; там-таки докладний перелік того, чого перевірка НЕ покрила
(локально набір e2e досі не запускався жодного разу — тільки в CI, і саме тому
вигляд вітрини лишається ручною перевіркою).

## Стан на 21.09.2026 (після запису #225) — не запушено

> → **Виправлено 21.09.2026, того ж дня:** усе закомічено й запушено в обидва
> remote Студії та в маркетплейс (див. розділ вище). Цей блок лишається як
> історія стану на момент запису, а не як борг.

**Book_Creality (Студія).** Гілка `master`, зміни в робочій теці
**незакомічені** (код і тести — першим комітом, документація — другим):

- **Код і тести** (запис #225): варіанти товару — додаткові артикули зі своїми
  опціями (комплектація/колір/розмір), власною ціною й залишком. Файли:
  `src/components/adminOs/furnitureProduct.ts`,
  `src/components/adminOs/FurnitureProductEditor.tsx`,
  `server/marketplaceBridge.ts`, `server/furnitureProductRoutes.ts`,
  `scripts/test-furnitureProduct.mts`, `scripts/test-furniturePublish.mts`,
  `scripts/live-furnitureEditor.mts`.
- **Документація**: запис #225 у `log/119-151.md`, покажчик і цей розділ у
  `log.md`, журнал сесій у `log/sessions.md`.
- **Разом із цим комітом поїде правка середовища**: у
  `scripts/test-furniturePublish.mts` кількість фото більше не зашита числом
  (у теці власника їх стало 14, і набір падав 31/6 без жодного звʼязку з
  кодом).

**Fusion-Lab-Marketplaes (маркетплейс).** ОКРЕМИЙ репозиторій, гілка `master`,
зміни **незакомічені й не запушені**. Найважливіше — **там лежить міграція
БД, яка ще ніде не застосована**:

- `apps/api/prisma/migrations/20260922000000_product_variants/migration.sql` —
  `CartItem.variantSku` + новий унікальний індекс, `OrderItem.variantSku` і
  `variantLabel`.
- API: `prisma/schema.prisma`, `src/catalog/product-variants.ts` (новий),
  `src/catalog/product-variants.dto.ts` (новий), `src/catalog/listing.mapper.ts`,
  `src/bridge/bridge.dto.ts`, `src/bridge/bridge.service.ts`,
  `src/cart/{cart.controller,cart.dto,cart.service}.ts`,
  `src/orders/orders.service.ts`, `src/sellers/{seller.dto,sellers.service}.ts`,
  `test/purchase-download.e2e-spec.ts` (новий тест варіантів).
- Веб: `src/components/product-buy-panel.tsx` (новий),
  `src/components/{add-to-cart,listing-card,listing-form,sticky-buy-bar}.tsx`,
  `src/lib/cart-context.tsx`,
  `src/app/[locale]/{cart/page.tsx,catalog/[slug]/page.tsx,seller/orders/page.tsx,account/orders/[number]/page.tsx}`,
  `messages/{uk,en}.json`.
- `packages/shared-types/src/index.ts` — типи варіантів, `priceFrom`,
  `variantLabel`.

### Порядок деплою (важливо)

1. **Спершу міграція в маркетплейсі:** `npm run db:migrate` (`prisma migrate
   deploy`) у сервісі `@fusion-lab/web`/API (Railway, проєкт
   `adventurous-tranquility`). Без нової колонки **кошик перестане
   відкриватись у всіх** (`column variantSku does not exist`) — міграція й код
   API мають поїхати разом, міграція першою.
2. Потім деплой Студії (Railway, `Book_Creator`, гілка `master` → авто).
3. Студія й маркетплейс сумісні в обидва боки: міст просто не шле `variants`,
   коли їх немає, а маркетплейс не чіпає наявний список варіантів, якщо поля
   немає в payload-і. Тому порядок цих двох кроків між репозиторіями не
   критичний — критична лише міграція перед кодом API.

### Що перевірити після деплою

- Студія → «Управління товарами» → картка виробу: секція «Варіанти та
  додаткові артикули»; два варіанти з різними цінами → підказка «від»;
  два варіанти з ОДНАКОВИМ артикулом → публікація відмовляє локально.
- Вітрина: картка товару в каталозі показує «від N ₴»; на сторінці товару — вибір
  варіанта; ціна й кнопка купівлі змінюються разом із вибором; вибраний
  варіант видно в кошику; у замовленні збережено артикул; у кабінеті продавця
  в списку продажів видно, який артикул пакувати.
- **Міграція:** `/cart` відкривається, старі позиції кошика (без варіанта)
  працюють як раніше — вони отримали `variantSku = ''`.

## Стан гілок на 21.09.2026 (після пушу #224) — ✅ нічого не лишилось

**Book_Creality (Студія).** Гілка `master` = `origin/master` =
`production/master` = `25e42a7`. Поїхали обидва коміти: `46f801f` (код і тести)
і `25e42a7` (документація). `journal-check` на пуші пройшов — 224 записи, усі
посилання на журнал розвʼязуються. Railway (сервіс `Book_Creator`, гілка
`master`) деплоїть автоматично → `app.fusionlab.in.ua/studio/`.

**Fusion-Lab-Marketplaes.** Без змін — уся робота в Студії.

**Що перевірити після деплою** — у блоці «Стан на 21.09.2026 (після запису
#224)» нижче: він лишається чинним (вибір глави, підтвердження, перехід у
«Книга та текст», кнопки ✓/⊘ на чернетці, відсутність літеральних `<p>`).

## Стан на 21.09.2026 (після запису #224) — не запушено

**Book_Creality (Студія).** Гілка `master`, зміни в робочій теці
**незакомічені** (код і тести одним комітом, документація — другим):

- **Код і тести** (запис #224): передача опису з медіатеки тепер має вибір
  глави, підтвердження, перехід у «Книга та текст» і вставку блоком
  AI-чернетки з видимими «прийняти»/«відхилити». Файли:
  `src/utils/describeCharacterTransfer.ts`, `src/utils/manuscriptDoc.ts`,
  `src/components/DescribeCharacterModal.tsx`,
  `src/components/MediaLibraryView.tsx`,
  `src/components/manuscriptEditor/AiDraftBlockNode.tsx`,
  `src/components/EditorView.tsx`, `src/App.tsx`, `src/index.css`,
  `src/i18n/dictionaries/describeCharacter.ts`,
  `scripts/test-describeCharacter.mts`, `scripts/live-mediaDescribe.mts`.
- **Документація**: цей блок, запис #224 у `log/119-151.md`, покажчик і
  журнал сесій.

**Fusion-Lab-Marketplaes.** Ця сесія маркетплейса не торкалась — змін немає.

**Що перевірити після деплою** (Студія → «Медіатека» → фото → «Описати ШІ»):
- у вікні опису є вибір глави; натискання «Передати текст» спершу питає
  підтвердження з назвою глави, і лише потім передає;
- після підтвердження відкривається «Книга та текст» на вибраній главі, текст
  стоїть у її кінці бурштиновим блоком «✨ AI-чернетка»;
- на мітці чернетки видно ДВА значки: ✓ (прийняти) і ⊘ (відхилити, Ctrl+Z
  повертає). Після прийняття текст стає звичайним;
- вставлений текст виділено й прокручено до нього;
- у книзі немає літеральних `<p>` — це був дефект #220, виправлений тут.

**Борг #224:** співвідношення сторін фото в книзі типове (16:9) — `не почато`,
потребує розмірів файлу з медіатеки; глава «Опис персонажів» для книги без
глав перевірена лише юнітом; `findBlockRangeWithText` покрита живим прогоном,
бо в Node-тестах немає DOM; повний `npm test` не гнали.

## Стан гілок на 21.09.2026 (після пушу #223) — ✅ нічого не лишилось

**Book_Creality (Студія).** Гілка `master` = `origin/master` =
`production/master` = `6a32fb3`. Поїхали обидва коміти: `815150d` (код і тести)
і `6a32fb3` (документація). `journal-check` на пуші пройшов — 223 записи, усі
посилання на журнал розвʼязуються. Railway (сервіс `Book_Creator`, гілка
`master`) деплоїть автоматично → `app.fusionlab.in.ua/studio/`.

**Fusion-Lab-Marketplaes.** Без змін — уся робота в Студії.

**Що перевірити після деплою** — у блоці «Стан на 21.09.2026 (після запису
#223)» нижче: він лишається чинним (список моделей, відсутність текстових
моделей у вікні опису фото, привʼязка модуля «Текст за фото», живий виклик
Llama 4 Scout у Groq і тариф Scout).

## Стан на 21.09.2026 (після запису #223) — не запушено

**Book_Creality (Студія).** Гілка `master`, зміни в робочій теці
**незакомічені** (два блоки: код і тести — потім документація, як домовлено):

- **Код і тести** (запис #223): зір став властивістю моделі, а не рушія —
  `vision` у реєстрі `CHAT_MODELS`, `modelSupportsVision()`, `normalizeModelId()`
  з `LEGACY_MODEL_ALIASES`, переклад рушійних воріт на модельні в чотирьох
  маршрутах, нова перевірка на адмінській привʼязці модуля, тарифи по моделях,
  клієнтські списки фільтруються прапорцем із сервера. Файли:
  `server/chatProviders.ts`, `server/chatRoutes.ts`, `server.ts`,
  `server/coreAiRegistry.ts`, `server/pricing.ts`, `server/adminRoutes.ts`,
  `src/components/DescribeCharacterModal.tsx`, `src/components/EditorView.tsx`,
  `src/components/QuickAiModal.tsx`, `src/components/AiPricingAnalyticsView.tsx`,
  `src/i18n/dictionaries/editor.ts`, `scripts/test-chatProviders.mts`,
  `scripts/test-chatSessions.mts`, `scripts/live-mediaDescribe.mts`,
  `scripts/test-apiKeys.mts` (коментар).
- **Документація**: цей блок, запис #223 у `log/119-151.md`, покажчик і
  журнал сесій.

**Fusion-Lab-Marketplaes.** Ця сесія маркетплейсу не торкалась — змін немає.

**Що перевірити після деплою** (Студія → «Ядро AI» / «Медіатека» / «Книга та
текст»):
- у списках моделей зʼявилися **DeepSeek V4.1 Flash, DeepSeek V4 Pro,
  Llama 4 Scout (Groq), Mistral Large**; `deepseek-chat` зі списку зник (книги з
  ним працюють — id перекладається на `deepseek-flash`);
- у вікні «Описати ШІ» видно лише моделі із зором: DeepSeek V4 Pro і
  Llama 3.3 70B там відсутні;
- у «Ядрі AI» для модуля «Текст за фото» моделі без зору підписані
  «(не бачить фото)» і не вибираються — спробувати зберегти таку привʼязку
  вручну теж не вийде (сервер відповість 400);
- **найризикованіше місце**: живий виклик `meta-llama/llama-4-scout-17b-16e-instruct`
  через Groq. Якщо Groq знає модель під іншим імʼям — буде 404 на саму модель;
  правити один рядок у `CHAT_MODELS`. Ключа Groq у розробці не було, звірити
  було нічим;
- тариф Groq для Scout у «Тарифах та аналітиці ШІ» показується як «—» — це
  чесно «не звірено», впишіть число після перевірки на `groq.com/pricing`.

**Борг #223:** живого виклику DeepSeek/Groq/Mistral немає (немає ключів —
«чекає на зовнішнє»); ідентифікатор Scout у Groq не підтверджений; тариф Scout
не звірено; повний `npm test` не гнали (давні Windows-провали на `/bin/sh`).

## Стан гілок на 21.09.2026 (після пушу #222) — ✅ нічого не лишилось

**Book_Creality (Студія).** Гілка `master` = `origin/master` =
`production/master` = `6c3bd50`. Поїхали обидва коміти: `a4b7504` (код і
тести) і `6c3bd50` (документація). `journal-check` на пуші пройшов — 222
записи, усі посилання на журнал розвʼязуються. Railway (сервіс
`Book_Creator`, гілка `master`) деплоїть автоматично →
`app.fusionlab.in.ua/studio/`.

**Fusion-Lab-Marketplaes.** Без змін — уся робота в Студії.

**Що перевірити після деплою** — у блоці нижче, він лишається чинним.

## Стан гілок на 21.09.2026 (після пушу #221) — ✅ нічого не лишилось

**Book_Creality (Студія).** Гілка `master` = `origin/master` =
`production/master` = `7303f7b`. Поїхали обидва коміти: `1a83338` (код і
тести) і `7303f7b` (документація). `journal-check` на пуші пройшов — 221
запис, усі посилання на журнал розвʼязуються. Railway (сервіс
`Book_Creator`, гілка `master`) деплоїть автоматично →
`app.fusionlab.in.ua/studio/`. Після пушу додався третій коміт — стан гілок
`edc869b` (він теж на обох remote).

**Fusion-Lab-Marketplaes.** Без змін — уся робота в Студії.

**Що перевірити після деплою** — у блоці нижче, він лишається чинним.

## Стан на 21.09.2026 (після запису #222) — не запушено

**Book_Creality (Студія).** Гілка `master`, один коміт попереду
`origin`/`production` плюс документація до нього (цей блок, запис #222,
журнал сесій, покажчик):

- `a4b7504 feat(media)`: «Описати ШІ» в медіатеці виконує обрану модель ядра
  AI — випадаючий список у вікні опису, типово та сама модель, що в «Книга та
  текст»; моделі без зору відхиляються з поясненням, старий контракт (лише
  `engine`) збережено. Деталі — запис #222.

**Fusion-Lab-Marketplaes.** Ця сесія маркетплейсу не торкалась — змін немає.

**Що перевірити після деплою** (Студія → «Медіатека» → фото → «Описати ШІ»):
- у вікні є список «Модель:»; перший пункт — «Автоматично (налаштування
  адміністратора)», далі — Gemini/GPT/Claude, а там, де ключа немає, стоїть
  «— немає ключа» (такий пункт не вибирається);
- якщо в «Книга та текст» обрано модель, вона стоїть типово й підписана
  «— обрана у «Книга та текст»»;
- опис іде саме обраною моделлю (це видно у витратах AI-асистента);
- DeepSeek / Llama / Mistral у списку ВІДСУТНІ — вони не бачать зображень;
- підказка під списком каже, що вибір тут не змінює налаштування книги.

**Борг #222:** немає, крім повного `npm test` (не гнали). Свідомо поза
межами: друге вікно «AI текст за фото» (вкладка «Ілюстрації») і далі
Gemini/GPT; завантаження відео з диска не робилося (окрема задача).

## Стан на 21.09.2026 (після запису #221) — не запушено

**Book_Creality (Студія).** Гілка `master`, один коміт попереду
`origin`/`production` плюс документація до нього (цей запис, журнал сесій,
покажчик):

- `1a83338 fix(ai)`: «модель перевантажена» (`503 UNAVAILABLE`) більше не
  показується авторові сирим JSON — новий вид відмови `busy`, людське
  пояснення українською, підказка перемкнути рушій, повтор до трьох запитів
  (700 і 1800 мс) для обох рушіїв, статус 503 у трьох маршрутах читання
  тексту із зображення й кнопка «Спробувати ще раз». Деталі — запис #221.

**Fusion-Lab-Marketplaes.** Ця сесія маркетплейсу не торкалась — змін немає.

**Що перевірити після деплою** (Студія, вкладка «Медіатека»):
- відкрити «Описати ШІ» на фото саме тоді, коли Gemini перевантажений
  (буває рідко): автор має побачити українське речення про тимчасову
  відмову, підказку перемкнути рушій на GPT і кнопку «Спробувати ще раз» —
  і жодного JSON;
- якщо перевантаження коротке, опис має зʼявитися сам, без участі автора
  (це і є новий повтор);
- перевірити, що звичайний опис не змінився: без перевантаження поведінка
  та сама, зайвих запитів до моделі немає.

**Борг #221:** немає, крім повного `npm test` (не гнали). Свідомо поза межами:
справжнього 503 від Google у тестах немає (немає ключа), `quota` (429) і далі
остаточна відмова — повторюється лише `busy`.

## Стан гілок на 19.09.2026 (після пушу #218) — ✅ нічого не лишилось

**Book_Creality (Студія).** Гілка `master` = `origin/master` =
`production/master` = `de2bda4`. Поїхали обидва коміти: `a9eabcc` (код і
тести) і `de2bda4` (документація). `journal-check` на пуші пройшов — 218
записів, усі посилання на журнал розвʼязуються. Railway (сервіс
`Book_Creator`, гілка `master`) деплоїть автоматично →
`app.fusionlab.in.ua/studio/`.

**Fusion-Lab-Marketplaes.** Без змін — ця робота повністю в Студії.

**Що перевірити після деплою** — у блоці нижче, він лишається чинним.

## Стан гілок на 19.09.2026 (після пушу #220) — ✅ нічого не лишилось

**Book_Creality (Студія).** Гілка `master` = `origin/master` =
`production/master` = `7cd143e`. Поїхали обидва коміти: `5c6c9c5` (код,
промпт, тести) і `7cd143e` (документація). `journal-check` на пуші пройшов —
220 записів, усі посилання на журнал розвʼязуються. Railway (сервіс
`Book_Creator`, гілка `master`) деплоїть автоматично →
`app.fusionlab.in.ua/studio/`.

**Fusion-Lab-Marketplaes.** Без змін — уся робота в Студії.

**Що перевірити після деплою** — у блоці нижче, він лишається чинним.

## Стан гілок на 19.09.2026 (після запису #220) — не запушено

**Book_Creality (Студія).** Гілка `master`, один коміт попереду
`origin`/`production` плюс документація до нього (цей запис, журнал сесій,
покажчик):

- `5c6c9c5 feat(media)`: дві дії на кожному фото — «Генерувати відео»
  (стартовий кадр) і «Описати ШІ» (опис персонажа за фото + передача тексту,
  за бажанням разом із фото, у книгу, інструкцію або курс). Новий маршрут
  `/api/ai/describe-character-from-image`, промпт
  `server/characterFromImagePrompt.ts`, три чисті передачі
  (`describeCharacterTransfer.ts`), словник `describeCharacter`, тести
  `test:describe-character` і `live:media-describe`. Деталі — запис #220.

**Fusion-Lab-Marketplaes.** Ця сесія маркетплейсу не торкалась — змін немає.

**Що перевірити після деплою** (Студія, вкладка «Медіатека»):
- на картці ФОТО в рядку «ЕКСПОРТ:» дві нові кнопки; на картці відео їх
  немає — це правильно;
- «Генерувати відео» перемикає панель ліворуч у режим «відео» й показує саме
  це фото у полі стартового кадру;
- «Описати ШІ» відкриває вікно: для справжнього опису потрібен ключ
  (`GEMINI_API_KEY` або `OPENAI_API_KEY`) — без ключа буде чесне повідомлення
  українською, і це очікувано;
- текст у полі редагований, його видно з лічильником слів; кнопки передачі —
  «Передати текст» і «Передати текст разом з фото»;
- передача в книгу дає новий розділ (дивіться «Зміст» книги), в курсі —
  нову чернетку з одним уроком, в інструкції — запис у «Базі знань» чернетки.

**Борг #220:** немає, крім повного `npm test` (не гнали). Свідомо поза межами:
живого виклику ШІ зі справжнім ключем немає (якість самого опису перевіряє
власник своїм ключем), промпт живе в коді й поки не редагується в «Ядрі AI»,
передача в інструкцію потребує наявної чернетки, курс створюється новим.

## Стан гілок на 19.09.2026 (після пушу #219) — ✅ нічого не лишилось

**Book_Creality (Студія).** Гілка `master` = `origin/master` =
`production/master` = `e89ab2d`. Поїхали обидва коміти: `f310f6c` (код і
живий прогін видалення) і `e89ab2d` (документація). `journal-check` на пуші
пройшов — 219 записів, усі посилання на журнал розвʼязуються. Railway
(сервіс `Book_Creator`, гілка `master`) деплоїть автоматично →
`app.fusionlab.in.ua/studio/`.

**Fusion-Lab-Marketplaes.** Без змін — ця робота повністю в Студії.

**Що перевірити після деплою** — у блоці нижче, він лишається чинним.

## Стан гілок на 19.09.2026 (після запису #219) — не запушено

**Book_Creality (Студія).** Гілка `master`, один коміт попереду
`origin`/`production` плюс документація до нього (цей запис, журнал сесій,
покажчик):

- `f310f6c feat(media)`: видалення фото й відео з підтвердженням —
  попередження про незворотність. Кошик більше не видаляє відразу, є і в
  лайтбоксі, на картці видно без наведення; два різні тексти (файл сховища /
  посилання з книги). Новий `scripts/live-mediaDelete.mts`
  (`npm run live:media-delete`). Деталі — запис #219.

**Fusion-Lab-Marketplaes.** Ця сесія маркетплейсу не торкалась — змін немає.

**Що перевірити після деплою** (Студія, вкладка «Медіатека»):
- кошик на картці фото чи відео видно **одразу**, без наведення курсора;
- клік по кошику **не видаляє**, а відкриває діалог із мініатюрою того самого
  файлу й попередженням «Відновити видалене неможливо»;
- «Скасувати» лишає все як було; «Видалити назавжди» прибирає картку;
- кошик є і в лайтбоксі (клік по мініатюрі відкриває кадр на весь екран);
- у тексті попередження прямо сказано, що **місце в тарифі при видаленні не
  звільняється** — це очікувана поведінка (політика `mediaStorage.ts`), а не
  вада.

**Борг #219:** немає, крім повного `npm test` (не гнали — давні Windows-провали
на `/bin/sh`). Свідомо поза межами: кошика-архіву немає (власник просив
протилежне — попередити, що відновлення неможливе), підтвердження в решті
екранів (ілюстрації, персонажі, картки товарів) не додано, синхронного
видалення обох джерел немає — про це сказано в самому діалозі.

## Стан гілок на 19.09.2026 (після запису #218) — не запушено

**Book_Creality (Студія).** Гілка `master`, один коміт попереду
`origin`/`production` плюс документація до нього (цей запис, журнал сесій,
покажчик):

- `a9eabcc feat(media)`: сортування медіатеки — пʼять способів (типово «від
  першої генерації до останньої»), селект у тулбарі галереї, плашка з датою
  появи файлу; новий `src/utils/mediaSort.ts`, два нові набори тестів
  (`test:media-sort`, `live:media-sort`). Деталі — запис #218.

**Fusion-Lab-Marketplaes.** Ця сесія маркетплейсу не торкалась — змін немає.

**Що перевірити після деплою** (Студія, вкладка «Медіатека»):
- у тулбарі галереї, праворуч від фільтрів, зʼявився селект сортування з
  пʼятьма пунктами; типово — «Від першої генерації до останньої»;
- на мініатюрах видно дату появи — у файлів, які справді лежать у сховищі.
  Посилання, вставлені в книгу до появи сховища, підуть у кінець переліку:
  дати в них немає ніде, і це не помилка;
- вкладка «Відео» показує лише кліпи, і вони вишиковуються за часом
  генерації (перший кадр — першим);
- «Від останньої до першої», «За форматом», «За назвою», «За розміром»
  справді змінюють порядок.

**Борг #218:** немає, крім повного `npm test` (не гнали — давні
Windows-провали на `/bin/sh`). Окремо, і не через цю роботу: на Windows
`test:media-library` дає 58/1 — сам тест порівнює POSIX-шлях `…/media/`, а на
диску `\…\media\` (це не стосується сортування й не змінювалось у #218).

## Стан гілок на 17.09.2026 (після запису #181) — не запушено

**Book_Creality (Студія).** Гілка `master`, тепер дванадцять комітів
попереду `origin`/`production` — одинадцять попередніх (до і включно з
#180, див. блок нижче) плюс:
- `7b9c2cf feat(закупівлі)`: новий адмін-розділ «Закупівлі» — записи
  закупівлі дешевшого матеріалу (стара/нова ціна за одиницю,
  постачальник, звідки взялась дешевша ціна) і ручне застосування
  знижки від конкретного запису до обраних товарів через наявний
  `basePriceUah`/`priceUah` (та сама пара, що вже читає бейдж знижки на
  вітрині). Деталі — запис #181 вище.

**Fusion-Lab-Marketplaes.** Без змін від попереднього блоку — усе ще три
коміти поверх `36722a2`, ті самі, що й після запису #179. Ця сесія
маркетплейсу не торкалась — фіча повністю на боці Студії, повторно
використовує вже задеплойований пайплайн знижки.

**Що перевірити після деплою:**
- В адмінці: новий вузол «Закупівлі» на карті (operations, ліва
  колонка, поруч з «Управління цінами») відкриває розділ з формою
  додавання закупівлі й списком записів.
- Додати тестову закупівлю (стара ціна > нова ціна) → кнопка «Знижка» →
  обрати один чи кілька товарів → застосувати → перевірити, що
  `priceUah` товару(ів) зменшився, а `basePriceUah` виставився в
  попередню ціну (референс «до знижки»), і що на вітрині (після
  публікації) малюється вже наявна плашка знижки.
- Запис закупівлі після застосування показує чип із назвою товару й
  відсотком у списку — історія застосувань (`appliedTo`).

**Борг #181:** немає. Пошук альтернативних постачальників — на запит
асистента в розмові, не автоматизований (свідомий вибір власника, не
недороблено).

## Стан гілок на 17.09.2026 (після запису #180) — не запушено

**Book_Creality (Студія).** Гілка `master`, тепер одинадцять комітів
попереду `origin`/`production` — десять попередніх (до і включно з #179,
див. блок нижче) плюс цей самий коміт (`docs(log)`: запис #180 і
виправлення хибного висновку про `basePriceMinor` із запису #179).
Коду цей запис не чіпав — лише журнал: перевірка спростувала попередній
висновок, тож правити код не було чого.

**Fusion-Lab-Marketplaes.** Без змін від попереднього блоку — усе ще три
коміти поверх `36722a2`, ті самі, що й після запису #179.

**Що перевірити після деплою:** те саме, що вже описано в блоках #177–
#179 вище — цей запис нових пунктів не додає. Якщо власник хоче
ПОБАЧИТИ плашку знижки на реальному товарі: заповнити поле «Базова
ціна» (`basePriceUah`) для будь-якого опублікованого товару в редакторі
картки, опублікувати — плашка малюється кодом, який уже є в обох
репозиторіях, нічого додатково деплоїти для цього не треба.

**Борг #180:** немає нового — цей запис закрив хибний борг із #179, не
додав власного.

## Стан гілок на 17.09.2026 (після запису #179) — не запушено

**Book_Creality (Студія).** Гілка `master`, тепер девʼять комітів попереду
`origin`/`production` — вісім попередніх (до і включно з #178, див. блок
нижче) плюс:
- `89e4d61 feat(admin,bridge)`: вбудована електроніка (ESP32/Arduino) —
  окремий фіолетовий блок у редакторі картки товару понад LED-секцію
  (тумблер, контролер, самопрограмування, чекліст із 10 прикладів
  функцій), атрибути прокинуто через міст; новий `ApplyToProductModal.tsx`
  у калькуляторі собівартості — кнопка «Додати до ціни товару» додає
  повний підсумок розрахунку до `priceUah` обраного товару (опублікованого
  чи чорнетки).
- Цей самий коміт (`docs(log)`: запис #179) — журнальний запис і цей блок.

**Fusion-Lab-Marketplaes (маркетплейс, окремий репозиторій).** Три нові
коміти поверх `36722a2`, ще не запушено:
- `48ef598 feat(catalog): built-in electronics attributes and storefront block (accent-tech)`
  — ті самі чотири поля електроніки на `ProductAttributes`; нова секція
  на сторінці товару одразу після LED-блоку, обгорнута в наявний клас
  `.accent-tech` (Phase B6) — локально перемикає колір фону на
  технологічну гаму без жодного нового CSS-токена.

**Порядок деплою:** так само, як і для #177/#178 — спершу маркетплейс
(приймає нові поля `attributes` без власних правок бекенду, DTO й так
валідує їх вільно), потім Студія.

**Що перевірити після деплою:**
- В адмінці: редактор картки товару → новий фіолетовий блок «Вбудована
  електроніка» під LED-секцією помітно іншого кольору фону, ніж решта
  редактора (і в нічній, і в денній темі).
- Увімкнути електроніку, обрати кілька функцій, зберегти й опублікувати
  — на сторінці товару зʼявляється нова секція з іншим (прохолодним)
  кольором фону, ніж решта сторінки.
- У калькуляторі собівартості: кнопка «Додати до ціни товару» → обрати
  наявний товар зі списку → підтвердити → перевірити, що ціна товару в
  редакторі картки справді зросла рівно на підсумок розрахунку.

**Борг #179 (перевіркою НЕ покрито):** живої перевірки в браузері не було
— ні нового блоку в редакторі, ні нової секції на вітрині, ні модалки
калькулятора; наскрізний сценарій «увімкнути → опублікувати → побачити →
застосувати калькуляцію» не прогонявся одним потоком; модалка «Додати до
ціни товару» не захищена від подвійного застосування при повторному
натисканні без оновлення списку. ~~Виявлений побіжно борг про
`basePriceMinor`~~ — **спростовано в записі #180**: шлях уже працює,
висновок був помилковий (неповний огляд коду).

## Стан гілок на 17.09.2026 (після запису #178) — не запушено

**Book_Creality (Студія).** Гілка `master`, тепер вісім комітів попереду
`origin`/`production` — сім попередніх (до і включно з #177, див. блок
нижче) плюс:
- Цей самий коміт (`docs(log)`: запис #178) — у самій Студії коду не
  чіпав: усі зміни цього запису — на боці маркетплейсу (нижче). Запис
  документує друге коло правок галереї товару, схвалене власником одним
  словом («Давай.») після переліку з 6 пропозицій.

**Fusion-Lab-Marketplaes (маркетплейс, окремий репозиторій).** Один новий
коміт поверх `36722a2`, ще не запушено:
- `ef5547d feat(catalog): gallery zoom, swipe, video hover-preview, badges, sticky mobile buy bar`
  — лайтбокс на великому фото (Escape/стрілки/клік по підложці), свайп на
  мобільному, ховер-прев'ю відеомініатюри (десктоп), плашки «Новинка»/
  знижка над великим фото (`isNew`/`discountPercent`, рахуються в
  `page.tsx`), скелетон-заглушка під час завантаження великого слайда,
  новий `sticky-buy-bar.tsx` — липка панель купівлі на мобільному
  (`lg:hidden`, обгортає наявний `<AddToCart>`). Три нові ключі
  словника (`badgeNew`, `badgeDiscount`, `galleryClose`).

**Порядок деплою:** так само, як і для #177 — спершу маркетплейс, потім
Студія (Студія цього разу коду не змінювала, тож порядок значення не має,
але для послідовності — той самий).

**Що перевірити після деплою:**
- Сторінка товару органайзера: клік по великому фото відкриває
  повноекранний перегляд і закривається по Escape/кліку повз фото/кнопці
  закриття; стрілки клавіатури й свайп на телефоні перемикають слайди
  і в звичайному, і в повноекранному режимі.
- Наведення курсора на відеомініатюру в стрічці (десктоп) — відео
  програється мовчки просто в мініатюрі, не чекаючи кліку.
- На мобільному екрані — знизу видно панель «Купити» з ціною й кнопкою,
  що не перекриває вміст сторінки й зникає на широких екранах.
- Товар зі знижкою (якщо такий буде) показує плашку відсотка над великим
  фото; товар, опублікований менше двох тижнів тому, показує «Новинка».

**Борг #178 (перевіркою НЕ покрито):** живої перевірки в браузері не було
(лайтбокс, свайп на тачскріні, ховер-відео, вигляд липкої панелі на
телефоні) — лише `tsc --noEmit` і `eslint`, обидва чисто; юніт- чи
компонентних тестів для `ProductGallery`/`StickyBuyBar` не написано
(у веб-застосунку маркетплейсу немає усталеної інфраструктури фронтенд-
тестів); плашку знижки не перевірено на реальному товарі з
`basePriceMinor` (органайзер, з якого прийшов запит, знижки не має);
`next build`/`next typegen` не запускались (немає мережі до
`registry.npmjs.org` у пісочниці для SWC-бінарника — той самий,
задокументований у #177, непов'язаний борг).

## Стан гілок на 17.09.2026 (після запису #177) — не запушено

**Book_Creality (Студія).** Гілка `master`, чотири коміти попереду `origin`
(`FusionLabPush`) і `production` (`Book_Creator`), жоден ще не запушено —
три попередні (#174/#175/#176, див. блок нижче) плюс:
- `03555a3` — `feat(admin,bridge)`: фото/відео виробу як слайд-шоу на
  вітрині — до 20 фото і 2 відео (#177). Шаблон медіа картки товару
  (`FurnitureProductEditor.tsx`) тепер приймає відео, `uploadProductMedia`
  шле перше фото як `cover`, решту фото — `gallery`, відео — `video`.
- Цей самий коміт (`docs(log)`: запис #177) — журнальний запис і цей блок.

**Fusion-Lab-Marketplaes (маркетплейс, окремий репозиторій).** Один новий
коміт, ще не запушено (за тим самим правилом AGENTS.md — пуш лише на пряме
прохання власника):
- `36722a2 feat(catalog,bridge): product gallery slideshow with video, one shared media door` — приймає
  `kind: 'video'` у `/bridge/products/:externalId/media` (своя межа розміру
  150 МБ, mime mp4/webm/mov, ліміти 20 фото / 2 відео перевіряються на
  сервері незалежно від Студії); `media.controller.ts` — `Content-
  Disposition: inline` для відео + підтримка HTTP `Range`/206 (перемотка
  відео); новий компонент вітрини `apps/web/src/components/product-
  gallery.tsx` — один шаблон галереї для книги/курсу/виробу, замінив
  статичний блок обкладинка+грід у `catalog/[slug]/page.tsx`.

**Порядок деплою:** так само, як і для #173 — спершу маркетплейс (новий
код приймача `/bridge/products/:externalId/media` і `/media/:id/download`),
потім Студія. Стара Студія (без цього коміту) публікуватиме фото як і
раніше — зворотної несумісності немає, нове поле `kind: 'video'` приймач
просто ще не знав би, доки не задеплоєний.

**Що перевірити після деплою:**
- Сторінка товару органайзера на вітрині: велике фото + смуга мініатюр (4
  в ряд на телефоні), клік по мініатюрі перемикає велике фото.
- Завантажити відео через «Управління карткою вітрини» → «Опублікувати» →
  переконатись, що воно грає на сторінці товару (не завантажується файлом)
  і перемотка працює без затримки (Range-запити).
- Ліміти 20 фото / 2 відео — спробувати перевищити з обох боків (клієнт
  Студії і напряму через API мосту) і побачити повідомлення про межу, а не
  мовчазне обрізання чи 500-ту.

**Борг #177 (перевіркою НЕ покрито):** реального відеофайла для наскрізного
прогону не було (лише синтетичні байти в юніт-тесті); `nest build` і
`jest` для `apps/api` не запускались через непов'язані з цією правкою
несправності середовища (зіпсований symlink `@fusion-lab/*`,
`deleteOutDir` конфліктує з забороною видалення файлів у теці на цьому
пристрої) — компільованість перевірено через чистий `tsc --noEmit` замість
цього; `next build`/`next typegen` для `apps/web` не запускались (немає
мережі до `registry.npmjs.org` у пісочниці для SWC-бінарника); ручної
перевірки в браузері не було.

## Стан гілок на 17.09.2026 (після записів #174–#176) — не запушено

**Book_Creality (Студія).** Гілка `master`, три коміти попереду `origin`
(`FusionLabPush`) і `production` (`Book_Creator`), жоден ще не запушено:
- `fd43c0a` — `feat(auth,adminOs)`: роль «Менеджер сайту» (`site_manager`) —
  вузький доступ до чату підтримки й CRM (#174).
- `6798dd9` — `feat(admin)`: калькулятор собівартості меблів (дерево +
  епоксидна смола) у «Управлінні цінами» (#175).
- `87ba9e9` — `feat(trainers,wdi)`: «Емоційна майстерність письменника» +
  «Поріг» — два нові AI-модулі тренажерів, вбудовані в ядро навичків WDI
  (#176); докази пишуться в наявні компетенції `emotion_reader_impact` і
  `character_craft`, нових компетенцій не додавалось.
- Цей самий коміт (`docs(log)`: запис #176) — журнальний запис і цей блок.

**Fusion-Lab-Marketplaes (маркетплейс).** Не торкався цією сесією — жодних
змін, пушити нема чого.

**Порядок деплою:** для #174/#175/#176 достатньо самої Студії — жоден з
трьох записів не додає й не змінює маршрутів мосту `/bridge/*`.

**Що перевірити після деплою:**
- Роль «Менеджер сайту» (#174): вхід під цією роллю бачить лише чат
  підтримки й CRM, решту розділів адмінпанелі — ні.
- Калькулятор собівартості (#175): реальний прогін на живих цінах
  дерева/епоксидки, а не лише тестові фікстури.
- Нові вкладки «Емоційна майстерність письменника» й «Поріг» (#176) —
  жодного ручного прогону в браузері ще не було; перший реальний виклик
  `POST /api/ai/emotion-analyze` і `POST /api/ai/threshold-analyze` проти
  живої моделі теж не робився.

**Борг #174–#176 (перевіркою НЕ покрито):** повний агрегований `npm run test`
(~90 скриптів) жодного разу не доведено до кінця однією командою — лише
представницькі набори індивідуально (див. запис #176, розділ «Перевірка»);
наскрізного виклику ШІ для «Емоційної майстерності»/«Порогу» не було; ручної
перевірки нових вкладок тренажера в браузері не було.

## Стан гілок на 16.09.2026 (після ПРАВОК #173) — ✅ нічого не лишилось

**Book_Creality (Студія).** Два коміти, обидва запушено в `origin`
(`FusionLabPush`) і `production` (`Book_Creator`); хук `pre-push` пропустив
обидва (`journal-check` — 173 записи):
- `50b8822` — `fix(admin,bridge)`: фото виробу тепер їдуть у публікацію
  (`uploadProductMedia`), межі полів приймача (160 / 300 / 40000) видно в
  редакторі, залишок `null` = «на замовлення».
- `db1333e` — `docs(log)`: правки #173 після реальних прогонів.

**Fusion-Lab-Marketplaes (маркетплейс).** Коміт `dbeaeec` запушено в `origin`:
галерея ДОДАЄТЬСЯ (раніше фото того самого виду замінювали одне одного),
новий маршрут `DELETE /bridge/products/:externalId/media`.

**Порядок деплою — тепер критичний: спершу маркетплейс, потім Студія.**
Без нового маршруту публікація відмовиться на кроці очищення фото
(`Маркетплейс не прибрав фото виробу: … Cannot DELETE`).

**Що перевірити після деплою:**
- **Перепублікувати виріб** — у наявного лістинга фото немає (публікація їх не
  несла). Після повторної мають зʼявитися головний банер і галерея.
- **«Немає в наявності»** за порожнього залишку більше не показується, кнопка
  покупки активна.
- Для найрізкіших фото — **перетягнути файли в редактор заново**: у чорнетці
  лежать превʼю 1200px зі старою якістю (нове превʼю — 1600px / JPEG 0.88).

**Борг #173 (перевіркою НЕ покрито):** реальний multipart через busboy/multer
(у тестах FormData розбирається напряму), вигляд сторінки товару з фото в
браузері, вигляд денної теми, повний `npm test`.

## Стан гілок на 16.09.2026 (після пушу #173) — ✅ нічого не лишилось

**Book_Creality (Студія).** Два коміти в `master`, обидва запушено в `origin`
(`FusionLabPush`) і `production` (`Book_Creator`); хук `pre-push` пропустив
обидва пуші (`journal-check` — 173 записи):
- `d4e3b66` — `feat(admin,bridge)`: редактор картки виробу, маршрути
  `/api/admin/furniture-products`, гілка мосту `/bridge/products`, скрипти
  (живий прогін редактора, юніт-набір моделі, прогін публікації).
- `da082f6` — `docs(log)`: запис #173, журнал сесій, `task.md`.

**Fusion-Lab-Marketplaes (маркетплейс, окремий репозиторій).** Коміт `c3ad022`
запушено в `origin`:
- `attributes Json?` + міграція `20260915000000_product_attributes`;
- приймач `/bridge/products` (+ `/media`, GET, DELETE);
- `attributes` у мапі каталогу й `shared-types`; секції товару на сторінці
  `catalog/[slug]` + ключі uk/en.

**Порядок деплою (обидва репозиторії разом):** спершу маркетплейс (міграція
`product_attributes` + маршрути `/bridge/products`), потім Студія. Якщо
задеплоїти лише Студію — публікація виробу віддасть 404 «немає маршруту»,
і це очікувано.

**Що перевірити після деплою:**
- «Управління товарами» → «Додати виріб» → заповнити → «Опублікувати на
  вітрині» (потрібен налаштований міст: адреса + BRIDGE_API_KEY).
- Сторінку товару у вітрині: обкладинка, галерея, характеристики, LED,
  кольори, тони дерева.
- Реальний прогін усіх 10 фото: `npm run test:furniture-publish` (зараз
  працює проти мока; після деплою — спрямувати на справжній API).

**Борг #173 (перевіркою НЕ покрито):** реальний прогін публікації (приймач не
задеплоєний, ключа мосту немає, Docker вимкнений), застосування міграції на
живій базі, сторінка товару в браузері, реальний multipart (busboy/multer),
вигляд денної теми.

## Стан гілок на 14.09.2026 (після запису #172) — ✅ нічого не лишилось

**Book_Creality.** Коміт правки — `b9cb9b3` (#172 — «Управління товарами»,
повернення до мапінгу, справжнє авторство книги, однорядкова шапка), коміт
документації — `b6e22a5` (`docs(log)`: запис #172). Обидва запушено в `origin`
(`FusionLabPush`) і `production` (`Book_Creator`); хук `pre-push` пропустив
обидва пуші, `journal-check` — 172 записи. Точний стан —
`git log --oneline -1`.

**Що перевірити після деплою Railway** (проєкт `comfortable-caring`, сервіс
`Book_Creator`, автодеплой із `Book_Creator@master`):
- **Свою книгу**: у шапці студії на місці автора має стояти справжнє імʼя
  («Деменко Денис») замість «Невідомий автор». Заміна береться з обкладинки
  книги, інакше з імені залогіненого користувача, і зберігається одразу.
- **Шапку застосунку**: вона стала одним рядком замість двох (заміряно
  128px → 86px). Чіпи «Збережено», ID, «Слів», «Формат» показуються лише від
  1536px — на вужчих екранах вони ховаються, і це свідомо: інакше шапка
  переносила другий рядок і зʼїдала 40px висоти тексту.
- **Канву редактора**: вона має вміщатися в екран без прокрутки (раніше низ
  аркуша на 23px був за межею екрана). Сама висота канви не збільшилась — її
  визначають смуги всередині редактора, і це окреме завдання.
- **Карту адмінки**: дванадцять пігулок, «Управління товарами» в лівій колонці,
  кнопка «Повернутися до мапінгу адмін панелі» над вмістом кожного розділу.
- **Розділ «Управління товарами»**: перелік того, що стоїть у вітрині, і зняття
  лістинга. Кнопки «додати» там немає навмисно — товар додається публікацією зі
  Студії.

**Що НЕ перевірено й лишається боргом (#172):** зняття товару з вітрини живим
кліком (немає налаштованого мосту), заміна авторства у справжній книзі, вузькі
екрани після згортання шапки, висота канви, повний `npm test`.

## Стан гілок на 14.09.2026 (після запису #171) — ✅ нічого не лишилось

**Що саме лежить у робочій теці:**
- `src/components/AdminProductsView.tsx` (новий) — розділ «Управління товарами»:
  перелік того, що стоїть у вітрині, і зняття лістинга. Кнопки «додати» немає
  свідомо — товар додається публікацією зі Студії.
- `src/components/adminOs/nodes.ts` — дванадцятий вузол «Управління товарами»,
  розкладка карти 3/5/4, `AdminView` отримав `'products'`.
- `src/components/adminOs/AdminOsView.tsx` — сторінка товарів, кнопка
  «Повернутися до мапінгу адмін панелі» над вмістом кожного розділу, пʼята
  лінія провідників у лівій колонці.
- `src/utils/bookAuthor.ts` (новий) + `src/App.tsx` — авторство книги: службове
  «Невідомий автор» замінюється іменем з обкладинки, інакше іменем користувача,
  і одразу зберігається.
- `src/components/ImportMaterialsWizardModal.tsx`, `src/components/CreateBookModal.tsx`
  — більше не створюють службового значення, підставляють імʼя користувача.
- `src/components/HeaderNav.tsx` — шапка з двох рядків стала одним
  (`display: contents`, чіпи лише на `2xl`); `src/components/EditorView.tsx` —
  магічне `calc(100vh - 105px)` замінено на `EDITOR_SURFACE_HEIGHT` від
  `--app-header-h`.
- `scripts/test-adminOs.mts` (48/48), `scripts/test-editorToolbar.mts` (59/59),
  `scripts/test-bookAuthor.mts` (новий, 20/20), `scripts/live-adminMap.mts`
  (37/37) + `package.json`.
- Журнали: `log.md`, `log/119-151.md`, `log/sessions.md`, `task.md`.

**Перевірено перед тим, як лишити незакоміченим:** `tsc --noEmit` 0;
`npm run build` OK; `test:admin-os` 48/48; `test:editor-toolbar` 59/59;
`test:book-author` 20/20; **`live:admin-map` 37/37** (дванадцять вузлів, кнопка
повернення, сторінка товарів, тулбар редактора). Заміри живого прогону:
**шапка 128px → 86px**, поверхня редактора 895px — усе вміщається в екран без
прокрутки (раніше 23px канви були за межею екрана).

**Що перевірити оком перед комітом:**
- Шапку застосунку на своєму моніторі: на 1280–1536 чіпи («Збережено», ID,
  «Слів», «Формат») ховаються — вони показуються лише від 1536px.
- Карту адмінки: дванадцять пігулок, «Управління товарами» в лівій колонці.
- Свою книгу: у шапці має стояти «Деменко Денис» замість «Невідомий автор»
  (у headless-прогоні це не перевіряється — там книга демонстраційна).

**Що НЕ перевірено й лишається боргом (#172):** зняття товару з вітрини живим
кліком (немає налаштованого мосту), заміна авторства у справжній книзі, вузькі
екрани після згортання шапки, висота канви (її визначають смуги всередині
редактора — окреме завдання), повний `npm test`.

## Стан гілок на 14.09.2026 (після запису #171) — ✅ нічого не лишилось

**Book_Creality.** Коміт правки — `3447065` (#171 — іконки замість підписів у
тулбарі «Книга та текст» + неонова карта адмінпанелі з реальними розділами),
коміт документації — `f188bfb` (`docs(log)`: запис #171). Обидва запушено в
`origin` (`FusionLabPush`) і `production` (`Book_Creator`); хук `pre-push`
пропустив обидва пуші, `journal-check` — 171 запис. Точний стан —
`git log --oneline -1`.

**Що саме змінилось (щоб наступна сесія не шукала по історії):**
- `src/components/EditorView.tsx` — видимі підписи знято з десяти кнопок блоку
  швидкої типографіки, текст переїхав у `title` і `aria-label`, іконки 18px
  (акцентна — 20px). Верхній рядок не чіпано.
- `src/components/adminOs/nodes.ts` — `slot` (розкладка 3/4/4), `tone`,
  `kind: 'view'` замість модалок, новий вузол «Модерація».
- `src/components/adminOs/AdminOsView.tsx` + новий `src/components/adminOs/adminOs.css`
  — неонова карта за макетом власника, реальні сторінки замість модалок,
  справжні числа смуги сутностей і інспектора.
- `src/components/AdminPanelView.tsx` — вкладка `'moderation'`; панель модерації
  більше не показується під «Мостом до вітрини».
- `src/components/AdminModerationView.tsx` (новий) — розділ «Модерація».
- `src/components/QuickAiModal.tsx` — `CoreAiPanel` експортовано, `book`/`onClose`
  необовʼязкові (він і сторінка адмінки, і вкладенка «Швидкого AI»).
- `scripts/test-adminOs.mts`, `scripts/test-editorToolbar.mts`,
  `scripts/live-adminMap.mts` (нові) + `package.json` (`test:admin-os`,
  `test:editor-toolbar`, `live:admin-map`; обидва тести в `npm test`).

**Що перевірити після пушу:** тулбар «Книга та текст» у **вузькому** вікні —
саме там підписи зʼїдали місце; підказки мають зʼявлятися при наведенні. Карту
адмінки: пігулки, червона «Історія комітів», кільце ядра, смуга сутностей із
числами. Що «Сонечко» на адмінці поводиться як і досі (код його не чіпав, але
вигляд неонової сторінки змінився). Знімки живого прогону — `tmp/admin-map-*.png`.

**Перевірено перед комітом:** `tsc --noEmit` 0; `npm run build` OK;
`test:admin-os` 43/43; `test:editor-toolbar` 59/59; **`live:admin-map` 32/32**
(справжній сервер + headless Chrome + клік по кожній з 11 пігулок; окремо —
тулбар редактора: без видимих підписів, з підказками); суміжні
`test:prompt-templates` 29, `test:core-module-models` 45, `test:core-ai` 52,
`test:market` 120, `test:publishing-routes` 89, `test:support-chat` 50,
`test:pdf-routes` 57 — 0 провалів; `journal:check` 171 запис, 0 мертвих посилань.

**Що НЕ перевірено й лишається боргом (#171):** натискання «Погодити»/
«Відхилити» в «Модерації» живим прогоном (черга була порожня);
`prefers-reduced-motion` і вузькі екрани в браузері; повний `npm test`.

## Стан гілок на 14.09.2026 (після запису #170) — ✅ нічого не лишилось

**Book_Creality.** Два коміти: `48cb6e9` (`chore(pdf)`: живий прогін рушія
«chromium» — прапорці `--engine`/`--chapters`/`--chromium` і захист
спостерігача) і `4a07037` (`docs(log)`: запис #170). Обидва запушено в
`origin` (`FusionLabPush`) і `production` (`Book_Creator`); хук `pre-push`
пропустив обидва пуші, `journal-check` — 170 записів. Точний стан —
`git log --oneline -1`.

**Важливе для деплою: продовий код не змінювався.** Єдина зміна в коді —
`scripts/live-agniImport.mts`, а це скрипт розробника (він у `npm test` не
входить і в образ не потрапляє). Тому деплой Railway нічого не ламає й нічого
не додає користувачу — перевіряти треба не «чи не зламалось», а **чи працює
верстка чужим рушієм**.

**Що перевірити після деплою** (проєкт `comfortable-caring`, сервіс
`Book_Creator`, автодеплой із `Book_Creator@master`):
- **Опублікувати редакцію з рушієм Chromium** і подивитись на картки у
  вітрині: картинки мають стояти в тексті (не купою в кінці глави), маркера
  `[IMG: …]` у файлі бути не повинно. Це створює **ОКРЕМЕ тестове видання**,
  у назві якого є позначка рушія — після перевірки його треба прибрати.
- **Очікувати іншу кількість сторінок**: на тому самому рукописі Nova дала
  489 сторінок, Chromium — 470. Це різні моделі розриву сторінок, а не
  втрата вмісту (картинок стільки ж, остання в обох у самому кінці). Якщо
  побачите розбіжність — це нормально, а от **зникла картинка чи
  надрукований маркер — ні**.
- **Примітка рушія «макет виконано браузером: формат сторінки й кегль узято з
  налаштувань, решта — з таблиці стилів рушія»** — це пояснення, а не помилка.
  Провалом вважати лише «не вставлено / пропущено / не знайдено».
- **pandoc** — єдиний рушій, який досі не бачив ніхто: локально його запустити
  неможливо (немає ні `pandoc`, ні `xelatex`), тому єдиний спосіб — публікація
  на проді тим самим шляхом, що й Chromium. Вирішено не робити цього разу
  (власник: «далі подивимося інші рушії пізніше»).
- **KDP / друкована редакція** — у Chromium і pandoc `supportsPrint: false`,
  тож друкованого видання вони не роблять; перевіряти це не потрібно, але
  варто знати, чому вибір рушія не впливає на `*_KDP.pdf`.
- **Обтікання текстом** (`wrap=left/right/contour`) у PDF не відтворюється —
  картинка стоїть блоком на місці маркера. Це не регресія #170, а
  підтвердження висновку #169 уже на другому рушії.

## Стан гілок на 14.09.2026 (запис #169 + підтвердження власника) — ✅ нічого не лишилось

**Book_Creality.** Коміт правки — `b424e0c` (#169 — картинки з рукопису
друкуються на своєму місці: маркери `[IMG: …]` у серверній верстці + три
причини, чому з 42 ілюстрацій не їхали дві), коміти документації — `3577fdb` і
наступні `docs(log)` (точний стан — `git log --oneline -1`). Запушено в обидва
remote.

**Підтвердження власника (14.09.2026): «Все працює, молодець!»** — книгу
перепубліковано, картинки на своїх місцях. Перевірено **лише рушій Nova**; сам
власник сказав: «далі подивимося інші рушії пізніше».

**Що перевірити наступного разу** (проєкт `comfortable-caring`, сервіс
`Book_Creator`, автодеплой із `Book_Creator@master`):
- **Рушії Chromium і pandoc.** Вони покриті тестами (`test:pdf-engines`: маркер →
  картинка на своєму місці в Markdown), але **у вітрину жодна книга ними не
  публікувалась**. Перевіряти так само, як Nova: опублікувати редакцію з
  вибраним рушієм (це створює ОКРЕМЕ тестове видання з позначкою рушія в назві)
  і подивитись, чи картинки стоять у тексті, а не купою в кінці глави, і чи не
  надруковано маркера.
- **Обтікання текстом** (`wrap=left/right/contour`) у PDF не відтворюється —
  картинка стає блоком на місці маркера. Це не регресія, але якщо власник
  захоче обтікання, це окреме завдання верстки.
- **Дві картки без обкладинки** (курс Fusion Autodesk і тестова книга) — борг із
  #168: курсу треба дати обкладинку й перепублікувати, тестову книгу зняти.

## Стан гілок на 13.09.2026 (після запису #169) — ✅ нічого не лишилось

**Book_Creality.** Коміт `b424e0c` (#169 — картинки з рукопису друкуються на
своєму місці: маркери `[IMG: …]` у серверній верстці + три причини, чому з 42
ілюстрацій не їхали дві) запушено в обидва remote; `origin` (`FusionLabPush`) і
`production` (`Book_Creator`) стоять на `b424e0c`.

**Що перевірити після деплою Railway** (проєкт `comfortable-caring`,
сервіс `Book_Creator`, автодеплой із `Book_Creator@master`):
- **Книгу Архата треба ПЕРЕПУБЛІКУВАТИ.** У каталозі лежить файл, зібраний ДО
  цієї правки: у ньому замість малюнків надруковано `[IMG: docx-img-N …]` на
  39 сторінках із 373, а всі 42 картинки зібрані купою на сторінках 6–41.
  Після перепублікації очікується: жодного маркера в тексті, картинка на
  кожному місці, де автор її поставив, остання (підпис «Слушайте музыку») —
  ближче до кінця книги.
- **Перевіряти саме друковану редакцію** («друковане видання», файл `*_KDP.pdf`):
  саме вона була у скарзі. Уривок перевіряти теж — він ріжеться з того самого
  PDF.
- **Якщо якась картинка не вставиться** — у звіті публікації буде примітка
  з причиною і першими байтами файлу (ці 4 байти — навмисно, бо саме вони
  знайшли причину «SOI not found in JPEG»).
- **Перевірити, що в текстовій редакції студії нічого не змінилось**:
  правка торкнулась лише серверної верстки PDF і майстра перенесення .docx.
- **Перенесення .docx**: під час імпорту лічильник картинок має доходити до 42
  без «переплутаних» — правка прибрала гонку, у якій плейсхолдер міг узяти чужі
  байти.

## Стан гілок на 13.09.2026 (після запису #168) — ✅ нічого не лишилось

**Book_Creality.** Коміт `9586e7d` (#168 — акцент «Сонечка» в трьох розділах,
статус і перебіг публікації у вітрину, заборона публікації без обкладинки)
запушено в обидва remote; `origin` (`FusionLabPush`) і `production`
(`Book_Creator`) стоять на `9586e7d`.

**Що перевірити після деплою Railway** (проєкт `comfortable-caring`,
сервіс `Book_Creator`, автодеплой із `Book_Creator@master`):
- **Колір кнопок.** У «Публікації та експорті», «Форматуванні файлу» й
  «Аналітиці ринку Etsy» текст кнопок, активні вкладки, фокус полів і прапорці
  мають слідувати за вибраним кольором сонця — як в інших розділах студії.
  Перевіряти найкраще, пересунувши сонце на інший колір: усе має перефарбуватись
  одразу, без перезавантаження.
- **Статус публікації.** Натиснути «Опублікувати у вітрину»: мають з'явитися три
  кроки зі станами («складаю PDF» → «малюю обкладинку» → «створюю картку й
  завантажую файли»), екран сам прокрутиться до блоку стану, а після завершення
  буде звіт на кожну редакцію з ✓/✗ для файла й обкладинки та посиланням на
  картку. Публікація триває хвилинами — це нормально; звіт переживе
  перезавантаження вкладки.
- **Картка без обкладинки більше не створюється.** Курс без обкладинки тепер
  відхиляється з причиною (400, `cover_required`) — це навмисно.
- **Борг у каталозі:** дві картки Студії досі без фото — «Курс Fusion Autodesk
  для ЧПУ станків» і «Тестова книга мосту NOVA — можна видаляти». Курсу треба
  дати обкладинку (поле «Обкладинка (URL)» у студії курсу) і перепублікувати;
  тестову книгу можна просто зняти з вітрини. Прод-базу маркетплейсу я не
  чіпав.

## Стан гілок на 13.09.2026 (після запису #167) — ✅ нічого не лишилось

**Book_Creality.** Коміт `276933d` (#167 — живий прогін експорту у PDF для
вітрини, новий `npm run live:storefront-export`) запушено в обидва remote;
`origin` (`FusionLabPush`) і `production` (`Book_Creator`) стоять на
`276933d`.

**Що перевірити власнику (це не деплой, а рішення).** Прогін виявив дві речі,
які варто вирішити окремо:
- **Передмова книги у PDF відсутня.** Копірайт, присвята, епіграф і зміст
  увімкнені в «Верстка & Поля», але серверна верстка Nova малює лише титул
  (у `PdfLayoutSpec` для решти блоків немає полів). Тобто у вітрину поїде файл
  без присвяти й епіграфа. Полагодити це — окреме завдання: воно змінює
  вигляд усіх книг і торкається нумерації сторінок.
- **Публікувати можна лише зі Студії, яку бачить маркетплейс.** Обкладинка в
  картці — це посилання `<адреса Студії>/api/public/books/<id>/cover`, і його
  відкриває покупець. З локальної машини там був би `127.0.0.1`, тобто картка
  без обкладинки.
- **Живий прогін експорту** (коли знадобиться ще раз):
  `npm run live:storefront-export -- --book=<id>`; скрипт пише зверстані файли
  в робоче сховище книги й падає з кодом 1, якщо котрийсь крок не відбувся.

## Стан гілок на 13.09.2026 (після запису #166) — ✅ нічого не лишилось

**Book_Creality.** Коміт `533709b` (#166 — фаза 4: чесний зум, лінійка в
одному контейнері з колонкою, абзацний відступ у друк) запушено в обидва
remote; `origin` (`FusionLabPush`) і `production` (`Book_Creator`) стоять на
`533709b`.

**Що перевірити після деплою Railway** (проєкт `comfortable-caring`,
сервіс `Book_Creator`, автодеплой із `Book_Creator@master`). Фаза 4 — про
поведінку, яку тести бачать лише числами, тож перевіряти треба руками:
- **Чесний зум.** Поставити 150 % у панелі, вужчій за аркуш: аркуш має стати
  **ширшим за панель і зʼявитися горизонтальна смуга прокрутки**. Раніше він
  тихо стискався до ширини панелі (у вузькій панелі «100 %» показувало ~78 %),
  а прокрутки не було взагалі.
- **100 % = рівно 1:1**, а не «скільки влізло». Прокрутити аркуш убік — лінійка
  має їхати **разом із текстом** (вона тепер у тому самому прокручуваному
  контейнері).
- **Перемикач «Авто (вмістити)»** — аркуш вміщається в панель повністю, і
  понад заданий відсоток не збільшується.
- **Лінійка при прокручуванні**: горизонтальна лишається зверху (не тікає вгору),
  смуга вертикальної — зліва. Світла зона тексту має стояти рівно над текстом і
  при зумі (живий замір дав ±0.00 px, але очима теж варто глянути).
- **Друк (PDF)**: абзацний відступ тепер приходить із «Верстка & Поля»
  (за замовчуванням 6 мм), і між абзацами зʼявилася відбивка, якщо її задати.
  Раніше рушій Chromium тримав жорстке `text-indent: 1.2em` і **ігнорував**
  налаштування автора. Локально друк не прогнано жодного разу — це єдине, що
  перевірено лише таблицею стилів.
- **Очікувано, не баг**: при чесному зумі краї аркуша виходять за панель (це і є
  прокрутка); розрив **усередині** абзацу, вищого за аркуш, досі не
  реалізовано — така сторінка лишається бурштиновою на вертикальній лінійці.
- **(#164–#165 у тому ж деплої)** якщо деплой один — перевірити і їх: «Розворот
  книги» має давати кілька сторінок на розділ, канва — той самий абзацний
  відступ, що в «Верстка & Поля»; докладніше в секції нижче.

## Стан гілок на 13.09.2026 (після записів #164–#165) — ✅ нічого не лишилось

**Book_Creality.** Три коміти цієї сесії запушено в обидва remote:
`8a47a88` (#164 — термінове виправлення регресії: `computeBreaks` перестав
накопичувати висоту, і «Розворот книги» показував по одній сторінці на розділ),
`8690d8b` (#165 — фаза 3: канва й вимір рахують однаково) і `42e3cdb` (той
самий #165 — живий знімок лінійок). `origin` (`FusionLabPush`) і `production`
(`Book_Creator`) стоять на `42e3cdb`.

**Увага до #164.** Це виправлення того, що поїхало в продакшн записом #163 —
якщо деплой #163 устиг поїхати, автор **бачив** «Розворот книги» з однією
сторінкою на розділ. Після деплою `42e3cdb` це має зникнути; перевіряти саме
на книзі з кількома розділами.

**Що перевірити після деплою Railway** (проєкт `comfortable-caring`,
сервіс `Book_Creator`, автодеплой із `Book_Creator@master`):
- **«Розворот книги»** — у кожному розділі має бути кілька сторінок (а не одна),
  і кількість сторінок має збігатися з тим, що показує канва редактора.
- **Канва редактора (#165)**: абзаци тепер мають ТОЙ САМИЙ абзацний відступ і
  ту саму відбивку, що задані в «Верстка & Поля» (за замовчуванням — 6 мм
  червоний рядок, без відбивки). Раніше канва ці налаштування ігнорувала
  геть, тож вигляд письма зміниться — це навмисно.
- **Кегль у канві** зріс на 2.6 % (14 px замість 13.65 px): кегль рахується як
  пункт × 96/72, а не × 1.3. Рядки переносяться так само, як у книзі.
- **Ширина тексту в канві** тепер дорівнює текстовій зоні аркуша: раніше
  обгортка мала 16 px відступу з кожного боку, і рядки були на 8.5 мм вужчі.
  Заодно зона тексту на горизонтальній лінійці має тепер стояти **точно** по
  тексту, а не по краях колонки.
- **Переповнена сторінка видна**: якщо в розділі є абзац або фото, вище за
  аркуш, його сторінка на вертикальній лінійці буде **бурштиновою**, а в
  підказці буде сказано, що друк розірве цей абзац, а канва поки що ні.
- **Очікувано, не баг**: краї лінійок обрізаються на вузькій панелі; верхнє й
  нижнє поле на вертикальній лінійці не показані (канва їх не малює);
  розрив усередині абзацу ще не реалізовано.
- **Друк**: канва тепер збігається з виміром «Розвороту» (перевірено живим
  заміром у Chrome), але з самим PDF може відрізнятись — у контейнері інша
  гарнітура (DejaVu Serif) і ввімкнені переноси. Це наступна фаза.

**Що перевірити з попередніх записів (#158–#163)** — усе вже задеплоєне
раніше; див. секції нижче.

## Стан гілок на 13.09.2026 (після запису #163) — ✅ нічого не лишилось

**Book_Creality.** Коміт `aa5fd27` (#163 — фаза 2: вертикальна лінійка по
сторінках) запушено в обидва remote; `origin` (`FusionLabPush`) і `production`
(`Book_Creator`) стоять на `aa5fd27`.

**Що перевірити після деплою Railway** (проєкт `comfortable-caring`,
сервіс `Book_Creator`, автодеплой із `Book_Creator@master`). Фази 0–2
перевірені ЛИШЕ тестами, без браузера, тож перевіряти треба очима:
- **Вертикальна лінійка (нова, #163)**: увімкнути лінійку — зліва від тексту
  мають з'явитися **світлі зони на кожну сторінку** зі своєю шкалою `0…257 мм`
  (для A4 з полями 20+20), цифри **скидаються на 0 на кожному аркуші**. Між
  зонами — темна смуга, і вона має стояти **точно навпроти смуги розриву** в
  тексті (тієї, де написано «— 2 —»). Якщо зони поїхали вниз щодо тексту —
  це та сама тонкість із `nodeDOM` після `dispatch`, про яку попереджає
  запис #163.
- **Підказка зони** (наведення на світлу смугу): «Сторінка 1: текст 257 мм із
  257 мм. Аркуш 297 мм». Якщо в підказці стоїть не та висота — перевіряти
  `getPageContentHeightMm`.
- **Зміна формату** (список «Формат:»): і розриви, і вертикальна лінійка
  мають перерахуватись **за секунду**, без потреби щось друкувати (це
  супутнє виправлення #163 — сигнал `paginationRescanKey`).
- **Горизонтальна лінійка (#162)**: шкала `0…210` по аркушу, підпис ширини
  тексту, ручки полів на межах зони (докладніше — секція #162 нижче).
- **Після перевірки розривів уважно**: якщо текст почне «стрибати» або
  розриви поповзуть після правки — це регресія від нового виміру після
  `dispatch`, і треба дивитись `onMeasured` у `PaginationPlugin.ts`.
- **Очікувано, не баг**: краї горизонтальної лінійки обрізаються на вузькій
  панелі; остання (недозаповнена) сторінка на вертикальній лінійці
  обрізається там, де закінчується текст; верхнє й нижнє поле на вертикальній
  лінійці не показані взагалі (канва їх не малює).

## Стан гілок на 13.09.2026 (після запису #162) — ✅ нічого не лишилось

**Book_Creality.** Коміт `eecf78c` (#162 — фаза 1: лінійка міряє аркуш)
запушено в обидва remote; `origin` (`FusionLabPush`) і `production`
(`Book_Creator`) стоять на `eecf78c`.

**Що перевірити після деплою Railway** (проєкт `comfortable-caring`,
сервіс `Book_Creator`, автодеплой із `Book_Creator@master`) — це НАЙВАЖЛИВІШЕ
для цього запису, бо фази 0 і 1 перевірені лише тестами, без браузера:
- **Лінійка над аркушем** (перемикач «показати лінійку») — шкала має йти від
  **0 на лівому краї АРКУША** до повного формату (A4 → 210, A5 → 148), а не
  обриватись на ширині тексту (для A4 з полями 20 — 170 мм). Сіра смуга полів
  має бути по краях, світла зона тексту — посередині, рівно над текстом
  колонки нижче.
- **Вигляд при іншому форматі**: змінити формат у списку «Формат:» — шкала
  має перемалюватись під новий аркуш, а не лишитись старою.
- **Зум**: 50 %, 100 %, 150 % — світла зона тексту на лінійці має лишатися
  рівно над колонкою тексту (розбіжність до 3 px — відома, див. #162).
- **Перетягування ручок** (жовті смужки на межах тексту): поле має змінюватись
  синхронно з «Верстка & Поля», не давати піти менше 5 мм і не звужувати текст
  до нуля. Підпис праворуч має показувати ту саму ширину тексту в мм.
- **Режим «UA | EN»**: лінійок дві (по одній на панель) — обидві мають стояти
  над своїм текстом.
- **Англійська мова інтерфейсу**: підпис «Text: 170 mm», підказки ручок
  англійською.
- Якщо аркуш ширший за панель, краї лінійки обрізаються — це очікувано;
  повністю аркуш видно з меншим масштабом (чесний зум — фаза 4).

**Що перевірити з попередніх записів (#158–#161)** — усе вже задеплоєне
раніше, окремо робити нічого не треба; див. секції нижче.

## Стан гілок на 13.09.2026 (після запису #161) — ✅ нічого не лишилось

**Book_Creality.** Коміт `c9c386c` (#161 — фаза 0: єдина геометрія аркуша)
запушено в обидва remote, `origin` (`FusionLabPush`) і `production`
(`Book_Creator`) стоять на `c9c386c`.

**Що перевірити після деплою Railway** (проєкт `comfortable-caring`,
сервіс `Book_Creator`, автодеплой із `Book_Creator@master`):
- **Фаза 0 вигляду не змінює** — це навмисно. Єдине, що варто глянути: книга
  **без** явно заданого розміру сторінки тепер міряється як A5 148×210, а не
  152×229. Якщо така книга в когось є, її текстова колонка й пагінація
  «Розвороту книги» стануть вужчими на 4 мм — це виправлення розбіжності, але
  помітне оком.
- **Книги з відкритим «Розворотом» (`.docx`-імпорт)**: ширина сторінки більше
  не може стати `NaN` (раніше така книга мовчки втрачала пагінацію).
- **Регресія рендера**: колонка тексту, лінійка й зона тексту не мають
  «поплисти» після заміни трьох розрахунків одним.
- **Іконкова панель і сама лінійка ще старі** — це фази 1–4, вони не в цьому
  коміті.

**Що перевірити з попередніх записів (#158–#160) — те саме, що було нижче**
(смужка стану в майстрі, перенесення .docx із 42 картинками, лічильник
сховища, розділи медіатеки, KOI8-U .txt). Усі вони вже задеплоєні цим же
пушем і в попередньому — окремо нічого робити не треба.

## Стан гілок на 13.09.2026 (після запису #160) — ✅ нічого не лишилось

**Book_Creality.** Три коміти цієї сесії запушено в обидва remote:
`ccf160c` (#158 — кодування .txt і імпорт .docx), `f2a68ef` (#159 —
картинки з .docx у книзі й медіатеці, розділи медіатеки по книгах, два
виправлення поділу на глави) і `ac3207e` (#160 — смужка стану в майстрі).
`origin` (`FusionLabPush`) і `production` (`Book_Creator`) стоять на
`ac3207e`. Хук `pre-push` пропустив усі пуші; `journal-check` — 160 записів
(#1–#160), 0 посилань у нікуди.

**Що перевірити після деплою Railway** (проєкт `comfortable-caring`,
сервіс `Book_Creator`, автодеплой із `Book_Creator@master`):
- **Смужка стану (#160)** — те, що перевірено лише в Node, а не в браузері:
  на кроці «Текст» при завантаженні рукопису власника (35 МБ) має з'явитися
  «Читаємо файл рукопису…», далі «Розбираємо .docx — читаємо зображення…» з
  лічильником, що росте до **42**, і на «Завершити» — «Зберігаємо зображення
  в медіатеку… 42 / 42». Смужка має зникати після завершення.
- **Перенесення .docx** (`D:\Книга первая\Книга Агни Йог _Архат_\
  Самоучитель…docx`): 42 картинки в «Медіатеці» й на своїх місцях у канві,
  близько 16 глав, перша — «Вступ».
- **Лічильник сховища**: 37.4 МБ списуються; під **адмінським** акаунтом
  квота не діє взагалі (перевірено в коді: `role === 'admin'` → без ліміту),
  тож справжню поведінку ліміту видно лише під звичайним автором.
- **Розділи медіатеки** — селектор з'являється, коли книг стає дві.
- **Текстовий .txt** із тієї ж теки (KOI8-U) — без «@@@@@».

## Стан гілок на 13.09.2026 (після запису #159) — ✅ нічого не лишилось

**Book_Creality.** Два коміти цієї сесії запушено в обидва remote:
`ccf160c` (запис #158 — визначення кодування .txt і імпорт .docx) і
`f2a68ef` (запис #159 — картинки з .docx у книзі й медіатеці, розділи
медіатеки по книгах, два виправлення поділу на глави). `origin`
(`FusionLabPush`) і `production` (`Book_Creator`) стоять на `f2a68ef`.
Хук `pre-push` пропустив обидва пуші; `journal-check` — 159 записів
(#1–#159), 0 посилань у нікуди.

**Що перевірити після деплою Railway** (проєкт `comfortable-caring`,
сервіс `Book_Creator`, автодеплой із `Book_Creator@master`):
- **Перенесення .docx зі справжнім рукописом** (`D:\Книга первая\Книга Агни
  Йог _Архат_\Самоучитель…docx`, 35 МБ): мають приїхати **42 картинки** —
  вони з'являються в розділі «Медіатека» під назвою нової книги й на своїх
  місцях у канві «Книга і текст»; глав — близько 16, перша — «Вступ».
- **Лічильник сховища.** Тільки цей рукопис — 37.4 МБ проти 100 МБ
  безкоштовного тарифу: після імпорту подивитися смужку «Фотоальбом» і
  переконатися, що байти списались. Якщо ліміт вичерпається посеред
  імпорту, має приїхати тост із цифрою втрачених картинок, а не мовчазна
  втрата.
- **Розділи медіатеки.** Коли книг стане дві, у «Медіатеці» має з'явитися
  селектор розділів; у режимі «Усі книги» картки групуються під заголовком
  книги, активна — першою.
- **Текстовий .txt** із тієї ж теки (KOI8-U) — без «@@@@@», з главами.

## Стан гілок на 13.09.2026 (після запису #158) — ✅ нічого не лишилось

**Book_Creality.** За прямим проханням власника виконано обидва пуші —
`git push origin master` (`FusionLabPush`) і `git push production master`
(`Book_Creator`) — `cc99dc0..ccf160c`. Тобто **нічого незапушеного немає**:
обидва remote стоять на тому самому коміті, що й локальний `master`.
Зміст коміту — запис #158 (визначення кодування .txt + імпорт .docx у
майстрі перенесення; зображення → медіатека користувача).

Хук `pre-push` спрацював штатно на обох пушах (перевірив посилання
`#158` у коміті проти журналу), погодивши обидва. `journal-check` на всій
історії: **158 записів (#1–#158), 217 комітів, 116 посилань, нуль у
нікуди.** Третій важіль перевірки — `test:text-encoding` (18/18) і
`tsc --noEmit` — прогнано до коміту, не після.

**Що перевірити після деплою Railway** (проєкт `comfortable-caring`,
сервіс `Book_Creator`, автодеплой із `Book_Creator@master`):
- Розділ «Перенесення книги з іншого сервісу» → крок «Текст»: завантажити
  .txt у Windows-1251, KOI8-U і UTF-8 — текст має приїхати читабельним,
  без «@@@@@» (це саме та вада, яку полагоджено).
- Там же завантажити `.docx` — текст має розібратися на глави за
  заголовками.
- Крок «Зображення»: додати картинку — після завершення вона має
  з'явитися в розділі «Медіатека» (файл на сервері, короткий URL у книзі),
  а не лише в книзі як data-URL.
- Лічильник сховища тарифу має врахувати ці файли — це та сама квота, що
  й у «Медіатеці» (`POST /api/media/upload` рахує байти сам).

## Стан гілок на 10.09.2026 (після запису #124)

**Book_Creality.** `origin/master` (GitHub) синхронізовано до `6e30012`
включно. `D:/Rama/Book_Creality` на пристрої власника — на 3 коміти
попереду: `2d60e71` (запис #123, Task C редизайн), `dd13838` (docs), і
`0aa8003` (запис #124, H1-H3). **Пуш ЗАПИТАНО власником прямо** (запис
#124, AskUserQuestion → всі 4 пункти, включно з «Запушити коміти в
GitHub»), але **не вдався технічно, не з політичних причин**: `git push
origin master` через `device_bash` на пристрої власника впав з `fatal:
could not read Username for 'https://github.com'` — headless-шелл без
збереженого `credential.helper` і без TTY для інтерактивного логіна;
вводити облікові дані самостійно заборонено правилами безпеки. **Отже
досі нічого не запушено — власнику потрібно самому виконати `git push
origin master` у `D:\Rama\Book_Creality`** (звичайний `git push` у
терміналі/GUI на його машині відкриє знайомий запит на вхід чи
використає вже налаштований на його боці credential-helper).
**ВАЖЛИВО (правило репозиторію, AGENTS.md, рішення власника 02.09.2026):**
кожен коміт містить лише `Co-Authored-By: Claude Sonnet 5
<noreply@anthropic.com>`, БЕЗ рядка `Claude-Session:` — звірено в обох
копіях після перенесення. Репозиторій публічний.

**Маркетплейс.** Сторінку огляду курсу мебелярів запушено в `master`:
`53bb86a` — `feat(web): add furniture course overview page`. Чужі локальні
`.vscode/`, `apps/web/src/app/[locale]/admin/infrastructure/` і `desktop.ini`
не входять до коміту. Після деплою перевірити картку `furniture-makers`,
маршрут `/catalog/furniture-makers/overview`, перехід до купівлі та доступ
студента до платних відео.
