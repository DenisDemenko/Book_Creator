# ТЕХНІЧНЕ ЗАВДАННЯ (TECH SPEC)

## «Цифрова майстерня письменника» — Фази 0, 1, 2, 3

**Версія:** 2.0 (затверджена)
**Дата:** 19.08.2026

---

## ОГЛЯД

Документ фіксує конкретні зміни в коді NOVA STUDIO. Основний фокус —
Фаза 0 (швидкі виправлення) та Фаза 1 (модуль стилю + дашборд). Фази 2
і 3 описані концептуально для планування архітектури.

---

## ФАЗА 0 — Швидкі виправлення (без нової архітектури)

**Термін:** 1–2 дні
**Мета:** Усунути «заглушки» та зробити наявний функціонал робочим.

### Завдання 0.1. Підключити реальну AI-перевірку граматики

- **Локація:** `src/views/AIStudioView.tsx` (або де зараз хардкод 88%/92%).
- **Що зробити:** Видалити статичні показники. Викликати
  `fetch('/api/ai/check-grammar', { body: text })`.
- **Відповідь API:**
  `{ score: number, issues: [{ type: 'grammar'|'style'|'readability', message: string, offset: number }] }`.
- **UI:** Показати реальний score та список проблем із підсвічуванням у
  тексті (якщо є офсети — підсвітити жовтим).

### Завдання 0.2. Доповнити діагностичний квіз

- **Локація:** `src/data/diagnosticQuestions.ts`.
- **Що зробити:** Додати питання для категорій, яких бракує:
  - `emotion` (емоційний інтелект у тексті),
  - `visual` (візуалізація та образи),
  - `book_prep` (підготовка рукопису до видання).
- **Кількість:** По 2–3 питання на категорію (самооцінка за шкалою 1–5).

### Завдання 0.3. Реальна ротація «Завдання дня» + Streak

- **Локація:** `src/hooks/useDailyTask.ts` (створити, якщо немає) та
  `server/dailyTask.ts`.
- **Логіка:**
  1. Бекенд зберігає `lastTaskDate` та `taskIndex` у базі (або в
     localStorage для MVP).
  2. Якщо `today > lastTaskDate`, генерується нове завдання з пулу (з
     урахуванням слабких навичок).
  3. Streak = кількість днів поспіль, коли автор виконав завдання
     (відмітка «виконано»).
- **UI:** Відображати номер дня (Day 1, Day 2...) та індикатор серії (🔥).

### Завдання 0.4. Персоналізація черги вправ

- **Локація:** `src/views/MasteryView.tsx` (функція
  `getRecommendedExercises`).
- **Що зробити:** Зараз бере перші 6 із загального списку.
- **Нова логіка:** Сортувати всі вправи за
  `(weakness_score * 0.7 + randomness * 0.3)`, де `weakness_score` —
  рівень відповідної компетентності (що нижчий рівень, то вищий
  пріоритет). Показувати топ-6.

### Завдання 0.5. Додати відсутні бейджі

- **Локація:** `src/data/achievements.ts` та `server/triggers/`.
- **Список:**
  - «100 000 слів» (тригер: сума слів у всіх текстах автора).
  - «Перший курс» (тригер: створено курс із ≥3 модулів).
  - «50 відредагованих текстів» (тригер: використано AI-редактор 50 разів).
- **UI:** Бейджі з'являються в профілі автоматично.

---

## ФАЗА 1 — Модуль стилю + Дашборд (ГОЛОВНИЙ ПРІОРИТЕТ)

**Термін:** 1–2 тижні
**Мета:** Створити ядро продуктивності — файл `ім'я_автора.md` та
інформаційний хаб.

### Завдання 1.1. Бекенд: API для роботи зі стилем

- **Маршрути** (створити в `server/routes/style.ts`):
  - `POST /api/style/generate` — приймає `userId`, збирає всі тексти
    автора (з книг, нотаток, виконаних вправ), надсилає їх у Claude/GPT
    із промптом:
    > «Проаналізуй ці тексти. Вияви: улюблену довжину речень, частоту
    > використання дієприкметників, типові граматичні конструкції,
    > лексичне розмаїття, тон (іронічний/серйозний/науковий). Сформуй
    > файл у форматі Markdown з розділами: #Словник, #Ритм,
    > #Типові_звороти, #Заборонені_слова (якщо часто повторюються).»
  - `GET /api/style/:userId` — повертає вміст файлу `ім'я_автора.md`.
  - `DELETE /api/style/:userId` — видаляє файл (тільки для автора або
    адміна).
  - `PUT /api/style/:userId` — оновлює вміст (якщо автор хоче
    відредагувати вручну).
- **Безпека:** Мідлвара перевіряє `userId` з токена. Якщо
  `userId !== request.user.id && !isAdmin` → 403.

### Завдання 1.2. Фронтенд: UI модуля стилю

- **Локація:** Нова вкладка `StyleView.tsx` у розділі «Майстерність».
- **Елементи:**
  1. Кнопка «Сформувати мій стиль» (велика, синя). При натисканні —
     показує Loading... (AI може працювати 20–30 сек).
  2. Поле попереднього перегляду (Markdown-рендер) із вмістом
     `ім'я_автора.md`.
  3. Кнопка «Редагувати» (відкриває текстовий редактор для правок).
  4. Чекбокс «Автоматично використовувати стиль для AI-доповнень»
     (зберігати налаштування в `user.settings`).
  5. Кнопка «🗑️ Видалити мій файл стилю» (червона, з модалкою
     підтвердження «Ви впевнені? Це незворотно»).

### Завдання 1.3. Інтеграція стилю в текстовий редактор

- **Локація:** `src/components/Editor/Editor.tsx` (там, де є
  AI-генерація тексту).
- **Логіка:** Якщо чекбокс активовано, при виклику AI Continue / Write
  with AI у промпт додається префікс:
  > «Напиши продовження в стилі автора, дотримуючись таких правил:
  > [вставити вміст ім'я_автора.md]. Ось початок тексту: ...»
- **Fallback:** Якщо файл стилю не знайдено — використовувати
  стандартний нейтральний тон.

### Завдання 1.4. Новий головний дашборд (замість старої сторінки метаданих)

- **Локація:** `src/views/DashboardView.tsx` (переробити кореневу
  сторінку `/`).
- **Секції** (зверху вниз):
  1. Профіль + XP + Рівень (взяти з MasteryView).
  2. Мій стиль — короткий статус: «Стиль сформовано [дата]» / «Стиль не
     сформовано».
  3. Інтерактивна карта — міні-версія з 5 найслабшими навичками.
  4. Поточний проєкт — назва книги, прогрес у відсотках (за кількістю
     написаних слів / планованих).
  5. Сьогоднішня вправа — взяти з `useDailyTask`.
  6. Останній AI-аналіз — результати останньої перевірки тексту.
  7. Швидкий доступ (іконки): Книги / Курси / Навички / Дослідження /
     AI / Прогрес.

---

## ФАЗА 2 — База знань та Тренажери (Проміжний етап)

**Термін:** 3–4 тижні
**Мета:** Перетворити медіатеку на робочий інструмент.

### Завдання 2.1. Інтерактивна база знань (Knowledge Base)

- **Локація:** Новий розділ `KnowledgeView.tsx`.
- **Функціонал:**
  - Завантаження: Окрім зображень, дозволити TXT, MD, DOCX
    (використовувати `mammoth.js` для парсингу DOCX на фронті).
  - Подвійна панель: Ліворуч — список файлів (з іконками). Праворуч —
    прев'ю контенту.
  - Перетягування (Drag & Drop): З прев'ю можна перетягнути виділений
    текст прямо у відкритий редактор книги (використовувати
    `onDragStart` / `onDrop`).
  - AI-цитата: Виділяєш текст у прев'ю → кнопка «Вилучити цитату» →
    випадає модалка з 3 варіантами:
    1. Пряма цитата («...» — [Джерело]).
    2. Перефразування (AI переказує своїми словами).
    3. Аналітичний коментар (AI пише: «Це важливо, бо...»).

    Обраний варіант вставляється в редактор.

### Завдання 2.2. Пілотні тренажери

- **Локація:** `src/views/Trainers/CharacterTrainer.tsx`,
  `DialogueTrainer.tsx`.
- **Структура кожного:**
  - Крок 1: Пояснення теорії (розгорнутий опис).
  - Крок 2: Завдання (наприклад, «Опиши персонажа через дію»).
  - Крок 3: Поле введення (до 1000 символів).
  - Крок 4: Кнопка «Оцінити AI» — викликає API з критеріями (для
    персонажа: унікальність, мотивація, архетип; для діалогу:
    природність, підтекст, розкриття характеру).
  - Результат: Бали + поради + XP.

---

## ФАЗА 3 — Фінальні великі системи (Без курсів і методик)

**Термін:** 5–8 тижнів
**Мета:** Завершити інфраструктуру.

### Завдання 3.1. Генератор структури книги (Конструктор)

- **Локація:** `src/views/BookStructureBuilder.tsx`.
- **Шаблони:**
  - Нон-фікшн: Проблема → Рішення → Докази → Вправи → Висновок.
  - Художня: Експозиція → Зав'язка → Розвиток → Кульмінація → Розв'язка.
- **Логіка:** Автор заповнює блоки текстом. AI для кожного блоку
  пропонує 3 варіанти заголовків або підпунктів (без зміни авторського
  тексту).
- **Експорт:** Згенерована структура автоматично створює розділи в
  книзі.

### Завдання 3.2. Авторський AI-асистент із пам'яттю

- **Локація:** `src/components/ChatAssistant.tsx`.
- **Зміни:**
  - Зберігати історію чату в localStorage/базі (для поточної сесії).
  - Додати системний промпт: «Ти помічник письменника. Ти знаєш його
    стиль: [вставити з ім'я_автора.md]. Відповідай лаконічно, ділово й
    натхненно.»
  - Без ролей (як я і просив) — просто один режим «Помічник».

### Завдання 3.3. Портфоліо (Приватний кабінет)

- **Локація:** `src/views/PortfolioView.tsx`.
- **Вміст:** Список усіх книг, курсів, методик (якщо є) автора з
  картками.
- **Статистика:** Загальна кількість слів, кількість завершених
  проєктів, середній бал AI-оцінок.
- **Примітка:** Без публічного доступу. Доступ тільки для автора.

### Завдання 3.4. Чітке ВИЛУЧЕННЯ (НЕ РОБИТИ)

Розробники мають проігнорувати ці функції, навіть якщо вони згадані в
старому ТЗ:

- ❌ Генератор навчального курсу (LMS).
- ❌ Конструктор авторської методики.
- ❌ Симулятор читача (будь-які ролі).
- ❌ Публічна сторінка автора.
- ❌ AI-оцінка «новизни» книги (заміна на чек-ліст у Фазі 1).

---

## ДОДАТКОВІ ТЕХНІЧНІ ВИМОГИ

1. **Безпека:** Усі роути `/api/style/*`, `/api/knowledge/*` мають
   перевіряти `userId` через JWT. Адмін має повний доступ.
2. **Продуктивність:** Генерація стилю — це важка операція.
   Використовувати чергу (BullMQ) або хоча б `setTimeout` з відповіддю
   `202 Accepted` і статусом `processing`, а оновлювати через
   WebSocket / Polling.
3. **Міграція БД:** Додати таблицю `user_styles`
   (`user_id, content_md, updated_at`) та `knowledge_files`
   (`user_id, filename, file_type, content_text, preview_image`).
4. **Тестування:** Написати мінімум по 2 тести для кожного нового API
   (позитивний і негативний сценарій).

---

**Дата початку:** (погоджується після затвердження документа)
**Відповідальний:** Tech Lead NOVA STUDIO

---

*Кінець документа.*
