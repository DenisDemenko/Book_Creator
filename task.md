# Завдання: «Покращити AI» ігнорував обрану модель (баг, продовження #127)

> Сесія Claude: 10.09.2026 #02 (продовження, з живого пристрою власника,
> реальний Gemini-ключ). Стан: причину знайдено, полагоджено,
> протестовано, закомічено. Деталі — `log.md`, запис #128.

**Постановка (власник, зі скріншотами).** Кнопка «Покращити AI» падає з
«This model is currently experiencing high demand», хоча в AI Асистенті
обрано GPT-4o і коуч (та сама модель) працює нормально.

**Причина.** `/api/ai/edit-text` жорстко викликав `generateWithGemini()`
напряму — вибір моделі в AI Асистенті на цю кнопку не впливав, запит
завжди йшов у Gemini. Власник підтвердив обсяг: полагодити тільки цю
кнопку (не решту ~20 старих ендпоінтів із тим самим патерном — окрема,
більша робота, залишається технічним боргом).

**Що зроблено.** `server.ts`: `/api/ai/edit-text` тепер резолвить рушій
через `resolveCoachEngine` і викликає `generateAiText` (обраний рушій
реально йде в запит); локальна симуляція лишена фолбеком тільки коли
рушій взагалі не налаштований. `EditorView.tsx`: `handleTriggerAiEdit`
надсилає `modelId`/`bookId`.

**Перевірка.** `tsc --noEmit` чисто; 27/27 юніт-тестів; Playwright
перехопив фактичне тіло запиту — `modelId` тепер коректно передається;
з `gpt-4o` (не налаштований у пісочниці) — чесна локальна симуляція
замість силуваного виклику Gemini; з `gemini-3.7-flash` — поведінка не
змінилась (реальна помилка від Gemini); `coach-analyze` (спільний
хелпер) — без regression.

**Чого перевірка НЕ покриває.** Реальна відповідь від справжнього
OpenAI/Claude (перевірено лише маршрутизацію, не якість відповіді).
Решта ~20 старих ендпоінтів із тим самим патерном — свідомо не
займались, лишаються технічним боргом.

---

# Завдання: рестайл AI-коуча + відкладені тести (продовження #125)

> Сесія Claude: 10.09.2026 #02 (продовження). Стан: реалізовано,
> протестовано (dev-сервер, реальна admin-сесія, Playwright), закомічено
> на пристрій `D:\Rama\Book_Creality` і **запушено власником у
> `origin/master`** (`7eaeb0a..8991b1b`). Деталі — `log.md`, запис #127.

**Постановка (власник, українською, 3 пункти).**
1. Провести всі тестування, які визначались раніше по задачах і були
   відкладені (після git push) — перевірити `log.md`/`task.md` на
   незакриті пункти й провести ті, що можна провести в цій сесії.
2. Попап AI-коуча виглядав нестильно (не як частина продукту) —
   переробити в тому самому стилі, що й чат «AI Літературний
   Консультант» (QuickAiModal.tsx).
3. Рядок кількості слів виділення стиснути й перемістити нагору,
   лівіше кнопки «Англійська версія».

## Що зроблено

1. **Відкладений тест проведено.** З `task.md`/`log.md` (запис #125,
   «Чого перевірка НЕ покриває») піднято єдиний реально відкладений і
   testable пункт — мобільна/планшетна ширина попапу коуча (390px і
   768px, Playwright) — раніше НЕ перевірялась окремо. Проведено:
   немає горизонтального скролу на жодній ширині, попап повністю
   влазить у в'юпорт, усі три вкладки й кнопка вставки лишаються
   клікабельними. (Якість AI-відповідей і далі не testable — дивись
   нижче, той самий, незмінний блокер із заглушкою `GEMINI_API_KEY`.)
2. **`CoachModal.tsx` повністю переюзує неоморфну тему Modul_token**
   (`.token-module-scope`, `nm-outset`/`nm-inset`/`nm-btn`/
   `nm-btn-primary`, `msg-user`/`msg-assistant` для бульбашок чату) —
   ту саму, що й у `QuickAiModal.tsx` («AI Літературний Консультант»,
   `src/styles/tokenModuleTheme.css`), замість власної палітри
   bg-slate-900/cyan-500. Кнопка «Вставити в книгу» свідомо лишена
   смарагдовою (інша за змістом дія — комміт у рукопис, а не
   повідомлення в чат), але в тій самій мові тіней (`nm-btn`).
3. **Компактний чіп кількості слів.** Рядок «Слів (UA): N • EN: N •
   ~N хв читання» (три окремі `<span>`, раніше в переповненому
   горизонтальним скролом тулбарі — легко губився) прибрано звідти;
   один стиснутий моноширинний чіп доданий у рядок форматування
   режиму UA (`EditorView.tsx`), лівіше кнопки «Англійська версія»
   (`openEnglishWindow`/`editor.openEnWindowBtn` — саме так, дослівно,
   називається ця кнопка, не плутати з пільками режиму 🇺🇦/🇬🇧 вище).
   Для режимів EN/Паралельно (де немає цього рядка форматування)
   лишено резервний варіант у зовнішньому тулбарі — без дублювання в
   режимі UA. +1 i18n ключ `readingTimeShort` (uk/en), решта тексту
   переюзана з наявних ключів (`wordsShort`, `wordsUaLabel`).

## Перевірка

- `npx tsc --noEmit` — чисто.
- `scripts/test-manuscriptDoc.mts` — 27/27 (без змін: маркери не
  займались, це суто UI-рестайл + перестановка).
- Наскрізний Playwright на реальній тестовій книзі («Тіні Нео-Києва
  2084»): усі 3 вкладки коуча (Аналіз/Чат/Аудит книги) рендеряться в
  новому стилі; вставка в книгу через реальний API перевірена
  наскрізно (вставлено → підтверджено в збереженому JSON → прибрано
  після перевірки); чіп кількості слів видимий і коректний у режимі
  UA (перевірено текстовим змістом DOM, не лише скріншотом); перемикання
  UA → Паралельно → EN → назад — без рантайм-помилок (`page.on
  ('pageerror')` чистий); mobile 390px і tablet 768px — див. пункт 1
  вище.

## Чого перевірка НЕ покриває

- **Якість AI-відповідей коуча** — той самий, незмінний блокер із
  запису #125: `GEMINI_API_KEY` у хмарній пісочниці — заглушка. Цей
  раунд роботи не змінював серверну логіку (`server.ts` не займався),
  тож повторно не перевірялось.
- **Реальний скріншот на живому пристрої власника** — вся перевірка
  велась у хмарному клоні (`/root/work/bookcreality`), не на
  `D:\Rama\Book_Creality`; перенесення — окремим кроком одразу після
  цього запису (патч + `git am --3way`, той самий перевірений шлях).

---

# Завдання: AI-коуч «Книга і текст» (тренажер письменника, макет fusion_lab_ai_mentor_v4.html)

> Сесія Claude: 10.09.2026 #02 (хмарна пісочниця, клон у `/root/work/bookcreality`,
> перенесено на пристрій власника `D:/Rama/Book_Creality` через
> `git format-patch` → `git am --3way` після відновлення зв'язку — див.
> `log.md` запис #126). Стан: реалізовано, протестовано (dev-сервер,
> реальна admin-сесія, Playwright), закомічено в обох місцях,
> **запушено власником у `origin/master`** (`git push` виконано власноруч
> на своїй машині, `6e30012..2f108be`). Деталі перевірки — `log.md`,
> запис #125.

**Постановка (власник, українською).** Уже було кілька спроб зробити ШІ-
тренажера для письменника, у Fusion Lab Studio вже понад два такі напрями.
Взяти наданий макет `fusion_lab_ai_mentor_v4.html` за основу; кнопка
«Запустити AI коуча» на видному місці в «Книга і текст»; попап зʼявляється
після виділення тексту письменником у книзі; повертає створений ШІ та
автором текст у книгу за (новими чи вже наявними) тегами. Додано власний
технічний аналіз «Фрагмент vs уся книга» (MESO/MICRO — сцена, MACRO —
книга, Phase 2 = крос-сценна перевірка).

**Уточнення (AskUserQuestion, 3 питання) — усі відповіді визначили обсяг:**
1. Тригер: кнопка в тулбарі + значок біля виділення (не автоматично).
2. Обсяг: **повний Phase 2 зараз** (MACRO книжковий аудит — Continuity/
   Arc/Setup-Payoff), не лише MVP.
3. Джерело «ШІ»: макет — локальні regex-детектори; підтверджено підключити
   **справжній AI-бекенд** (те саме ядро `generateAiText`, що й решта
   продукту), не копіювати симуляцію.

**Ключове архітектурне рішення (після дослідження коду).** Новий bracket-
маркер для вставки тексту з коуча в книгу НЕ створювався. У продукті вже
є перевірений, використовуваний маркер `[AI-DRAFT]…[/AI-DRAFT]`
(`AiDraftBlockNode.tsx` — інтерактивний блок «AI-чернетка» з кнопками
прийняти/відхилити, раніше застосовувався для вставки AI-тексту за фото).
Вставка з коуча переюзає його повністю — той самий шлях, що й «Вставити
абзац за виділенням» (`runSelectionParagraphs`). Це відповідає вимозі
«повернути текст за створеними тегами» без дублювання інфраструктури й
без ризикованих змін у `manuscriptDoc.ts`.

## Що зроблено

1. **Сервер (`server.ts`)** — три нові ендпоінти, `requirePermission
   ('canUseAi')`, спільний резолвер рушія/ключа `resolveCoachEngine`:
   `/api/ai/coach-analyze` (здоров'я фрагмента сцени — 9 показників,
   головна проблема, розрив наміру, мікровправа, JSON-режим),
   `/api/ai/coach-chat` (багатоходовий чат з ментором), `/api/ai/coach-
   book-audit` (Phase 2 MACRO: суперечності/сюжетні лінії/арка персонажа/
   сетап-розкриття, читає реальний `book.chapters`/`book.characters`/
   `book.heroArc`, клієнтський+серверний ліміти довжини тексту).
2. **`src/components/CoachModal.tsx`** (новий) — попап на три вкладки
   (Аналіз/Чат/Аудит книги) + постійна нижня панель «Текст для вставки в
   книгу» з кнопкою вставки.
3. **`EditorView.tsx`** — кнопка «AI-коуч» у вкладці тулбару «AI
   Асистент»; плаваючий значок «✦ Коуч» біля кінця виділення тексту
   (`coordsAtPos`, прийом уже використаний для поповера згадок
   персонажа); `openCoachWithSelection`/`insertCoachTextToBook`.
4. **i18n** — ~35 нових ключів uk/en у `editor.ts`.

## Знайдений і полагоджений баг

React-помилка «Rendered more hooks than during the previous render» у
`CoachModal.tsx` — ранній `return null` стояв перед `useMemo`, кількість
хуків відрізнялась між закритим/відкритим станом (компонент змонтований
постійно). Полагоджено переміщенням `useMemo` перед early return. Цей
клас багів `tsc` не ловить — знайдено лише живим Playwright-прогоном.

## Перевірка

- `npx tsc --noEmit` — чисто.
- `scripts/test-manuscriptDoc.mts` — 27/27 (без змін: маркер не новий).
- Наскрізний Playwright-прогін на реальній тестовій книзі («Тіні
  Нео-Києва 2084»): значок «✦ Коуч» зʼявляється точно біля виділення;
  попап відкривається кнопкою тулбару; усі 3 вкладки рендеряться й
  звертаються до нових ендпоінтів; вставка в книгу перевірена НАСКРІЗНО
  (вручну вписаний текст → `[AI-DRAFT]…[/AI-DRAFT]` у збереженому JSON
  книги, точно на місці виділення → рендериться як звична «AI-чернетка»
  з кнопкою відхилення). Тестову вставку прибрано з бази після перевірки.

## Чого перевірка НЕ покриває

- **Якість AI-відповідей коуча.** Хмарна пісочниця має `GEMINI_API_KEY`
  = заглушка (`"dummy...dev"`), не справжній ключ — усі три ендпоінти
  падають з чесною помилкою (401/403). Перевірено, що це не баг нових
  ендпоінтів: ідентичний виклик уже перевіреного, довіреного
  `/api/ai/generate-paragraphs-from-selection` падає так само на цьому ж
  ключі — доводить коректність резолву рушія/запиту/відповіді в нових
  ендпоінтах, а не їхню поломку. Сама якість аналізу/чату/аудиту
  лишається неперевіреною до першого запуску зі справжнім ключем
  (продакшн Railway чи пристрій власника з реальним `.env`).
- Мобільна ширина попапу коуча окремо не перевірялась (той самий
  `fixed inset-0 ... p-3 sm:p-6` патерн, що й `HeroJourneyModal`, який
  мобільну перевірку вже пройшов раніше).
- Юніт-тестів на нові серверні ендпоінти немає — у проєкті немає
  тестового фреймворку для `server.ts` (лише `scripts/test-
  manuscriptDoc.mts` для клієнтського маркер-парсера).

## ✅ Завершено — перенесення й git push зроблено власником

Перенесення на пристрій завершено (`log.md`, запис #126) — усі коміти
цього розділу (позначення незавершеного з #124, сам AI-коуч,
документація) застосовані в `D:\Rama\Book_Creality` через `git am
--3way`, без конфліктів. **Власник власноруч виконав `git push origin
master`** у своєму терміналі (PowerShell, `D:\Rama\Book_Creality`) —
`6e30012..2f108be  master -> master`, успішно, без помилок. Усі коміти
цього завдання тепер в `origin/master` на GitHub. Питання відсутнього
`credential.helper` у headless-шеллі пристрою більше не блокер — власник
використав уже налаштований у себе логін.

---

# Завдання: редизайн «Книга і текст» за макетом FusionWrite (Task C)

> Сесія Claude: 10.09.2026 (хмарна пісочниця, клон у `/root/work/bookcreality`
> + перенесення на `D:/Rama/Book_Creality` через `device_bash`). Основна
> реалізація й H1-H3 (запис #124) закомічені в обох місцях і **запушені
> власником у `origin/master`**. **НЕЗАВЕРШЕНО лишається тільки публікація
> на живій вітрині** — див. розділ нижче, заблокована з причин поза межами
> цієї сесії (неробочий прод Railway), потребує дії власника.

## ⛔ Незавершено (потребує дії власника) — станом на 10.09.2026, запис #124

Власник прямо підтвердив (AskUserQuestion, усі 4 пункти розділу «10. Що
далі» звіту), що хоче виконати обидва наступні кроки цієї ж сесії. Обидва
спробувано і обидва впираються в межу, яку я не можу подолати сам:

1. **Публікація тестової книги на живій вітрині** — НЕ зроблено. Причина:
   продакшн `bookcreator-production-7304.up.railway.app` (Railway,
   проєкт `comfortable-caring`, сервіс `Book_Creator`) зараз віддає білу
   сторінку УСІМ відвідувачам — `/studio/assets/*.js` повертає розмітку
   `index.html` замість самого JS-файлу (`content-type: text/html`),
   застосунок не запускається в браузері взагалі. Перевірено двічі, не
   збіг обставин. Не пов'язано з цією сесією — ще нічого не задеплоєно.
   **Наступний крок:** власнику перевірити останній деплой/білд-логи в
   Railway (можливий redeploy), і лише після цього повторити спробу.
2. **git push origin master у `D:\Rama\Book_Creality`** — ✅ зроблено
   власником. Спершу впав через `device_bash` (`fatal: could not read
   Username for 'https://github.com'` — немає credential.helper/TTY у
   headless-шеллі, вводити облікові дані самому заборонено правилами
   безпеки). Власник виконав `git push origin master` самостійно у
   своєму терміналі (PowerShell) — успішно, `6e30012..2f108be`. Усі
   локальні коміти (включно з git push #124, AI-коучем #125 і записом
   #126) тепер в `origin/master`.

Деталі перевірки — у `log.md`, запис #124 і розділ «⚠️ Прод (Railway)
зараз повністю не працює».

**Постановка (від власника, українською, попередня сесія):** змінити дизайн
світлої теми сторінки «Книга і текст» на прикладений макет `code.html`
(«FusionWrite»); додати блоки, яких бракує; з кнопок макета реалізувати
НОВОЮ функціональністю лише ті, що її ще не мають (наявні — не чіпати);
перелік нових функцій — на затвердження (зроблено в попередній сесії,
власник обрав максимальний обсяг для всіх трьох пунктів); після реалізації
протестувати ВСЕ; лишити «сонечко» (DraggableSun), додати ефект світлових
хвиль на фоні як від води; тестова публікація книги на вітрині з прогоном
по конвеєру, у тестовій книзі — застосувати всі нові й старі функції
розділу; звіт — артефактом.

**Затверджений обсяг (AskUserQuestion, попередня сесія):** усі
формат-функції повністю; повна панель керування «Вода і відблиски», як у
макеті; тулбар — лише косметичне перегрупування наявного в «Текст»/«AI
Асистент» (без нових Макросів/режиму «Новий»).

## Що зроблено

1. **Дизайн-токени liquid-glass + вода на фоні світлої теми** — CSS-змінні
   `--glass-*`/`--water-tint-*`, `.app-shell-root` радіальний градієнт з
   `@keyframes waterDrift` (42s, `prefers-reduced-motion` враховано),
   `.editor-shell-glass`/`.nova-char-panel` напівпрозорі з `backdrop-filter`.
2. **Панель «Вода і відблиски»** — повний пульт (canvas-анімація хвиль/
   відблисків, рівні low/medium/high/ultra, швидкість, частота, мерехтіння,
   кнопка «сплеск»), `WaterCausticsCanvas.tsx` + `WaterCausticsPanel.tsx`,
   налаштування в `usePersistentState`.
3. **Нові Tiptap-позначки форматування:** Colors (`TextColorMark`),
   Highlight (`HighlightMark`), Link (`LinkMark`, mark `nlink`) і блоковий
   Divider (`DividerNode`, node `sceneDivider`) — усі повністю проведені
   через `utils/manuscriptDoc.ts` (парсинг + серіалізація +
   `markerOffsetToDocPos`) і `utils/helpers.ts` (`renderSectionContentHtml`,
   один choke-point для класичної/редакторської верстки і «Розворот
   книги»), з кнопками/попапами в тулбарі та i18n uk/en.
4. **Find & Replace, авто-підбір «Складу глави» зі сцен, AI-аналіз сцени**
   — виявлено вже готовими в коді; додано лише нову точку входу («Аналізувати
   сцену» в панелі «Персонажі і сцена») там, де бракувало UI.
5. **Тулбар «Текст»/«AI Асистент»** — косметичні вкладки-перемикачі над
   панеллю форматування (`formatTabFor`), що показують/ховають наявні групи
   кнопок через `contents`/`hidden` — жодна кнопка не видалена й не
   переписана, тільки згруповано.

## Свідомо відкладено (НЕ реалізовано в цій сесії)

**Таблиця, «Зміст книги» (TOC), «Розрив сторінки», Формула, заголовки
H1/H2/H3** — з переліку формат-функцій макета. Причина: кожен вимагає
значно ширших змін, ніж Link/Divider — переввімкнення відповідних вузлів
StarterKit, обізнаність PDF-рушія пагінації про нові блокові типи, і
головне: DOCX/EPUB-експортер (`src/utils/fileExporters.ts`) взагалі не
парсить bracket-маркери (пише їх у файл як є) — новий блоковий тип там
просто зламається без окремої, більшої за обсягом роботи. Рішення:
задокументувати як явний залишок, а не поспішати з ризикованою
реалізацією «на коліні».

**Тестова публікація на ЖИВІЙ вітрині** (`/api/admin/marketplace-bridge/
test-publish` або власна «Опублікувати у вітрину» книги) — власник дав
пряме підтвердження в наступній частині цієї ж сесії (запис #124), і
спроба таки була зроблена — але зараз заблокована технічно (продакшн не
завантажується). Див. розділ «⛔ Незавершено» на початку цього файлу.
Раніше, до підтвердження, «прогін по конвеєру книги» був перевірений там,
де це було безпечно без публічної публікації: реальний рендер-конвеєр
(`renderSectionContentHtml` — той самий, що йде і в PDF/HTML-експорт, і в
«Розворот книги») прогнаний на збереженому через API контенті розділу з
усіма новими й старими маркерами одночасно
(bold/italic/FONT/SIZE/COLOR/HL/LINK/DIVIDER) — і в живому браузері
(«Розворот книги», сторінки 7–8), і напряму викликом функції.

## Перевірка

- `npx tsc --noEmit` — чисто.
- `scripts/test-manuscriptDoc.mts` — 17/17 (COLOR/HL/LINK/DIVIDER round-trip
  окремо і в комбінаціях зі старими маркерами, авто-закриття незакритих
  маркерів, `markerSnippetToNodes`).
- Наскрізний Playwright-прогін у справжньому dev-редакторі (реальна сесія
  через `scripts/dev-seed-session.mts`, не обхід прав): виділення слова →
  Link → збереження через `POST` API → перевірка `[LINK=…]`/`[DIVIDER]` у
  збереженому JSON книги → перевірка `<a>`/`<hr>` в HTML-рендері → перевірка
  того самого в живому «Розворот книги» (сторінка з реальним текстом).
  Вкладки «Текст»/«AI Асистент» — перемикання туди-назад, і підтверджено, що
  Жирний далі працює під вкладкою «Текст» після рефакторингу. Світла тема —
  computed style ТА пряме піксельне семплування скріншота (перше візуальне
  враження від зображення виявилось оманливим — обʼєктивні пікселі
  підтвердили коректний світлий фон).

## Чого перевірка НЕ покриває

- **DOCX/EPUB-експорт маркерів** — вже наявна вада (не цієї сесії): жоден
  bracket-маркер (bold/italic/FONT/SIZE, тепер і COLOR/HL/LINK) не
  розгортається в `fileExporters.ts`, там вони йдуть у файл як сирий текст.
  Стосується і старих, і нових позначок однаково — не regressed цією
  сесією, але й не полагоджено.
- **Живий сервер vs `device_bash`.** Уся робота велась у свіжому хмарному
  клоні (`/root/work/bookcreality`), не на пристрої власника — перенесення
  й фінальний коміт роблю окремим кроком одразу після цього запису.
- **Мобільна/планшетна ширина** нового дизайну — перевірено в запису
  #124 (390px/768px): без горизонтального скролу, але контент витіснено
  нерухомим сайдбаром/панеллю — наскрізна, доволідавня вада всього
  застосунку (не цього редизайну), не полагоджена, бо потребує окремого
  проєкту.
- Мокап мав додаткові елементи поза затвердженим переліком (наприклад,
  Макроси, режим «Новий» у тулбарі) — свідомо НЕ реалізовувались за
  власним вибором власника («тільки перегрупувати наявне»).

---

# Завдання 4: проміжна сторінка «Курс Fusion для мебелярів»

> Сесія: 08.09.2026 #04 (Codex). Стан: реалізовано локально, перевірено й запушено; не задеплоєно.

- Джерело змісту: `C:/Users/Денис/Downloads/Kurs-Fusion-dlya-mebelyariv.pdf`, 10 сторінок. Зберегти дизайн чинного сайту.
- Перевірити результати попередньої сесії до змін. У Book_Creality, marketplace та site окремої проміжної сторінки не знайдено. У marketplace уже є загальна сторінка товару, бібліотека та імпортована програма furniture-makers; використати їх.
- Ціль за контекстом: новий маркетплейс `D:/Rama/furnivision-architecture/marketplace`. Уточнення користувачу надіслано через наявність також старого сайту `kurs.html`.
- Шлях: картка furniture-makers → `/catalog/furniture-makers/overview` → чинна сторінка деталей/купівлі. Інші картки не змінювати.
- Зміст: опис, результати, аудиторія, 5 тематичних етапів, практичні поради навчання, особистий проєкт, актуальна програма, ціна та перехід до запису/бібліотеки.
- Програму, кількість занять і ціну брати з API; не вигадувати доступність відео або домашніх робіт. Не переносити непідтверджені числові твердження про мозок із PDF.
- Публічна сторінка не отримує адрес платних відео та не містить редагування. Зберегти чинний контроль доступу.
- Перевірити типи/лінт, переходи, адаптивність. Код запушено в marketplace комітом `53bb86a`.

**Результат:** у marketplace додано `apps/web/src/app/[locale]/catalog/[slug]/overview/page.tsx`, `apps/web/src/components/furniture-course-overview.tsx`; змінено лише посилання картки furniture-makers у `listing-card.tsx`. Збережено штатні кольори, шрифти, шапку й футер. Підтверджено 5 модулів / 25 занять / 8400 грн з чинного API. TypeScript, ESLint і git diff --check пройшли. У браузері перевірено desktop, мобільну ширину 390 px, якір програми, акордеон і шлях картка → огляд → деталі купівлі.

**Не покрито:** реальна оплата, обліковий запис студента, відтворення платних відео, production build/deploy. На чинній сторінці деталей у локальному перегляді помилка Failed to fetch для відгуків (причину не встановлено); це окрема наявна інтеграція. Результат попередньої сесії не приписується поточній: використано вже наявні сторінки та імпортовану програму.

---

# Завдання: тренажер «Стиль письменника»

> Сесія: 08.09.2026. Джерело структури — макет Stitch (`code.html` + `DESIGN.md`).

## Постановка

У розділі «Тренажери» (`https://www.fusionlab.in.ua/studio/`) є дві вкладки:
**Персонаж** і **Діалог**. Перед ними додати третю вкладку — **«Стиль
письменника»**, яка є діалогом з ШІ-ядром за такою схемою.

## Структура тренажера

### 8 компонентів авторського стилю

Стиль письменника — унікальне поєднання восьми взаємопов'язаних елементів:

- 💡 **Думка** — авторська думка про сцену, персонаж, діалог та інші сутності книги.
- 🔥 **Воля** — суб'єктивне бажання висловити правду про свої думки у творі.
- 🖼 **Образність** — здатність яскраво бачити персонажів і світ та передавати це читачеві.
- 🎵 **Ритм** — темп і музикальність речень, їх довжина та чергування.
- 💬 **Діалоги** — природність, характерність і функція мовлення персонажів.
- 📖 **Лексика** — вибір слів: рівень, тон, унікальні мовні маркери автора.
- ❤️ **Емоційність** — глибина і достовірність емоційного впливу на читача.
- 🔍 **Деталізація** — рівень і точність деталей, що створюють ефект присутності.

### Вправи · Частина 1 (Думка · Воля · Образність · Ритм)

ШІ обирає уривки з книги автора і ставить цільові запитання для кожного елемента.

**Думка:** «Стоп-кадр» (10 хв), «Підтекст» (10 хв), «Авторський коментар» (15 хв).
**Воля:** «Лист до читача» (10 хв), «ШІ-провокатор» (10 хв), «Маніфест сцени» (10 хв).
**Образність:** «ШІ-дзеркало» (15 хв), «Кінорежисер» (15 хв), «Порівняння образів» (10 хв).
**Ритм:** «ШІ-метроном» (10 хв), «Читання вголос» (10 хв), «Монтаж ритму» (15 хв).

### Вправи · Частина 2 (Діалоги · Лексика · Емоційність · Деталізація)

**Діалоги:** «ШІ-слухач» (10 хв), «Голос персонажа» (10 хв), «Мовчання» (10 хв).
**Лексика:** «ШІ-словник» (10 хв), «Заборонені слова» (10 хв), «Словниковий портрет» (15 хв).
**Емоційність:** «ШІ-провокатор» (10 хв), «Термометр емоцій» (10 хв), «Без прикметників» (15 хв).
**Деталізація:** «ШІ-оператор» (15 хв), «Одна деталь» (10 хв), «Детектив» (10 хв).

## Вимоги до реалізації

1. Зберегти дизайн сайту (темна студія, `nova-glass-dark`, акценти amber/cyan).

---

# Завдання 2: «Мої книги» (створити/видалити) + акцент сонечка в панелях

> Сесія: 08.09.2026.

## Частина A — блок «Мої книги» на дашборді

1. У блоці «Мої книги» (вкладка «Дашборд») додати **велику кнопку зверху**
   «Створити нову книгу».
2. Під уже створеними книгами додати **«Видалити книгу»** з перезпитанням:
   «Ви точно хочете удалити обрану книгу?».
3. По натисканні «Створити нову книгу» зʼявляється вибір:
   - «Чи ви хочете створити книгу візардом?» → наявний `CreateBookModal`;
   - «Чи ви хочете створити книгу з нульової планки» → порожній аркуш нової
     книги в «Книга & Текст» **без героїв і сюжету** (усі записи героїв і
     сценарію обнулені).
4. Рівень письменника (майстерність, XP у тренажерах і тестах) **не чіпати**.
5. Після створення «з нульової планки» власник додатково вкаже, що ще анулювати.

## Частина B — акцент «Сонечка» в панелях і тулбарі

1. Кольоровий текст у **панелі інструментів набору тексту** зробити кольором
   вибраного «сонечка» (12 кольорів, `DraggableSun`) — так, як це вже робить
   верхня панель (`HeaderNav` → CSS-змінні `--sun-acc*`).
2. Те саме на **лівій панелі** (SidebarNav) та **правій/лівій панелях** сторінки
   білої теми (тулбар редактора, дерево розділів, панель «Персонажі і сцена»).
3. Чорний/сірий текст на панелях (у денній темі) лишається чорним/сірим.

## Реалізація

- `src/utils/sunAccent.ts` — спільний хук `useSunAccentVars()` (CSS-змінні
  `--sun-acc`, `--sun-soft`, `--sun-acc-10..90`) + `darkenHex`.
- `src/utils/storage.ts` — `deleteBook(id)` (IndexedDB + localStorage + знімки).
- `src/App.tsx` — `handleCreateBlankBook` (порожня книга) і `handleDeleteBook`
  (перемикання активної книги / порожня, якщо не лишилось).
- `src/components/DashboardView.tsx` — кнопка «Створити нову книгу», «Видалити»
  на кожній книзі, модалки вибору способу та підтвердження видалення.
- `src/i18n/dictionaries/dashboard.ts` — ключі `createBook`, `deleteBook*`,
  `createBook*`.
- `src/components/EditorView.tsx` + `SidebarNav.tsx` — акценти amber/cyan/
  emerald → `var(--sun-acc*)`, `style={sunVars}` на контейнерах.

---

# Завдання 3: рушій ядра в тулбарі + «Шлях героя» + фільтр моделей генерації

> Сесія: 08.09.2026.

## Частина A — вибір моделі в панелі інструментів

1. Над полем вибору моделі в тулбарі набору тексту додати надпис:
   «Обрано тепер в ядрі сайта модель: {назва}» (або «Автоматично»).
2. Перевірити, хто має доступ до вибору моделі.
3. Користувач вибирає з моделей, чиї ключі введені адміністратором і активні.

## Частина B — кнопка «Шлях героя»

1. У «Книга & Текст» поруч із «Згенерувати героя» додати третю кнопку
   «Шлях героя».
2. У нових вспливаючих вкладках відобразити «Шлях героя» з файлів (крива
   сюжету, діагностика, зведення кроків), зберігши дизайн сайту.
3. Додати логіку посилання на ШІ, активні в ядрі сайту.

## Частина C — фільтр моделей генерації героя

1. У «Згенерувати нового героя» показувати лише ті image-моделі, що додані
   адміністратором і мають ключі в статусі «Активні».
2. Всі промпти ШІ адресувати ядру сайту (поточний рушій-модель).

## Реалізація

- `EditorView.tsx` — надпис над селектором моделі; кнопка «Шлях героя»;
  рендер `HeroJourneyModal`; `preferredAiModelId` прокинуто в
  `GenerateCharacterModal`.
- `HeroJourneyModal.tsx` (новий) — 3 вкладки: Конструктор (13 кроків),
  Крива сюжету (SVG + повзунки 3 актів), Підсумок (діагностика + зведення);
  ШІ-синхронізація → `/api/ai/analyze-emotional-arc` з `modelId`.
- `GenerateCharacterModal.tsx` — `/api/ai/image-engines` → фільтр
  `visibleModelOptions` лише з активними ключами.
- `server.ts` — `analyze-emotional-arc` приймає `modelId` і маршрутизує
  через `generateAiText` (ядро, мультипровайдер) з обраним рушієм.
- i18n `editor.ts` — ключі `aiModelSelectedLabel`, `heroJourney*`.


2. Відповіді автора можна додавати до тексту книги через виділення мишкою:
   - **за тегом** — тег автор заздалегідь додав у текст (`тега: #...`);
   - **у кінець обраної глави** (або розділу).
3. Реалізувати дії кнопок і взаємодію з ШІ (оцінка відповіді за 3 критеріями).
4. Протестувати: вставку відповідей у книгу та AI-оцінку з кожного тренажера.

## Точки інтеграції (знайдені під час аналізу)

- `src/components/TrainersView.tsx` — перемикач вкладок (character/dialogue).
- `src/components/TrainerView.tsx` — спільний движок тренажера.
- `src/App.tsx:1874` — рендер `<TrainersView />`; `handleSendChatTextToChapter`
  (додавання тексту в кінець глави) і `handleUpdateBook`.
- `src/utils/bookTags.ts` — `collectBookTags` / `insertTextAfterTag` (вставка за тегом).
- `src/utils/bookText.ts` — `appendTextToChapterEnd`.
- `src/context/WriterBookContext.tsx` — `bookExcerpts` (уривки для запитань ШІ).
- `server.ts:3757` — `/api/ai/evaluate-trainer` (критерії character/dialogue).
